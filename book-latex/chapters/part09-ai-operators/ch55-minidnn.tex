\plannedchapter{MiniDNN：教学版 AI CPU 算子库}
{本章把 normalization、activation、matmul、attention 组织成一个小型算子库。}
{\item operator API 如何设计？
\item primitive cache 和 shape specialization 如何工作？
\item 如何保证不同 ISA 路径输出一致？}
{\item tensor descriptor、memory layout、primitive descriptor。
\item ISA dispatch、workspace、scratchpad。
\item correctness tests、benchmark suite、regression tracking。}
{交付 MiniDNN，小规模复现 layernorm、GELU、matmul epilogue 和 attention kernel。}
