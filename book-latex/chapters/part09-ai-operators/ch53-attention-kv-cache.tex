\plannedchapter{Attention、KV Cache 和 Streaming Softmax}
{本章讲 transformer attention 在 CPU 上的数据流、瓶颈和优化方向。}
{\item prefill 和 decode 阶段瓶颈为何不同？
\item KV cache layout 如何决定带宽和 TLB 行为？
\item streaming softmax 如何避免 materialize 大矩阵？}
{\item QK、softmax、PV 三段模型。
\item \code{head_dim}、\code{seq_len}、batch 对 FLOP/byte 的影响。
\item KV cache paging、stride、layout。}
{实现单头 attention reference、streaming softmax、小型 KV cache layout benchmark。}
