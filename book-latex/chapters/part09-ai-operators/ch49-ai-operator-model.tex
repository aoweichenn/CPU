\plannedchapter{AI CPU 算子性能模型}
{本章建立 AI 算子的通用分析框架：shape、dtype、layout、FLOPs、bytes、reuse、vectorization 和数值误差。}
{\item 一个 AI 算子如何从数学公式变成 CPU kernel？
\item tensor layout 如何影响 SIMD 和 cache？
\item latency-oriented 和 throughput-oriented 优化有什么不同？}
{\item tensor shape/stride/layout。
\item dtype、quantization、broadcast、reduction。
\item operator benchmark、framework overhead 与 kernel overhead。}
{选择 5 个小算子，写 shape、FLOPs、bytes、expected bottleneck 表。}
