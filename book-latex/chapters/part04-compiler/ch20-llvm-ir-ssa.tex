\plannedchapter{LLVM IR、SSA 和控制流图}
{本章讲读懂 LLVM IR 所需的核心概念：SSA、phi、basic block、load/store、branch 和 metadata。}
{\item SSA 为什么适合优化？
\item loop 中的变量为什么变成 phi？
\item IR 里的 load/store 和 C++ 变量是什么关系？}
{\item SSA def-use chain。
\item CFG、dominance、phi 节点。
\item typed values、memory operations、attributes。}
{对 sum loop 生成 \code{-O0/-O3} IR，标注 basic block 和 phi 节点。}
