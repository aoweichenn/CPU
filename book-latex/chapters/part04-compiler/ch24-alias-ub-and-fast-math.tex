\plannedchapter{Alias、未定义行为和浮点语义}
{本章讲 C/C++ 语义如何给优化器提供假设，也如何让错误代码产生不可预测结果。}
{\item undefined behavior 为什么会改变汇编？
\item strict aliasing 如何影响 load/store 重排？
\item fast-math 到底放弃了哪些语义？}
{\item signed overflow、out-of-bounds、uninitialized read。
\item alias analysis、restrict、TBAA。
\item IEEE 浮点、NaN、Inf、reassociation、FMA contraction。}
{写 UB 示例，比较 \code{-O0/-O3}；写 fast-math 前后 reduction 结果和汇编差异。}
