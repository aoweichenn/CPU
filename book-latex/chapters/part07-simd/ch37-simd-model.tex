\plannedchapter{SIMD 思维模型}
{本章从数据并行的角度讲 SIMD：一个指令处理多个 lane，为什么它适合数组和 tensor。}
{\item SIMD lane 是什么？
\item scalar loop 如何变成 vector loop？
\item tail、alignment 和 mask 为什么不可避免？}
{\item XMM/YMM/ZMM register。
\item packed integer/floating operations。
\item vector length、lane、horizontal vs vertical operation。}
{写 scalar vector add，观察自动向量化生成的 AVX2 汇编。}
