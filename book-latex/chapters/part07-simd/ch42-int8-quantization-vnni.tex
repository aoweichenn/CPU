\plannedchapter{int8 量化、AVX-VNNI 和低精度推理}
{本章讲 AI 推理中的 int8 数据路径，从量化误差到 dot product 指令。}
{\item scale 和 zero-point 如何把 float 映射到 int8？
\item int8 dot 为什么累加到 int32？
\item AVX-VNNI 相比 AVX2 fallback 优势在哪里？}
{\item quantize/dequantize、rounding、saturation。
\item signed/unsigned int8 combinations。
\item VNNI dot-product 指令和 fallback 实现。}
{实现 int8 dot reference、AVX2 fallback、AVX-VNNI 版本，写误差与性能报告。}
