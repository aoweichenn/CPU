\plannedchapter{FMA、归约和浮点误差}
{本章讲 FMA 指令、dot product、horizontal reduction 和浮点累加顺序。}
{\item FMA 为什么既影响性能也影响数值？
\item SIMD reduction 为什么比 elementwise 更复杂？
\item 多累加器如何隐藏 FMA latency？}
{\item \code{vfmadd} 指令族。
\item horizontal add、shuffle reduction。
\item error model、reassociation、fast-math。}
{实现 scalar dot、auto-vectorized dot、AVX2 FMA dot，并写误差报告。}
