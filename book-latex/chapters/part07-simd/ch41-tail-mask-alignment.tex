\plannedchapter{Tail、Mask、Alignment 和边界策略}
{本章讲 SIMD kernel 的工程边界：非整齐长度、小 shape、对齐、padding 和 remainder kernels。}
{\item tail 处理为什么影响小 shape 延迟？
\item padding、scalar tail、masked load/store 如何取舍？
\item 对齐优化什么时候值得？}
{\item prologue/main loop/epilogue。
\item AVX2 mask load/store 的限制。
\item small-size dispatch 与 code size。}
{为 ReLU、vector add、dot 设计三种 tail policy 并 benchmark。}
