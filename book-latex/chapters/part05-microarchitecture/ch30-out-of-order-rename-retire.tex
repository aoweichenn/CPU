\plannedchapter{乱序执行、寄存器重命名和提交}
{本章深入现代 CPU 如何在保持单线程语义的前提下乱序执行。}
{\item 为什么 cache miss 后 CPU 不一定完全停住？
\item 寄存器重命名如何消除假依赖？
\item reorder buffer 为什么需要按序提交？}
{\item physical register file、ROB、reservation station。
\item load/store queue、memory disambiguation。
\item precise exception 和 speculation rollback。}
{构造独立链和依赖链，比较乱序执行能隐藏哪些延迟，哪些隐藏不了。}
