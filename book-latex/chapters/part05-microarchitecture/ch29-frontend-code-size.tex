\plannedchapter{前端带宽、I-cache、uop cache 和代码尺寸}
{本章讲为什么过度 inline、过度 unroll 和模板膨胀会让代码在前端变慢。}
{\item 代码越展开为什么不一定越快？
\item I-cache 和 uop cache 如何影响 hot loop？
\item operator fusion 如何在减少内存流量和增加代码尺寸之间取舍？}
{\item instruction fetch、decode bandwidth、uop cache。
\item code layout、hot/cold path、function alignment。
\item loop body size、unroll factor 和 front-end bound。}
{写不同 unroll 因子的 kernel，记录 text size、汇编大小和 benchmark。}
