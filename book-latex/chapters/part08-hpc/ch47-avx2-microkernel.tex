\plannedchapter{AVX2 GEMM Microkernel}
{本章讲 register blocking 和 AVX2 FMA microkernel 的设计。}
{\item MR/NR 为什么由寄存器数量、FMA latency 和 vector width 共同决定？
\item accumulator 如何常驻寄存器？
\item microkernel 如何与 packing 约定接口？}
{\item register tile。
\item broadcast A、vector load B、FMA accumulate。
\item unroll K、load/FMA scheduling、store C。}
{实现 4x8 或 6x8 fp32 AVX2 microkernel，用 \code{llvm-mca} 和 benchmark 分析。}
