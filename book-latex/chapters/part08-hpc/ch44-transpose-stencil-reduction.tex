\plannedchapter{Transpose、Stencil 和 Reduction}
{本章用三个典型 memory-bound kernel 训练 cache blocking、边界处理和归约设计。}
{\item transpose 为什么 naive 很慢？
\item stencil 如何复用邻域数据？
\item parallel/SIMD reduction 如何处理依赖和误差？}
{\item cache blocking for transpose。
\item stencil halo、boundary、temporal blocking 初步。
\item reduction tree、multiple accumulators、determinism。}
{实现 naive/blocked transpose、1D/2D stencil、scalar/SIMD reduction，并用 roofline 解释。}
