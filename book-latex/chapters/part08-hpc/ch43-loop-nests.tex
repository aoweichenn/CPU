\plannedchapter{Loop Nest 性能模型}
{本章讲多层循环如何决定数据复用、向量化、并行划分和 cache 行为，是 GEMM、stencil、tensor 算子的共同基础。}
{\item loop order 如何改变内存访问模式？
\item interchange、blocking、fusion、fission 分别解决什么？
\item 如何从循环结构推导 bytes 和 FLOPs？}
{\item iteration space、dependence、reuse distance。
\item loop transformation correctness。
\item compiler optimization 与手工重排的边界。}
{对 matrix-vector、matrix transpose、2D stencil 改变 loop order 并 benchmark。}
