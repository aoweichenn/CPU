\plannedchapter{GEMM：从 naive 到 cache blocking}
{本章从矩阵乘第一性原理出发，解释为什么 GEMM 可以接近峰值，以及 naive 为什么做不到。}
{\item GEMM 的 FLOP 和 bytes 如何计算？
\item loop order 如何影响 B 矩阵访问？
\item block size 如何与 cache 容量关联？}
{\item arithmetic intensity of GEMM。
\item ijk/ikj/jik loop order。
\item L1/L2/L3 blocking 和 C tile reuse。}
{实现 naive GEMM、loop-order variants、blocked GEMM，扫描 tile size 并写 roofline 报告。}
