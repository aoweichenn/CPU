\plannedchapter{内存层级：寄存器、cache、DRAM}
{本章从 CPU 与 DRAM 速度差距讲起，建立 memory hierarchy 的第一性原理。}
{\item 为什么需要多级 cache？
\item SRAM 和 DRAM 的角色差别是什么？
\item 工作集大小如何决定瓶颈？}
{\item register file、L1/L2/L3、DRAM。
\item latency、bandwidth、capacity 的三角关系。
\item inclusive/exclusive/non-inclusive cache 的概念边界。}
{做工作集扫描，观察 ns/element 和 GB/s 随数组大小变化的台阶。}
