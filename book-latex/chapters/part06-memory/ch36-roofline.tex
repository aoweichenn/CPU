\plannedchapter{Roofline：FLOP、Bytes 和 Arithmetic Intensity}
{本章建立判断 compute-bound 与 memory-bound 的基础模型。}
{\item 为什么只数 FLOP 会误导？
\item arithmetic intensity 如何计算？
\item roofline 能解释什么，不能解释什么？}
{\item FLOP counting、byte traffic、write allocate。
\item peak compute、memory bandwidth、attainable performance。
\item cache-aware roofline 和多层 roofline 初步。}
{对 vector add、SAXPY、dot、naive GEMM 计算 AI 并测量 GFLOP/s、GB/s。}
