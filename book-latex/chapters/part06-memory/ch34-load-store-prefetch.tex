\plannedchapter{Load/Store、store buffer、prefetch 和写入策略}
{本章讲内存访问在 CPU 内部如何排队、发射、等待和写回。}
{\item load miss 如何影响依赖链？
\item store 为什么可能引发 read-for-ownership？
\item prefetch 什么时候有用？}
{\item address generation unit、load queue、store queue。
\item store buffer、store forwarding、write allocate。
\item hardware/software prefetch 和 non-temporal store。}
{实现 copy、triad、stream store、pointer chasing，分析 load/store 瓶颈。}
