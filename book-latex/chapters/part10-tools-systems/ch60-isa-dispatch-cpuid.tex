\plannedchapter{CPUID、ISA Dispatch 和多版本 Kernel}
{本章讲生产库如何在不同 CPU feature 上选择最优实现。}
{\item 如何检测 AVX2、FMA、AVX-VNNI？
\item runtime dispatch 的开销应该放在哪里？
\item fallback 路径如何测试？}
{\item CPUID、XCR0、OS support check。
\item function pointer dispatch、ifunc、target attribute。
\item primitive cache 和 first-call overhead。}
{实现 scalar/AVX2/AVX-VNNI 三路径 dispatch，确保输出一致并测 first-call 与 steady-state。}
