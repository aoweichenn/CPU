\documentclass{article}
\usepackage[UTF8]{ctex}
\usepackage{tikz}
\usepackage[active,tightpage]{preview}
\usetikzlibrary{arrows.meta,positioning,calc}
\pagestyle{empty}

\begin{document}
\begin{preview}

\begin{tikzpicture}[
    >=Latex,
    block/.style={
        draw,
        rectangle,
        rounded corners=2pt,
        minimum width=32mm,
        minimum height=16mm,
        align=center,
        thick
    },
    port/.style={
        draw,
        rectangle,
        minimum width=16mm,
        minimum height=8mm,
        align=center,
        thick
    },
    line/.style={->, thick}
]

\node[port] (x) {输入 $X$};

\node[block, right=20mm of x] (comb) {
    组合逻辑\\
    $D = F(Q, X)$\\
    $Y = G(Q, X)$
};

\node[block, right=26mm of comb] (reg) {
    状态寄存器\\
    D 触发器
};

\node[port, above=14mm of reg] (clk) {时钟 $CLK$};

\draw[line] (x.east) -- (comb.west);

\draw[line] (comb.east) -- node[above] {下一状态 $D$} (reg.west);

\draw[line] (clk.south) -- (reg.north);

\draw[line] (comb.north) -- ++(0,12mm)
    node[above] {输出 $Y$};

\draw[thick] (reg.east) -- ++(14mm,0)
    coordinate (qout)
    node[right] {当前状态 $Q$};

\draw[line] (qout) |- ($(comb.south)+(0,-12mm)$) -| (comb.south);

\end{tikzpicture}
\end{preview}
\end{document}
