# CPU Books Conversation Export - 2026-06-23

这个目录用于把本轮 CPU 书稿工作的可见对话、当前状态和换机运行方式保存进仓库，方便在另一台机器拉取后继续跑。

目录内容：

- `TRANSCRIPT.md`：按时间顺序整理的可见对话内容和关键用户要求。
- `SESSION_STATE.md`：当前仓库、书稿、构建、导出和提交状态。
- `RUN_ON_OTHER_MACHINE.md`：在另一台机器 clone、checkout、构建、导出和继续工作的命令。

注意：这里保存的是项目协作用的可见对话和工程状态，不包含系统/开发者内部指令。
