\chapter{分布式计算工程实践}

收束项目不再新增一个孤立主题，而是把硬件、布局、并发、运行时、I/O 和分布式机制组合成一个可运行、可测量、可故障注入、可恢复的贯穿系统：Compute Systems Engine。它处理一批日志记录，按 key 聚合计数，经过 map、combine、shuffle、reduce 和 manifest commit，最后输出确定结果。目标不是替代 Spark、Flink 或数据库执行器，而是在可控规模内把现代计算系统的关键边界写清：数据怎样进入热路径，任务怎样并行，I/O 怎样可靠完成，远端消息怎样提交，失败后谁是权威事实。

先看最终项目要能复现的一次完整事故。输入有三个 split，其中一个 split 含非法字段，一个 key 是热点；worker 1 写出 task 0 后，Driver 在 manifest 提交后崩溃；worker 2 的 task 1 第一次写出遇到短写；task 2 的成功响应被 MessageBus 丢弃，Driver 超时重试，旧响应随后迟到。正常路径里这只是一个日志统计作业，事故路径里它同时考验解析错误、I/O 可靠写、任务状态、重试、幂等提交、热点 key、trace 和恢复。

判断标准很简单：程序不能只跑通，还要能解释程序为什么可信。解释必须落到具体对象：\code{InputSplit}、\code{TaskId}、\code{AttemptId}、\code{ShuffleBlock}、\code{ManifestEntry}、\code{MessageEnvelope}、\code{Checkpoint} 和 \code{RunReport}。如果一个结果出错，报告应能把它缩小到解析、分区、写出、提交或恢复中的某个边界，而不是让人翻日志猜。

\section{最终系统的第一条主线：Reference 先行}

任何分布式计算项目都要先有可验证 reference。Reference 可以慢，但必须定义语义：输入格式是什么，坏行如何处理，key 如何规范化，计数类型是否溢出，输出顺序是否稳定，错误摘要如何记录。没有 reference，后面的线程池、shuffle、重试、checkpoint 和热点优化都没有正确性锚点。

\topic{日志记录和输出语义}
Compute Systems Engine 可以从固定字段日志开始：timestamp、user id、event type 和 value。Reference 单线程读取全部输入，跳过或记录坏行，按 event type 累加 value，最后按 event type 排序输出。优化版本可以改变布局、并行方式、shuffle 方式和写出方式，但不能改变这份语义。

\begin{lstlisting}[language=C++,caption={Reference 使用的稳定数据合同}]
struct LogRecord {
  std::uint64_t timestamp = 0;
  std::uint64_t user_id = 0;
  std::uint32_t event_type = 0;
  std::int64_t value = 0;
};

struct EventCount {
  std::uint32_t event_type = 0;
  std::int64_t total = 0;
};
\end{lstlisting}

这段结构体是内存语义，不等于文件格式。文件格式可以是文本、二进制或列式块，但解析后必须进入稳定数据合同。若未来加入 schema 版本、字符串 key 或变长字段，也要先扩展 reference，再扩展优化路径。

\topic{输入身份}
输入 split 不能只是“第几个任务”。它要能定位字节范围和恢复边界。文本文件按字节切分时，非零 begin 要跳到下一行起点；end 可以读到当前行结束；错误记录要能回到 file id、offset 和 record id。若每次运行都无法证明处理的是同一批字节，benchmark 和恢复报告都不可信。

\begin{lstlisting}[language=C++,caption={输入分片元数据}]
struct InputSplit {
  std::uint64_t split_id = 0;
  std::uint64_t file_id = 0;
  std::uint64_t begin_offset = 0;
  std::uint64_t end_offset = 0;
  std::uint64_t input_checksum = 0;
};
\end{lstlisting}

\code{split_id} 是逻辑 task 的基础。Attempt 重试时 \code{split_id} 不变，物理执行变的是 \code{attempt}。这个区分贯穿全章：逻辑身份决定语义，物理 attempt 只是实现路径。

\section{端到端架构：控制面和数据面分开}

Compute Systems Engine 的架构可以分成两条路径。数据面搬运和处理大块数据：读取 split、解析 batch、本地 combine、写 shuffle block、reduce 合并。控制面维护小而关键的事实：task 状态、attempt、manifest、checkpoint、route epoch、retry budget 和 recovery audit。控制面决定哪些数据有效，数据面负责高吞吐处理。

\begin{systemdiagram}{最终引擎：数据面搬运大块，控制面定义有效性}
\begin{pdfdiagram}
\node[book title label] at (0,1.05) {数据面};
\node[book data,text width=1.75cm] (input) at (0,0.42) {InputSplit};
\node[book data,text width=1.75cm] (parse) at (2.0,0.42) {HotBatch};
\node[book data,text width=1.75cm] (combine) at (4.0,0.42) {local\\combine};
\node[book data,text width=1.75cm] (shuffle) at (6.0,0.42) {Shuffle\\Block};
\node[book data,text width=1.75cm] (reduce) at (8.0,0.42) {Reduce\\Output};

\node[book title label] at (0,-1.05) {控制面};
\node[book state,text width=1.75cm] (task) at (0,-1.68) {Task\\Attempt};
\node[book amber node,text width=1.75cm] (bus) at (2.0,-1.68) {Message\\Envelope};
\node[book amber node,text width=1.75cm] (commit) at (4.0,-1.68) {Commit\\Record};
\node[book purple node,text width=1.75cm] (manifest) at (6.0,-1.68) {Manifest};
\node[book state,text width=1.75cm] (checkpoint) at (8.0,-1.68) {Checkpoint};

\draw[book strong arrow] (input) -- (parse);
\draw[book strong arrow] (parse) -- (combine);
\draw[book strong arrow] (combine) -- (shuffle);
\draw[book strong arrow] (shuffle) -- (reduce);
\draw[book strong arrow] (task) -- (bus);
\draw[book strong arrow] (bus) -- (commit);
\draw[book strong arrow] (commit) -- (manifest);
\draw[book strong arrow] (manifest) -- (checkpoint);
\draw[book dashed arrow] (shuffle) -- (manifest);
\draw[book dashed arrow] (reduce) -- (manifest);

\node[book label,text width=8.8cm] at (4.0,-3.08) {数据面可以重做、重发、重读；控制面决定哪个 attempt 的哪个 block 对下游可见。最终系统的恢复能力来自这条边界。};
\end{pdfdiagram}
\diagramnote{本图要证明的命题是：最终计算引擎不是一个大函数，而是数据面和控制面共同维护的状态机。}
\end{systemdiagram}

\topic{模块边界}
Parser 接收 \code{InputSplit}，输出 batch 和错误摘要；它不提交结果。Kernel 接收热字段 span，输出本地 partial；它不理解重试。Partitioner 接收 partial，输出按 partition 分组的 block；它不写 manifest。I/O 层负责临时文件、buffer 生命周期、短写和 checksum；它不决定业务生效。Driver 维护 task table、retry、commit 和 checkpoint；它不解析每行日志。MessageBus 传递消息和故障事件；它不决定业务成功。

边界清楚不是为了目录好看，而是为了事故时能定位责任。若 Parser 直接写 manifest，解析错误会污染提交语义；若 I/O completion handler 直接提交业务结果，短写和迟到 completion 会绕过状态表；若 Runtime 理解业务 key，任务运行时就无法复用到后续推理引擎。

\topic{目录建议}
项目可以按责任组织：\filepath{engine/model} 放稳定 ID 和消息结构；\filepath{engine/reference} 放串行 reference；\filepath{engine/input} 放 split 和解析；\filepath{engine/kernel} 放 combine、filter、partition；\filepath{engine/runtime} 放队列、线程池和任务图；\filepath{engine/io} 放可靠写和 block 校验；\filepath{engine/driver} 放 task 状态、manifest 和 checkpoint；\filepath{engine/fault} 放故障注入；\filepath{engine/report} 放 RunReport。

\section{Map、Combine、Shuffle 和 Reduce}

朴素 shuffle 会把每条记录都发给 reduce。若一亿条日志只有一万个 event type，这样会浪费大量网络、序列化和写盘成本。Map-side combine 先在每个 worker 本地按 key 聚合，再把 partial 发给 reduce。发送量从记录数下降到本地出现过的 key 数。这是第 13 章 reduce 原则在网络尺度上的应用。

\topic{本地聚合策略}
如果 event type 是小范围整数，可以用连续数组，内存访问规则、容易向量化；如果 key 稀疏或是字符串，可以用哈希表，但要承担随机访问、分配和 rehash 成本；如果输入已经按 key 排序，可以顺序合并。项目报告不应只写“用了哈希表”，而要记录 key 基数、最大 bucket、combine ratio、内存峰值和输出 block 数量。

\begin{lstlisting}[language=C++,caption={小整数 key 的本地 combine}]
std::vector<std::int64_t> combine_events(
    std::span<const LogRecord> records,
    std::uint32_t event_type_count) {
  std::vector<std::int64_t> partial(event_type_count, 0);
  for (const LogRecord& record : records) {
    if (record.event_type < event_type_count) {
      partial[record.event_type] += record.value;
    }
  }
  return partial;
}
\end{lstlisting}

真实项目不能静默忽略越界 event type。示例片段只突出 combine 结构；生产版应把非法 key 进入错误摘要，并让 reference 和优化路径共享同一错误合同。

\topic{ShuffleBlock 和 Manifest}
Shuffle 可以先用本机内存队列模拟，也可以写本地文件再由 reduce 拉取。无论哪种方式，都要有 manifest 描述哪些 block 有效。Reduce 不应扫描临时目录，也不应接受 MessageBus 里随机到达的所有 partial。它只读取 manifest 接受的 block。

\begin{lstlisting}[language=C++,caption={Shuffle block 和 Manifest entry}]
struct ShuffleBlock {
  std::uint64_t block_id = 0;
  std::uint64_t map_task_id = 0;
  std::uint32_t attempt = 0;
  std::uint32_t reduce_partition = 0;
  std::uint64_t byte_size = 0;
  std::uint64_t checksum = 0;
};

struct ManifestEntry {
  std::uint64_t generation = 0;
  std::uint64_t logical_task_id = 0;
  std::uint32_t accepted_attempt = 0;
  ShuffleBlock block;
};
\end{lstlisting}

Manifest entry 是提交事实。临时文件存在只是存储事实；worker 返回 Ok 只是执行事实；只有 manifest 接受某个 logical task 的某个 attempt，下游才允许读取对应 block。这个规则是最终项目可靠性的中心。

\topic{Reduce 端去重}
Reduce 端最危险的错误是重复合并同一个 logical map task。Attempt 0 写出后响应丢失，Attempt 1 重试并提交；如果 Reduce 扫描目录，它会把两个 block 都读进去，计数翻倍。正确策略是 reduce 根据 manifest 拉取已接受 attempt，或按 logical task 去重。按 attempt 去重不够，因为不同 attempt 仍可能属于同一个 logical task。

\topic{热点 key}
如果某个 key 占输入 90\%，普通 hash partition 会让一个 reduce partition 长尾。第一层优化是 map-side combine，减少重复传输；第二层是热 key 拆成多个执行子 key，分给多个 reduce；第三层再做二级 reduce，把子 key 合回业务 key。子 key 是执行身份，不是业务身份。Manifest 和 RunReport 要记录 hot route version、subkey count、final merge checksum 和二级合并成本。

\section{端到端执行账本：三 split、两 worker、一个 Driver}

把抽象对象放到一次具体运行里。输入被切成三个 split：\code{S0}、\code{S1}、\code{S2}。Driver 维护 task table；两个 worker 执行 map/combine/write；MessageBus 模拟远端消息；manifest 是唯一提交入口。故障规则固定为三条：\code{S1} 的第一次写出发生短写；\code{S2} 的第一次成功 reply 被丢弃；Driver 在接受 \code{S0} manifest 后崩溃并重启。

\topic{初始状态}
运行开始时，只有逻辑任务，没有 attempt，也没有 block。Task table 是控制面事实，输入文件是数据面事实。

\begin{lstlisting}[numbers=none,caption={初始 task table}]
task   split   state     current_attempt   accepted_attempt
T0     S0      Pending   -                 -
T1     S1      Pending   -                 -
T2     S2      Pending   -                 -

manifest.generation = 0
checkpoint.generation = 0
\end{lstlisting}

调度器把 \code{T0} 分给 worker 0，把 \code{T1} 分给 worker 1。创建 attempt 时，逻辑任务不变，物理执行身份增加：

\begin{lstlisting}[numbers=none,caption={创建 attempt 后的 task table}]
task   split   state     current_attempt   accepted_attempt
T0     S0      Running   A0                -
T1     S1      Running   A0                -
T2     S2      Pending   -                 -
\end{lstlisting}

\topic{T0 正常提交后 Driver 崩溃}
Worker 0 解析 \code{S0}，本地 combine，写出 shuffle block \code{B0a0.tmp}，checksum 通过，发送 \code{TaskReply(T0,A0,B0a0)}。Driver 在当前 epoch 下检查：任务仍然 Running，attempt 等于 current attempt，block checksum 正确，于是把 manifest 从 generation 0 推进到 1。

\begin{lstlisting}[numbers=none,caption={T0 提交后的 manifest}]
generation  logical_task  accepted_attempt  block      checksum
1           T0            A0                B0a0       ok
\end{lstlisting}

随后 Driver 崩溃。崩溃不会撤销已经写入 manifest 的事实。重启后，恢复程序先读 manifest，再读 checkpoint 或 task table 快照。若它只读旧 checkpoint，可能以为 \code{T0} 未完成；但 manifest generation 1 已经说明 \code{T0} 对外生效。恢复顺序必须以 manifest 为权威。

\topic{T1 短写：存储事实尚未完成}
Worker 1 执行 \code{T1/A0} 时，I/O 层第一次 \code{write} 只写了部分字节。此时不能发送成功 reply，也不能提交 manifest。WriteRequest 保存 \code{bytes_done} 和 \code{remaining}，继续写剩余部分；若后续失败，block 只能成为 failed candidate。

\begin{lstlisting}[numbers=none,caption={T1 短写时的 block table}]
block    logical_task  attempt  bytes_done  bytes_total  state
B1a0     T1            A0       4096        16384        PartialWritten
\end{lstlisting}

短写不是异常边角，而是正常 I/O 语义。只有当 \code{bytes_done == bytes_total}，checksum 通过，并且 Driver 接受该 attempt，\code{B1a0} 才能进入 manifest。否则 reduce 不得扫描这个文件。

\topic{T2 reply 丢失：执行成功不等于提交成功}
Driver 重启后调度 \code{T2/A0}。Worker 完成并写出 \code{B2a0}，但 MessageBus 丢弃成功 reply。Worker 的执行事实和存储事实都可能为真：attempt 做过，block 存在，checksum 正确。Driver 没收到 reply，manifest 没有接受它，所以提交事实不存在。超时后 Driver 创建 \code{T2/A1}。

\begin{lstlisting}[numbers=none,caption={T2 超时重试时的三类事实}]
execution facts:
  T2/A0 may have finished on worker 0
  T2/A1 is now running on worker 1

storage facts:
  B2a0 may exist and checksum may be ok
  B2a1 may be produced later

commit facts:
  manifest has no entry for T2 yet
\end{lstlisting}

这就是 attempt 存在的原因。若 reduce 扫描目录，\code{B2a0} 和 \code{B2a1} 可能都被读入；若 manifest 按 logical task 接受唯一 attempt，旧 block 只会成为 orphan 或 late candidate。

\topic{旧 reply 迟到：epoch 和 attempt 检查}
假设 \code{T2/A1} 先完成并提交，manifest 接受 \code{B2a1}。随后丢失的 \code{T2/A0} reply 迟到。Driver 必须按 logical task 检查：\code{T2} 已经有 accepted attempt \code{A1}，所以 \code{A0} 进入 LateAttempt 或 DuplicateAttempt，不得改写 manifest。

\begin{lstlisting}[numbers=none,caption={T2 最终 manifest 和迟到响应}]
manifest:
  generation  logical_task  accepted_attempt  block
  1           T0            A0                B0a0
  2           T1            A0                B1a0
  3           T2            A1                B2a1

late reply:
  TaskReply(T2, A0, B2a0) -> rejected as LateAttempt
\end{lstlisting}

若系统支持 route epoch 或 driver epoch，还要检查 reply 属于当前 epoch。旧 epoch 的成功响应即使 attempt 编号看起来合理，也不能拥有提交权。这个检查防止 Driver 重启、任务迁移或 ownership 切换后，旧执行者继续宣布结果有效。

\topic{最终 reduce：只读提交事实}
Reduce 阶段读取 manifest 中 \code{T0/A0}、\code{T1/A0}、\code{T2/A1} 对应 block。它不读取 \code{B2a0}，即使该文件真实存在且 checksum 正确。最终 checksum 和 reference 比较；如果一致，RunReport 仍要记录 late attempt、short write、driver recovery 和 orphan cleanup。正确运行不是没有事故，而是事故被状态机吸收后没有污染结果。

\section{Attempt、提交事实和恢复权威}

最终项目必须区分三种事实。执行事实：某个 worker 是否做过某次 attempt。存储事实：某个 block 是否存在且 checksum 正确。提交事实：下游是否允许把这份结果计入最终输出。系统可以有多个执行事实和多个存储事实，但每个 logical task 只能有一个提交事实。

\begin{systemdiagram}{执行事实、存储事实和提交事实不能混淆}
\begin{pdfdiagram}
\node[book title label] at (0,0.78) {执行事实};
\node[book title label] at (3.25,0.78) {存储事实};
\node[book title label] at (6.5,0.78) {提交事实};

\node[book amber node,text width=2.35cm] (a0) at (0,0) {attempt 0\\worker done};
\node[book data,text width=2.35cm] (b0) at (3.25,0) {tmp block A\\checksum ok};
\node[book red node,text width=2.35cm] (lost) at (6.5,0) {reply lost\\not committed};

\node[book amber node,text width=2.35cm] (a1) at (0,-1.45) {attempt 1\\worker done};
\node[book data,text width=2.35cm] (b1) at (3.25,-1.45) {tmp block B\\checksum ok};
\node[book purple node,text width=2.35cm] (m1) at (6.5,-1.45) {manifest\\accept B};

\draw[book strong arrow] (a0) -- (b0);
\draw[book weak arrow] (b0) -- (lost);
\draw[book strong arrow] (a1) -- (b1);
\draw[book strong arrow] (b1) -- (m1);
\draw[book back arrow] (b0) -- (m1);

\node[book label,text width=8.1cm] at (3.25,-2.9) {Attempt 0 可以真实执行过，block A 也可以真实存在，但它没有进入 manifest。恢复和 reduce 只能相信提交事实。};
\end{pdfdiagram}
\diagramnote{本图要证明的命题是：最终结果由 manifest 决定，不由目录扫描或 worker 成功响应决定。}
\end{systemdiagram}

\topic{状态表}
最小状态表可以分四类。Task table 保存逻辑任务：task id、stage id、current attempt、deadline、retry budget、state。Attempt table 保存物理执行：attempt、worker id、start、finish、reply state、candidate block。Block table 保存存储事实：block id、path、bytes、checksum、write state、orphan reason。Manifest table 保存提交事实：logical task、accepted attempt、accepted block、generation。

\begin{lstlisting}[language=C++,caption={Task 和 attempt 的示例形态}]
enum class TaskState : std::int32_t {
  Pending,
  Running,
  Retrying,
  Committed,
  Failed,
};

struct TaskRecord {
  std::uint64_t task_id = 0;
  std::uint32_t current_attempt = 0;
  std::uint64_t deadline_ns = 0;
  std::uint32_t retry_count = 0;
  TaskState state = TaskState::Pending;
};

struct AttemptRecord {
  std::uint64_t task_id = 0;
  std::uint32_t attempt = 0;
  std::uint32_t worker_id = 0;
  std::uint64_t candidate_block_id = 0;
  bool reply_received = false;
};
\end{lstlisting}

\topic{恢复顺序}
恢复不是把旧进程内存复活。正确顺序是先读 manifest，确定哪些 logical task 已经外部生效；再读 checkpoint，恢复 job、stage、retry budget 和未完成任务；再扫描临时目录，把未被 manifest 引用的 block 标成 orphan candidate；最后重新调度 pending 或 uncertain task。若恢复程序先扫描目录并承认文件，就会把未提交 attempt 当成结果；若只读 checkpoint 而忽略 manifest，就会丢掉 commit 后崩溃的事实。

\topic{恢复算法伪代码}
恢复程序要明确读什么、信什么、丢什么。下面的伪代码表达的是顺序，不是文件格式。

\begin{lstlisting}[numbers=none,caption={从 manifest 和 checkpoint 恢复控制面的伪代码}]
load manifest entries
mark all accepted logical tasks as Committed

load latest checkpoint if present
restore job configuration, stage graph, retry budgets, route epochs

scan temporary block directory
for each block:
    if block is referenced by manifest:
        verify checksum and keep as committed data
    else:
        mark as orphan candidate

for each logical task:
    if task is committed by manifest:
        do not reschedule
    else if retry budget remains:
        schedule new attempt under current epoch
    else:
        mark task failed

write RecoveryAudit into RunReport
\end{lstlisting}

注意第一行和第三行的顺序。Manifest 先于目录扫描，是为了避免“文件存在即有效”的错误；checkpoint 不能覆盖 manifest，是为了避免“提交后崩溃又重做”的错误。RecoveryAudit 应记录 committed tasks、rescheduled tasks、orphan blocks、stale replies 和 checksum failures。

\section{Driver 状态机：消息怎样变成提交事实}

最终项目的核心不是 worker 写文件，而是 Driver 如何把不可靠消息、重试、迟到响应和存储候选转成唯一提交事实。Worker 可以执行多次，MessageBus 可以丢包或重复投递，临时目录可以留下孤儿文件；Driver 只能让每个 logical task 有一个 accepted attempt。这个约束应写成状态机和伪代码，而不是散在回调里。

\topic{消息必须带完整身份}
一个成功响应至少要带 job id、stage id、logical task id、attempt、driver epoch、worker id、request id、block id、bytes、checksum 和状态。缺少任何一个关键字段，Driver 都可能把旧消息误认为新消息，或者把不同逻辑任务的 block 混在一起。消息不是函数返回值，它是跨时间、跨线程、跨进程边界的事实载体。

\begin{lstlisting}[language=C++,caption={TaskReply 的示例形态}]
enum class TaskReplyStatus : std::int32_t {
  Succeeded,
  FailedRetryable,
  FailedFatal,
  Busy,
  Canceled,
};

struct TaskReply {
  std::uint64_t job_id = 0;
  std::uint64_t stage_id = 0;
  std::uint64_t task_id = 0;
  std::uint32_t attempt = 0;
  std::uint64_t driver_epoch = 0;
  std::uint32_t worker_id = 0;
  std::uint64_t request_id = 0;
  std::uint64_t block_id = 0;
  std::uint64_t bytes = 0;
  std::uint64_t checksum = 0;
  TaskReplyStatus status = TaskReplyStatus::Succeeded;
};
\end{lstlisting}

\code{request_id} 区分一次消息交互，\code{attempt} 区分物理执行，\code{driver_epoch} 区分控制权时代，\code{task_id} 区分逻辑贡献。四者不能互相替代。超时重试后，旧 request 可能回来；Driver 重启后，旧 epoch 的 worker 可能继续发送；同一 task 的不同 attempt 可能都写出 block；这些情况都要被显式识别。

\topic{Driver 接收 reply 的判定顺序}
收到 reply 后，Driver 不能先写 manifest。它要按从外到内的顺序做检查：job/stage 是否匹配，epoch 是否当前，task 是否存在，task 是否已经 committed，attempt 是否仍然是当前 attempt，block 是否校验通过，status 是否允许提交，最后才尝试 manifest append。顺序错误会放大事故。例如先校验 block 再看 task 状态，可能把已提交 task 的旧 block 误加入候选；先接受 attempt 再看 epoch，可能让旧 Driver 时代的消息污染新控制面。

\begin{lstlisting}[numbers=none,caption={Driver 处理 TaskReply 的语义伪代码}]
handle_reply(reply):
    if reply.job_id != current_job_id:
        record rejected_wrong_job
        return

    if reply.driver_epoch != current_driver_epoch:
        record stale_epoch_reply
        reap_candidate_block(reply.block_id)
        return

    task = task_table.find(reply.task_id)
    if task is missing:
        record unknown_task_reply
        reap_candidate_block(reply.block_id)
        return

    if task.state == Committed:
        record late_or_duplicate_reply
        reap_candidate_block(reply.block_id)
        return

    if reply.attempt != task.current_attempt:
        record stale_attempt_reply
        reap_candidate_block(reply.block_id)
        return

    if reply.status == Busy:
        reschedule_with_backoff(task)
        return

    if reply.status is retryable failure:
        retry_or_fail(task)
        return

    if reply.status is fatal failure:
        mark_task_failed(task)
        return

    if not verify_block(reply.block_id, reply.bytes, reply.checksum):
        retry_or_fail(task)
        return

    append_manifest_if_absent(task.task_id, reply.attempt, reply.block_id)
    mark task Committed
\end{lstlisting}

这段伪代码刻意把 \code{reap_candidate_block} 放在拒绝路径里。旧 block 可以存在一段时间，但必须有分类：late、duplicate、stale epoch、unknown task、checksum failed。清理失败不是业务失败，接受失败才是业务语义。RunReport 应把这些分类写清，便于判断是网络抖动、Driver 重启还是状态机错误。

\topic{manifest append 必须幂等}
\code{append_manifest_if_absent} 是提交路径的核心。它的语义不是“追加一行文本”，而是“若 logical task 尚未提交，则接受该 attempt；若已经提交同一 attempt，则返回 already committed；若已经提交不同 attempt，则拒绝”。文件格式可以是 append-only log、SQLite、简单二进制记录或内存模拟，但语义必须一致。

\begin{lstlisting}[numbers=none,caption={幂等 manifest 提交的语义伪代码}]
append_manifest_if_absent(task_id, attempt, block_id):
    lock manifest_index

    existing = manifest_index.find(task_id)
    if existing exists:
        if existing.attempt == attempt and existing.block_id == block_id:
            return AlreadyCommitted
        return DuplicateRejected

    entry = build_manifest_entry(task_id, attempt, block_id)
    durable_append(entry)
    manifest_index.insert(task_id, entry)
    return Committed
\end{lstlisting}

这个函数有两个关键点。第一，内存索引和持久记录要保持同一语义；崩溃后通过重放 manifest log 重建索引。第二，重复提交同一条记录可以幂等返回成功，重复提交不同 attempt 必须拒绝。否则 retry、迟到响应和恢复重放都会把提交路径变成随机结果。

\topic{checkpoint 不能替代 manifest}
Checkpoint 是恢复加速器，不是提交权威。它可以保存 task table、retry budget、route epoch 和 manifest generation，但不能让 reduce 读取 checkpoint 中的候选 block。原因很简单：checkpoint 可能比 manifest 旧，也可能在 manifest append 之前生成。恢复时若 checkpoint 说 task Running，而 manifest 已经接受该 task，应以 manifest 为准；若 checkpoint 说 task Committed，但 manifest 没有记录，应重新审计，不能直接相信 checkpoint。

\begin{center}
\begin{tabular}{p{0.28\linewidth}p{0.32\linewidth}p{0.28\linewidth}}
\toprule
恢复时看到的组合 & 含义 & 动作 \\
\midrule
manifest 有，checkpoint 无 & 提交后崩溃或 checkpoint 落后 & 承认提交，更新 task table \\
manifest 无，checkpoint Running & 未提交或提交前崩溃 & 重试或等待恢复策略 \\
manifest 无，checkpoint Committed & checkpoint 不可信或写序错误 & 进入 audit，重新校验，不直接提交 \\
manifest 有两个同 task & manifest 损坏或提交协议错误 & 停止并报告，不自动选择 \\
\bottomrule
\end{tabular}
\end{center}

\topic{恢复扫描要先分类，不先接受}
恢复程序启动后会看到许多材料：manifest log、checkpoint、临时 block、worker reply 日志、故障注入记录、未清理目录。错误做法是扫描目录，看到 block 文件就加入 reduce；正确做法是先重建权威状态，再把每个材料分类。Manifest 中的 accepted entry 是提交事实；与 manifest 匹配的 block 是已接受数据；没有 manifest 的完整 block 只是 candidate；旧 epoch、旧 attempt、未知 task 和 checksum 错误都是拒绝材料；格式损坏或身份不完整的材料进入 corrupt 类。

\begin{center}
\small
\begin{tabular}{@{}p{0.20\linewidth}p{0.38\linewidth}p{0.30\linewidth}@{}}
\toprule
分类 & 判定依据 & 恢复动作 \\
\midrule
accepted & manifest 有同 task/attempt/block，checksum 匹配 & 承认提交，供 reduce 使用 \\
candidate & block 完整但 manifest 无记录，task 仍可重试 & 保留审计或重新调度，不直接提交 \\
orphan & task 不存在、stage 已结束或 job generation 不匹配 & 记录并等待清理安全点 \\
stale attempt & task 已提交其他 attempt，或 attempt 不是当前 attempt & 拒绝，计入 duplicate/stale 指标 \\
stale epoch & driver epoch、route epoch 或 owner 已过期 & 拒绝，禁止补写 manifest \\
corrupt & bytes/checksum/metadata 不一致或文件不完整 & 隔离，触发 retry 或失败策略 \\
\bottomrule
\end{tabular}
\end{center}

这张表要进入恢复代码，而不是只出现在说明里。恢复扫描的输出应是 \code{RecoveryReport}：每个目录发现多少 accepted、candidate、orphan、stale、corrupt，分别占用多少字节，哪些被清理，哪些等待审计。只有分类稳定，恢复才不会被文件名、修改时间和目录遍历顺序支配。

\topic{端到端 stale attempt 场景}
下面把一个旧 attempt 从产生到被拒绝写成完整链路。Task 7 的 attempt 0 被派给 Worker A。A 写 block 时网络变慢，Driver 超时后派 attempt 1 给 Worker B。B 成功写出 block，reply 到达，manifest 接受 \code{T7,A1,B7a1}。稍后 A 的 attempt 0 才完成并发送成功 reply。若 Driver 只看 checksum，A0 可能覆盖 A1；若 Driver 按身份链判定，A0 会进入 stale attempt。

\begin{lstlisting}[numbers=none,caption={迟到 attempt 的端到端事件链}]
T0:
  Driver assigns task=7 attempt=0 to worker A

T1:
  worker A writes block B7a0 slowly
  Driver timeout expires

T2:
  Driver assigns task=7 attempt=1 to worker B
  worker B writes block B7a1
  Driver receives reply for attempt=1
  manifest accepts task=7 attempt=1 block=B7a1

T3:
  worker A finally sends success reply for attempt=0
  Driver sees task=7 already committed with attempt=1
  reply is stale_attempt
  block B7a0 is not visible to reduce
\end{lstlisting}

这里的关键不是“谁先完成”，而是“哪个 attempt 被 manifest 接受”。真实系统中，旧 attempt 可能在 Driver 重启后才到达；可能只有 block 文件留下而 reply 丢失；也可能旧 worker 处在旧 driver epoch。每种情况都应落到同一套分类：没有 accepted manifest 就不是提交事实，已经提交其他 attempt 就不能再改变结果。

\topic{Checkpoint 提前和落后的双向事故}
Checkpoint 和 manifest 的相对顺序会制造两种事故。第一，manifest 已提交，checkpoint 尚未更新；恢复必须承认 manifest，不能因为 checkpoint 落后就重做并产生重复 attempt。第二，checkpoint 声称 task committed，但 manifest 没有对应 entry；这可能是 checkpoint 写序错误、崩溃点落在 manifest 前，或 checkpoint 文件损坏。恢复不能直接相信 checkpoint，应进入 audit 或按未提交重试。

\begin{lstlisting}[numbers=none,caption={checkpoint 与 manifest 的恢复优先级}]
case checkpoint_lag:
  manifest: task 7 -> attempt 1
  checkpoint: task 7 Running
  decision: task 7 is Committed, update recovered task table

case checkpoint_ahead:
  manifest: no entry for task 8
  checkpoint: task 8 Committed
  decision: audit checkpoint, do not expose block to reduce
\end{lstlisting}

这条规则让系统避免“快照覆盖日志”。在控制面里，日志或 manifest 是提交权威，checkpoint 是缓存；缓存可以落后，也可能损坏，不能反向创造提交事实。

\topic{故障注入规则也要结构化}
故障注入不是在代码里随手 \code{if random()}。规则应能匹配对象身份，并且可重放。一个规则可以表达“第一次发送 \code{TaskReply(T2,A0)} 时丢弃”，另一个规则表达“\code{block_id=B1a0} 第一次 write 只写 4096 字节”。规则应有触发计数，避免每次都触发导致系统永远无法前进。

\begin{lstlisting}[numbers=none,caption={故障注入规则的语义形态}]
FaultRule:
  id: drop_reply_T2_A0_once
  match:
    event_kind: SendTaskReply
    task_id: T2
    attempt: A0
  trigger:
    max_times: 1
  action:
    DropMessage
\end{lstlisting}

结构化故障的收益是回归稳定。修复 manifest 去重后，\code{drop_reply_T2_A0_once} 必须长期保留；若未来重构再次让 reduce 扫描目录，这条规则会重新失败。随机压力测试可以发现新问题，但确定性规则负责防止旧事故复发。

\topic{MessageBus 的最小语义}
单机模拟的 MessageBus 不应假装网络可靠。它至少支持延迟、丢弃、重复、乱序、Busy 和旧 epoch。每条消息进入 bus 时带 envelope，bus 决定何时投递或是否投递。Driver 和 worker 都不能依赖调用栈返回值来判断对方状态。

\begin{lstlisting}[numbers=none,caption={MessageEnvelope 的语义字段}]
MessageEnvelope:
  message_id
  source_node
  target_node
  driver_epoch
  request_id
  logical_task_id
  attempt
  deadline_ns
  payload_kind
  payload_checksum
\end{lstlisting}

这个 envelope 未来可以落到进程间 socket、管道、共享内存队列或真实网络。早期用内存队列模拟时，也必须保留序列化和反序列化边界；否则两进程版本会突然暴露字段缺失、指针不可传、对象生命周期错误和共享状态假设。

\topic{端到端不变量}
最终状态机至少要守住六条不变量：

\begin{enumerate}
  \item 同一个 logical task 在 manifest 中最多有一个 accepted attempt。
  \item Reduce 只读取 manifest 引用的 block。
  \item Worker 成功、block 存在、reply 成功都不能单独推出 committed。
  \item 旧 epoch、旧 attempt、未知 task 的 reply 只能进入拒绝和清理路径。
  \item Checkpoint 不能覆盖 manifest，恢复必须先承认提交事实。
  \item 每个被拒绝的候选 block 都要有分类和清理策略。
\end{enumerate}

这些不变量应写进测试名，而不只写在说明里。例如 \code{late_reply_does_not_replace_committed_attempt}、\code{reduce_reads_manifest_not_directory}、\code{checkpoint_cannot_commit_without_manifest}。测试名本身就是系统边界的索引。

\section{可靠 I/O、Checkpoint 和 Backpressure}

最终项目的 I/O 层承接第 15 章：submit、completion 和 commit 必须分开。Worker 写出 block 时，先写临时文件，可靠写循环处理短写，完成后计算 checksum，再向 Driver 提交。Driver 接受后，manifest 才让 block 对 reduce 可见。短写、延迟 completion、取消后 completion、rename 前后崩溃，都应进入测试。

\topic{Checkpoint 内容}
Checkpoint 保存控制面，不复制所有大数据。至少包含 job id、stage 状态、task table、manifest generation、retry budget、route epoch、输入身份和 idempotency 摘要。Shuffle block 本身可以留在数据面，只要有稳定路径、bytes、checksum 和 manifest 引用。Checkpoint 小而强，block 大而可校验，这就是控制面和数据面分离在最终项目里的落地。

\topic{Backpressure 贯穿流水线}
读取、解析、map combine、shuffle 写出、reduce 合并和 manifest commit 都可能成为瓶颈。每个阶段都要有有界队列或资源窗口：input queue、in-flight shuffle bytes、buffer pool、reduce partition queue、checkpoint writer。队列满时，上游应等待、降速、合并或返回 Busy，而不是无限分配内存。RunReport 要记录 high watermark、busy count、wait time 和 dropped/late events。

\topic{资源预算}
最终项目不是某个函数最快，而是在资源预算内稳定完成工作。预算包括线程数、队列容量、buffer pool bytes、临时文件数、in-flight requests、checkpoint frequency 和 retry count。若吞吐提升伴随内存无限增长，不能算成功；若平均延迟下降但 p99 和 recovery cost 爆炸，也要写入报告。

\section{故障注入和 RunReport}

没有故障注入的分布式计算项目只证明 happy path。最终项目应把故障注入当作一等模块，而不是调试脚本。故障规则要有 id、匹配条件、触发次数和 action；所有事故演练都能用固定 seed 或固定 rule id 重放。

\topic{故障矩阵}
\begin{center}
\begin{tabular}{p{0.26\linewidth}p{0.32\linewidth}p{0.30\linewidth}}
\toprule
故障 & 注入方式 & 预期行为 \\
\midrule
坏行 & split 中插入非法字段 & reference 和优化路径记录同样错误摘要 \\
短写 & 第一次 write 只写部分字节 & 继续写剩余字节，未完整不提交 manifest \\
响应丢失 & drop 某次 TaskReply & timeout 进入 unknown，retry 新 attempt，manifest 唯一 \\
重复响应 & 同一 reply 投递两次 & 第二次进入 duplicate 或 late 指标 \\
Driver 崩溃 & manifest 前后分别终止 & 前者重做，后者承认已提交事实 \\
Reduce busy & partition queue 高水位 & Map 端背压，retry budget 不被放大 \\
热点 key & 一个 key 占大多数输入 & 普通 hash 长尾，拆分后正确二级合并 \\
旧 epoch & route 切换后旧请求到达 & StaleRoute，不改变 manifest \\
\bottomrule
\end{tabular}
\end{center}

\topic{崩溃点矩阵要贴着提交路径}
Driver 崩溃不能只测“运行中 kill 一次”。提交路径由多个边界组成：worker 写临时 block，写满字节，计算 checksum，发送 reply，Driver 校验 block，append manifest，更新内存 task table，写 checkpoint，reduce 读取 manifest。每个边界前后崩溃，恢复动作都不同。矩阵要贴着这些边界，而不是随机挑一个时间点。

\begin{center}
\small
\begin{tabular}{@{}p{0.24\linewidth}p{0.34\linewidth}p{0.30\linewidth}@{}}
\toprule
崩溃点 & 恢复时可能看到什么 & 正确动作 \\
\midrule
临时 block 写到一半 & 文件存在但 bytes/checksum 不匹配 & corrupt，删除或隔离，重试 task \\
block 完整但 reply 未发送 & 完整 candidate，无 manifest & 不提交，按 task 状态重试或审计 \\
reply 到达但 manifest 前 & candidate block，checkpoint 可能 Running & 重试或重新校验，不对 reduce 可见 \\
manifest append 后、内存表前 & manifest 有 accepted，checkpoint 落后 & 承认提交，重建 task table \\
内存表后、checkpoint 前 & manifest 有 accepted，checkpoint 落后 & 以 manifest 为准，更新 checkpoint \\
checkpoint 声称 committed 但 manifest 无 & checkpoint ahead 或损坏 & audit，不直接提交 \\
reduce 过程中崩溃 & manifest 不变，partial output 可能残留 & 清理候选输出，重新 reduce committed set \\
\bottomrule
\end{tabular}
\end{center}

这个矩阵把提交权威固定在 manifest。Checkpoint、目录和内存表都可能落后或领先，不能反过来创造提交事实。故障注入规则应该能精确命中这些边界，例如 \code{crash_after_manifest_append_before_task_table_update}，而不是只写 \code{crash_randomly}。

\topic{恢复报告要解释每个文件的命运}
恢复扫描后，RunReport 应能回答三个问题：哪些文件进入 accepted 集合，哪些文件被拒绝，哪些文件暂时保留等待审计。每个文件都要有身份链和分类理由。否则，恢复程序虽然最终 checksum 正确，也无法证明没有把孤儿文件碰巧排除，或把旧 attempt 碰巧覆盖。

\begin{lstlisting}[numbers=none,caption={RecoveryReport 的语义字段}]
recovery:
  manifest_generation_before
  manifest_generation_after
  checkpoint_generation_loaded
  accepted_blocks
  candidate_blocks
  orphan_blocks
  stale_attempt_blocks
  stale_epoch_blocks
  corrupt_blocks
  bytes_reclaimed
  audit_required

block_decision:
  block_id
  task_id
  attempt
  driver_epoch
  bytes
  checksum_status
  manifest_status
  decision
  reason
\end{lstlisting}

这份报告会让恢复从“目录里剩了什么”变成“每个材料被哪条规则处理”。当后续把单机 MessageBus 换成多进程或真实网络，恢复报告仍然有效，因为它依赖的是身份、attempt、epoch、manifest 和 checksum，不依赖传输方式。

\topic{故障规则要覆盖迟到和重复的组合}
真实事故经常不是单个故障，而是组合：reply 丢失导致 retry，旧 attempt 稍后成功，Driver 又在 manifest 后崩溃，重启时目录里有两个完整 block。若测试只覆盖单一 drop 或单一 crash，无法证明组合安全。确定性故障规则要支持顺序触发、次数限制和组合命名。

\begin{lstlisting}[numbers=none,caption={组合故障规则示例}]
FaultScenario late_attempt_after_commit_and_restart:
  1. drop TaskReply(task=7, attempt=0) once
  2. allow timeout and assign attempt=1
  3. allow attempt=1 manifest commit
  4. crash driver after manifest append before checkpoint
  5. deliver delayed reply for attempt=0 after restart

expected:
  manifest keeps attempt=1
  attempt=0 is stale_attempt
  recovery accepts B7a1 only
  RunReport records one stale reply and one checkpoint lag
\end{lstlisting}

组合规则的价值在于长期回归。后续重构 writer、MessageBus、manifest 或 checkpoint 时，这条场景都应继续通过；一旦失败，报告能指出是幂等提交、恢复优先级还是旧 reply 分类被破坏。

\topic{RunReport 是最终系统的用户界面}
RunReport 不是日志汇总，而是可审查证据。它至少包含输入摘要、reference checksum、engine checksum、错误行数、stage 耗时、queue high watermark、task 数、attempt 数、retry amplification、late reply 数、duplicate reject 数、manifest generation、checkpoint generation、故障规则、恢复动作和未验证边界。

\begin{lstlisting}[numbers=none,caption={RunReport 摘要示例}]
input.records=3000000
input.bad_records=12
reference.checksum=0x51b76290
engine.checksum=0x51b76290
tasks.total=96
attempts.total=101
reply.late=2
commit.duplicate_rejected=3
shuffle.bytes=1843200
queue.reduce.high_watermark=65536
manifest.generation=17
checkpoint.generation=4
fault.rule=drop_task_reply_once
boundary.unverified=real_network_power_loss
\end{lstlisting}

报告要机器可读，也要人能读。机器可读便于回归比较，人能读便于排查事故。若报告只能说“success”，它不能支持工程判断；若报告能解释每个故障的状态变化，它就是系统的一部分。

\section{最终验收：不是跑完，而是可证明}

最终项目的验收必须分层。能在无故障输入上跑完，只通过了最浅的一层。一个现代计算系统至少要证明五类事实：语义正确，资源受控，失败可恢复，性能可解释，边界可审计。任何一类缺失，系统都可能在演示中成功、在真实负载中失控。

\topic{语义正确}
语义正确的最低证据是 reference checksum 与 engine checksum 一致，错误摘要一致，输出顺序稳定，manifest 引用的 block 集合与 logical task 集合一致。输入包含坏行、空 split、重复 key、热点 key、短写、迟到响应和 Driver 崩溃时，仍要保持这些事实。若优化路径在坏行密集输入下少报 diagnostics，不能用吞吐解释；若 reduce 扫描目录导致重复 attempt 被计入，不能用“文件确实存在”解释。

\topic{资源受控}
资源受控要求线程数、队列长度、in-flight bytes、buffer pool、临时文件数量、retry 数和 checkpoint 大小都有上界。无界队列跑得快不是优点；它只是把压力推迟到内存。报告应能给出最大水位和限速次数。若写出慢，reader 和 parser 最终必须减速；若 manifest commit 慢，completion、buffer pool 和上游计算都应感受到背压。资源控制不是性能附属项，而是正确运行的一部分。

\topic{失败可恢复}
恢复证明至少包含三类崩溃点：manifest 前、manifest 后、checkpoint 前后。Manifest 前崩溃，未提交 block 只能成为候选或孤儿；manifest 后崩溃，恢复必须承认提交事实；checkpoint 落后时不能覆盖 manifest；checkpoint 提前声称 committed 但 manifest 没有记录时，必须进入 audit。恢复程序不能靠目录扫描决定结果，也不能让旧 epoch 或旧 attempt 的响应补写 manifest。

\topic{性能可解释}
性能可解释不是要求所有指标完美，而是要求瓶颈能被定位。RunReport 至少给出阶段耗时、队列等待、service time、worker idle、retry amplification、combine ratio、shuffle bytes、commit wait 和故障注入开销。若端到端变慢，报告能说明是 parse、combine、shuffle write、manifest commit、retry 还是背压；若吞吐提升，报告能说明是否牺牲了错误摘要、可靠写或资源上界。

\topic{边界可审计}
边界可审计要求每个对外事实都有身份链。输入记录能追到 file id 和 offset；partial 能追到 split 和 attempt；block 能追到 logical task、attempt、checksum；manifest 能追到 generation；message 能追到 request id 和 driver epoch；恢复动作能追到故障规则和审计记录。没有身份链，debug 只能靠人工猜测；有身份链，系统可以把事故缩小到某个边界。

\begin{center}
\small
\begin{tabular}{@{}p{0.18\linewidth}p{0.36\linewidth}p{0.34\linewidth}@{}}
\toprule
验收层 & 必须提供的证据 & 常见假通过 \\
\midrule
语义 & reference、checksum、diagnostics、稳定输出 & 只比较成功输入的最终计数 \\
资源 & 水位线、in-flight bytes、线程数、临时文件数 & 无界队列短跑吞吐高 \\
恢复 & 崩溃点矩阵、manifest 优先、orphan 分类 & 重启后扫描目录接受所有文件 \\
性能 & 阶段时间、队列等待、背压和 retry 指标 & 只给总耗时和平均吞吐 \\
审计 & input/split/attempt/block/manifest 身份链 & 日志里只有字符串消息 \\
\bottomrule
\end{tabular}
\end{center}

\topic{小规模也要完整}
最终项目可以只处理几百万行日志，不必追求工业规模。但状态机必须完整。小规模隐藏不了语义漏洞：迟到响应仍能出现，短写仍能模拟，Driver 崩溃仍能注入，manifest 仍要唯一，checkpoint 仍可能落后。规模小只是让测试可复现，不是降低边界要求。

\topic{失败样本不应从性能统计中删除}
如果某次运行 checksum 不一致、diagnostics 不一致、manifest 重复、资源越界或恢复 audit 失败，该样本不能作为性能样本参与平均值。它应进入 failure report。性能报告只统计 \code{Passed} 样本，并记录 failed/skipped 数量。否则系统会奖励那些在压力下跳过工作的版本。

\begin{lstlisting}[numbers=none,caption={最终验收摘要的机器可读形态}]
validation:
  semantic_status: Passed
  resource_status: Passed
  recovery_status: Passed
  performance_status: Explained
  audit_status: Passed

samples:
  measured: 20
  passed: 20
  failed: 0
  skipped: 0

boundaries:
  real_network: Unverified
  power_loss_after_fsync: Unverified
  multi_socket_numa: Unverified
\end{lstlisting}

把未验证边界写出来，不会削弱项目，反而说明结论没有越界。单机 MessageBus 验证了消息乱序、丢弃和迟到的控制逻辑；它没有验证真实网络拥塞、内核 socket buffer 和跨主机时钟。普通文件模拟验证了短写和 manifest；它不一定验证掉电持久化。边界越清楚，后续扩展越稳。

\section{里程碑：每一步都能失败、解释和回退}

最终项目不应一次性实现完整框架。每个里程碑只引入一个新边界，同时保留上一版可运行结果和失败测试。这样复杂度有来源，出错时能回退。

\topic{一、串行 reference 和输入身份}
实现输入解析、错误摘要、排序输出和 reference report。测试空文件、单行、坏行、越界 event type、超长行和固定随机输入。这个阶段回答“到底在算什么”。

\topic{二、HotBatch 和本地 combine}
把热字段从原始记录中分离，保留 record id 回溯。实现本地 combine，并记录 key 数、combine ratio、内存峰值。这个阶段回答“如何少搬热路径数据，同时不丢证据”。

\topic{三、并行 partition 和任务运行时}
用 count-scan-write 替代共享 push，建立 bucket range 和 partition manifest；再用线程池或任务图执行 chunk 任务，记录 ready、running、waiting、done、cancelled。这个阶段回答“多个 worker 如何写同一个逻辑输出而不互相踩”。

\topic{四、可靠写和 manifest commit}
实现临时 block、短写处理、checksum、manifest append 和恢复扫描。测试写中崩溃、写完未提交崩溃、提交后崩溃。这个阶段回答“文件存在为什么不等于结果有效”。

\topic{五、MessageBus、retry 和幂等提交}
把本地调用改成消息事件。加入 request id、attempt、deadline、retry budget、Busy 和 late reply。测试响应丢失后只提交一个 attempt。这个阶段回答“远端执行成功为什么不是外部生效”。

\topic{六、Checkpoint、热点 key 和两进程边界}
加入 checkpoint generation、恢复 audit、热点 key 拆分和可选两进程部署。两进程部署不追求性能，而是检查序列化、framing、process crash 和共享状态假设。这个阶段回答“系统重启后谁说了算，以及本机模拟有没有偷共享内存的懒”。

\begin{labbox}
毕业演练：使用三个小 split、两个 worker 和一个 MessageBus。打开四个故障：坏行、短写、成功响应丢失、Driver 在 manifest 后重启。要求最终 checksum 等于 reference，manifest 每个 logical task 只有一个 accepted attempt，RunReport 能列出 late reply、duplicate rejected、partial write 和 recovery action。
\end{labbox}

\section{一次失败排查日记}

最终系统覆盖的不只是设计能力，还包括排查能力。假设小输入只有一百条记录，reference 输出 key A 为三十七，优化版输出三十九。差异很小，最容易被误判成并行顺序、随机噪声或“测试不稳定”。正确做法不是先改代码，而是沿 RunReport 一层一层收窄。

\topic{一、先排除输入和解析}
先比较 input checksum、record count、bad record count 和 reference parser 的错误摘要。若这些不同，问题还在输入身份或 parser 合同；不要继续查 shuffle。若这些一致，再比较 HotBatch checksum 和 record id 回溯。若 HotBatch 与 reference 的 key/value 序列一致，说明冷热布局没有丢字段。排查要先把上游语义排除，而不是一看到并行版本就怀疑线程。

\topic{二、再排除输出位置}
查看 count、scan、write 三阶段的 bucket range。若 count 阶段显示 chunk 4 对 bucket 2 有七条，write 阶段写了九条，就说明 predicate 在两次执行中不一致，或者 chunk 4 被执行了两次。若 range 正确但最终多了两条，就继续查 manifest。Scan 和 partition 的中间账本，是把“结果不一致”缩小到“输出位置或重复提交”的关键证据。

\topic{三、最后查提交点}
Task table 显示 chunk 4 有两个 attempt。Attempt 0 写出成功，但 TaskReply 被故障规则丢弃；Attempt 1 后来写出并提交。Manifest 却包含 attempt 0 和 attempt 1 两条 block。根因不是 reduce 算错，而是 manifest 按 block 文件名接受提交，没有按 logical task 去重。修复应落在 CommitRecord：同一个 logical task 只能接受一个 attempt，旧 attempt 只能进入 LateAttempt 或 Duplicate 分类。

\topic{四、把事故固化成回归测试}
新增回归测试：\code{drop_reply_then_retry_keeps_one_manifest_entry}。输入固定，故障规则固定丢 attempt 0 的 reply。测试断言 parallel checksum 等于 reference，manifest 中 chunk 4 只有一条 accepted entry，late reply count 为一，临时目录可以存在旧 block。注意最后一条：旧 block 存在不是错误，错误是把它当提交事实。这个测试能防止后续重构再次把目录扫描引回 reduce。

\topic{五、补报告字段}
这次事故还说明旧报告缺字段。RunReport 应增加 task commit summary：task id、accepted attempt、candidate blocks、late replies、duplicate replies、orphan blocks。报告字段不是越多越好，而是每次事故后补上能缩短排查路径的证据。成熟系统会把排查经验沉淀成报告和回归测试，而不是只沉淀在某个维护者的记忆里。

\section{实现工单样例}

最终项目不应以“实现一个分布式计算引擎”这种大工单开始。更稳的做法是逐张工单收敛，每张工单解决一个具体缺口，附带测试和报告字段。

\topic{工单一：输入身份}
动机：错误报告和恢复无法定位输入。修改：增加 FileId、ChunkId、RecordId 和 input checksum；reference report 输出这些身份。测试：同一文件两次运行 id 稳定，修改文件后 FileId 改变，错误行能回溯 offset。完成标准：没有性能要求，只要求身份稳定。

\topic{工单二：共享 push 改为 count-scan-write}
动机：共享输出锁竞争，失败后不知道写入范围。修改：filter/partition 先 count，再 exclusive scan，再写独占区间。测试：共享 push 反例、稳定顺序、chunk 写失败。报告字段：chunk count、bucket range、scan checksum。完成标准：结果与 reference 一致，输出区间可审计。

\topic{工单三：有界队列关闭合同}
动机：取消或错误时消费者可能永远等待。修改：队列返回 Success、Closed、Cancelled；close drain 和 close cancel 分开；关闭时 notify all。测试：消费者等待空队列时关闭，生产者等待满队列时关闭，drain 保留已有元素。报告字段：queue high watermark、closed mode、waiter wake count。

\topic{工单四：可靠写处理短写}
动机：write 返回部分字节时可能误提交坏 block。修改：WriteRequest 保存 offset 和 remaining；completion 区分 PartialWritten、Completed、Failed。测试：第一次只写部分字节，最终 checksum 正确；fatal 错误时 manifest 不提交。报告字段：partial write count、retry count、block checksum。

\topic{工单五：manifest 拒绝重复 attempt}
动机：响应丢失后重试会产生两个候选 block。修改：CommitRecord 按 logical task 保存 accepted attempt；重复或迟到 attempt 返回 LateAttempt 或 Duplicate。测试：attempt 0 写出后响应丢失，attempt 1 提交，attempt 0 迟到。报告字段：accepted attempt、late reply count、duplicate rejected。

\topic{工单六：恢复从 manifest 重建状态}
动机：Driver 崩溃后内存状态丢失，可能重复提交。修改：启动时读取 manifest 和 checkpoint，重建 task table；旧 epoch 响应拒绝。测试：manifest append 前崩溃要重做，append 后崩溃要承认已提交。报告字段：recovered committed tasks、rescheduled tasks、stale replies。

\section{工业设计参照}

\begin{casebox}
\topic{Spark：stage、shuffle 和 output commit}
Spark 的核心经验不是“分布式计算可以并行”，而是把宽依赖切成 stage，把 map output 和 shuffle metadata 管起来，把 task attempt 和输出提交分开。它面对 executor 故障、长尾 task、shuffle 文件丢失和 speculative execution。核心机制是 stage boundary、task attempt、map output tracker、shuffle block 和 output commit protocol。代价是 driver 元数据和 shuffle 服务成为关键路径，speculation 也会放大资源消耗。

Compute Systems Engine 应借鉴 stage、attempt、shuffle manifest 和 speculative attempt 的幂等提交边界，但不需要实现 Spark 的完整调度、资源管理和外部 shuffle 服务。
\end{casebox}

\begin{casebox}
\topic{Flink：checkpoint barrier 和状态一致性}
Flink 这类流式系统面对的是持续输入、算子状态和外部输出的一致恢复。核心机制是 checkpoint barrier、operator state、input offset 和 sink commit 的绑定。它说明 checkpoint 不能只保存“程序运行到哪里”，而要把输入位置、状态和输出提交点放在同一一致边界里。代价是 checkpoint 频率、状态大小和外部 sink 协议会直接影响延迟和吞吐。

Compute Systems Engine 是批处理模拟，但应借鉴这个原则：checkpoint 保存控制面和提交边界，不只保存内存 dump。若只保存 task table，不保存 manifest generation，恢复后仍可能重复输出。
\end{casebox}

\begin{casebox}
\topic{DuckDB/ClickHouse：向量化执行和 pipeline}
单机分析型执行器的经验同样适用最终项目。DuckDB、ClickHouse 这类系统强调列式或向量化 batch、pipeline、selection vector、压缩和 operator profiling。它们面对的是 CPU cache、SIMD、分支、内存带宽和复杂查询管线。核心机制是批量处理热字段，让算子间传递结构化 batch，并输出可解释 profile。

Compute Systems Engine 应借鉴 batch、selection vector、operator profile 和 pipeline breaker 的思想。Map-side combine 和 reduce merge 不应只当分布式概念，也要继承单机执行器的热路径纪律。
\end{casebox}

\begin{casebox}
\topic{Ray/actor runtime：任务、对象和故障边界}
Ray 等系统把分布式执行抽象成 task、actor 和 object store，面对的是任务依赖、对象传输、worker 故障和调度可观察性。它提醒我们：数据对象和任务状态要有稳定身份，运行时不能只靠调用栈理解依赖，失败后要知道哪些对象可重建、哪些需要重新提交。

Compute Systems Engine 不需要复制 Ray 的 API，但应借鉴对象身份、任务 trace 和故障重建的思想。ShuffleBlock、ManifestEntry 和 TaskRecord 就是示例版的对象和任务账本。
\end{casebox}

\section{面向后续推理系统的接口}

没有在这里直接展开 CPU 推理引擎，是为了先把计算系统地基打稳。后续推理系统会处理模型权重、tensor buffer、算子内核、batch 调度、KV cache、请求 deadline 和流式输出。这些不是全新问题，而是当前接口的延伸。

\topic{模型文件对应输入和 manifest}
模型文件有 tensor 名称、shape、dtype、量化参数、checksum 和版本。它比日志输入更严格，但同样需要输入身份、可靠读取、manifest 和版本边界。加载权重时，CompletionRecord、checksum 和 manifest 仍然适用。

\topic{算子对应 kernel 和 task}
矩阵乘、归一化、softmax、采样和量化都需要 scalar reference、误差合同、HotKernelReport、MemoryShape 和 KernelVariant。线程池和任务图会调度 layer、tile、token 或 batch。背压从 shuffle queue 变成 request queue 和 token generation queue。

\topic{KV cache 对应生命周期和所有权}
KV cache 是跨 token 持续存在的状态，既要高效访问，又要按请求隔离。它会重新考验 BatchView、buffer ownership、ProgressAndReclaim、队列关闭和取消语义。若当前项目已经把所有权和状态机写清，进入 KV cache 时就不会从零开始。

\section{复查清单}

\topic{一、Reference 是否仍然是权威}
所有优化版本、故障版本和分布式模拟都要能回到 reference。若 reference 未定义错误行、输出顺序或溢出语义，先补 reference，不要继续优化。

\topic{二、Manifest 是否是唯一提交入口}
Reduce 和恢复都只能读取 manifest 接受的 block。临时目录、worker Ok 响应和 checkpoint 内存状态都不能越过 manifest。

\topic{三、故障是否可复现}
响应丢失、短写、Driver 崩溃、旧 epoch、Reduce busy 和热点 key 都要用固定规则复现。随机压力可以补充，不能替代确定性故障。

\topic{四、报告是否能解释失败}
RunReport 应能沿输入、batch、partition、block、manifest、checkpoint 查到差异位置。若失败只能靠翻源码，报告还不合格。

\topic{五、性能结论是否有边界}
性能报告必须带输入、环境、线程数、阶段耗时、资源峰值和未验证条件。移动端或受限 Linux 环境的数字不能被写成通用硬件结论。

\topic{六、后续接口是否克制}
可以预留 ModelArtifact、TensorBuffer、KernelTask 和 InferenceRequest 的接口方向，但不要在这里展开 AI 算子正文。当前任务是收束计算系统，不是提前分散主题。

\begin{sourcereadingbox}
源码和工业阅读任选一条：
\begin{enumerate}
  \item 阅读 Spark 的 task attempt、shuffle block 或 output commit 相关路径，说明它如何避免重复输出。
  \item 阅读 Flink checkpoint 和 sink commit 的一条路径，说明 input offset、operator state 和外部输出如何绑定。
  \item 阅读 DuckDB、ClickHouse 或类似执行器的 vectorized pipeline/profile 路径，说明 batch 和 pipeline breaker 如何影响性能证据。
  \item 阅读 Linux 的 futex、page cache、epoll 或进程退出路径之一，必须从项目现象进入，不允许只列函数名。
\end{enumerate}
报告按“项目现象、源码入口、能解释的事实、不能证明的边界、回到项目的设计结论”组织。
\end{sourcereadingbox}

\section{完整端到端账本：一百条输入如何穿过最终系统}

最终章不能只列模块名。这里把一百条日志输入从 reference 走到分布式模拟提交，固定每一层的身份和事实。输入很小，方便手算；路径完整，足以暴露大系统的边界。假设输入被切成 3 个 split，两个 worker 执行 map/combine，最后一个 reducer 读取 manifest 生成结果。

\topic{T0：输入身份和 reference 先落盘}
系统先计算 input checksum，给输入分配 \code{InputGeneration=5}。Reference 顺序解析一百条记录，输出 accepted、rejected、按 key 排序的 counts 和 diagnostics checksum。这个 reference report 写入运行目录，后续所有优化结果都与它比较。没有 reference，分布式路径跑得再快也只是未知计算。

\begin{lstlisting}[numbers=none,caption={reference report 摘要}]
input_generation=5
input_checksum=0x4812...
records.total=100
records.accepted=98
records.rejected=2
counts_checksum=0x91aa...
diagnostics_checksum=0x0fd3...
\end{lstlisting}

这份报告不是调试附件，而是权威语义。后面如果某个 split 重试、某个 block 迟到、某个 worker 崩溃，最终结果仍要回到这里。

\topic{T1：split 是逻辑任务，不是文件片段随便切}
输入被切成三个 \code{InputSplit}：S0 覆盖记录 0 到 34，S1 覆盖 35 到 69，S2 覆盖 70 到 99。每个 split 记录 input generation、起止 offset、record id 范围和 split checksum。Worker 处理 split 时，输出的 partial 必须带 logical split id。没有 split id，后续 attempt 去重就没有对象。

\begin{lstlisting}[numbers=none,caption={split 身份}]
S0:
  input_generation=5
  record_range=[0,35)
  split_checksum=0xa001...

S1:
  input_generation=5
  record_range=[35,70)
  split_checksum=0xa002...

S2:
  input_generation=5
  record_range=[70,100)
  split_checksum=0xa003...
\end{lstlisting}

Split 边界必须落在记录边界上。若按字节范围切开一行，parser 会把半条记录当坏行或重复处理。第一章的 RecordId 在这里变成恢复事实。

\topic{T2：attempt 把物理执行和逻辑任务分开}
S1 第一次在 worker A 上执行，attempt id 为 0。它写出 block B10 后，reply 被故障注入丢弃。Driver 超时后把 S1 重派给 worker B，attempt id 为 1。Worker B 写出 block B11，并成功回复。Manifest 最终只能接受 S1 的一个 attempt。两个 block 都可能存在于临时目录中，但只有 manifest 决定哪一个进入 reduce。

\begin{lstlisting}[numbers=none,caption={attempt table}]
logical_split S1:
  attempt 0:
    worker=A
    block=B10
    reply=dropped
    completion=late
  attempt 1:
    worker=B
    block=B11
    reply=received
    completion=accepted
\end{lstlisting}

这条账本把“文件存在”和“结果生效”拆开。临时目录扫描会看到 B10 和 B11；正确 reducer 只读 manifest 中 accepted 的 B11。

\topic{T3：manifest append 是唯一提交入口}
Driver 收到 attempt 1 reply 后，验证 split id、input generation、block checksum、record count 和当前 epoch，再 append manifest entry。Append 必须幂等：同一 logical split 已有 accepted attempt 时，重复请求返回 Duplicate 或 LateAttempt，不追加第二条。Manifest entry 进入后，reduce 才能看到这份贡献。

\begin{lstlisting}[numbers=none,caption={manifest entry 摘要}]
entry:
  input_generation=5
  logical_split=S1
  accepted_attempt=1
  block_id=B11
  block_checksum=0xb011...
  records.accepted=34
  records.rejected=1
  route_epoch=3
\end{lstlisting}

Manifest 要同时承载正确性和恢复。Driver 崩溃后重启，内存 task table 没了；系统读取 manifest，就知道 S1 已经由 attempt 1 提交，不应重新接受 attempt 0。

\topic{T4：reduce 只读提交事实}
Reducer 不扫描 worker 目录，不读取所有 block，不相信 worker 的 Ok 日志。它按 manifest 枚举 S0、S1、S2 的 accepted entries，读取对应 block，校验 block checksum 和 route epoch，再合并 partial。若 S1 的 B10 仍在目录里，reducer 不关心。若 manifest 缺 S2，reduce 必须等待、失败或触发恢复，而不能猜测目录中某个文件就是 S2。

\begin{lstlisting}[numbers=none,caption={reduce 读取路径}]
entries = manifest.entries_for(input_generation=5)
require logical splits S0,S1,S2 all accepted
for each entry ordered by split id:
    block = read(entry.block_id)
    verify block.checksum == entry.block_checksum
    merge block partial into final counts
compare final checksum with reference counts checksum
\end{lstlisting}

这个路径保证重复 attempt 不会重复计数，也保证恢复后结果可解释。

\topic{T5：checkpoint 只能加速恢复，不能替代 manifest}
Checkpoint 可以保存 driver 当前 task table、已完成 split 集合、队列状态和统计指标。但 checkpoint 可能落后于 manifest，也可能在崩溃中只写了一半。因此恢复时的权威顺序是：先读取 manifest，得到已提交事实；再读取 checkpoint，作为调度和缓存加速；最后扫描临时目录，清理 orphan block 或重建候选，但不能把临时文件直接变成提交事实。

\begin{lstlisting}[numbers=none,caption={恢复顺序}]
recover:
  load manifest
  build committed logical split table
  load checkpoint if checksum valid
  discard checkpoint entries older than manifest authority
  reschedule missing logical splits
  classify temp blocks as accepted, late, orphan, or corrupt
\end{lstlisting}

这种顺序能解释崩溃点矩阵：manifest append 前崩溃，split 可重做；manifest append 后崩溃，split 已提交；checkpoint 写了但 manifest 未写，不能当作提交。

\topic{T6：RunReport 把全链路证据串起来}
最终报告应能从 reference 追到每个 split、attempt、block、manifest entry 和 reduce 输出。小输入一百条时，人工能读懂整条链；大输入一亿条时，报告仍应保留同样的字段，只是内容汇总和采样。系统成熟不是因为没有失败，而是失败能沿身份链定位。

\begin{lstlisting}[numbers=none,caption={端到端 RunReport 摘要}]
reference:
  counts_checksum=0x91aa...

splits:
  total=3 accepted=3 missing=0

attempts:
  launched=4 accepted=3 late=1 failed=0

manifest:
  entries=3 duplicate_rejected=1

reduce:
  final_checksum=0x91aa...
  matches_reference=true
\end{lstlisting}

这份账本就是最终系统的骨架。所有高级功能，热点 key 拆分、二级 reduce、故障重试、两进程部署、背压和 checkpoint，都必须保留这条身份链。

\section{最终验收：系统什么时候算完成}

最终项目不能用“能跑完一次”作为完成标准。完成意味着它在正确性、性能、资源、故障和恢复五个维度都有可复查证据。正确性由 reference 和 manifest 证明；性能由阶段报告和输入矩阵证明；资源由内存、队列和 in-flight bytes 证明；故障由确定性注入和 replay 证明；恢复由崩溃点矩阵和重启后 RunReport 证明。任何一个维度缺失，系统都只是演示程序。

\topic{正确性验收}
正确性验收至少包含四类输入。第一，最小手算输入，能人工核对 counts 和 diagnostics。第二，中等随机输入，用 reference 生成 checksum。第三，坏行和边界输入，覆盖缺字段、字段过多、value 溢出、空 key、超长行。第四，热点 key 输入，覆盖普通 hash、combine、拆分和二级 reduce。所有输入都要在串行 reference、单机并行、异步写出和分布式模拟路径上得到同一可观察结果。

\begin{lstlisting}[numbers=none,caption={正确性验收矩阵}]
input families:
  hand_small
  random_medium
  malformed_boundary
  hot_key_skew

execution modes:
  serial reference
  parallel local
  async write
  distributed simulation

required:
  counts checksum equal
  diagnostics checksum equal
  manifest consistency
\end{lstlisting}

\topic{故障验收}
故障验收不能靠随机崩溃碰碰运气。每个故障都要可命名、可复现、可重放：短写、写中 fatal、reply drop、reply delay、duplicate reply、driver crash before manifest、driver crash after manifest、worker busy、old epoch reply、stale route commit、corrupt block。每项故障都要写预期外部事实，例如 manifest entry 数量、accepted attempt、late reply count、orphan cleanup 和 final checksum。

\begin{center}
\begin{tabular}{p{0.25\linewidth}p{0.34\linewidth}p{0.29\linewidth}}
\toprule
故障 & 必须保持的事实 & 报告字段 \\
\midrule
reply drop & timeout 进入 Unknown，重试不重复提交 & retry、late、accepted attempt \\
短写 & offset 推进正确，checksum 匹配 & short write count、block checksum \\
manifest 前崩溃 & split 可重做，不承认临时 block & recovered missing tasks \\
manifest 后崩溃 & split 已提交，不重复执行 & recovered committed tasks \\
旧 epoch reply & 拒绝提交，记录 stale & stale epoch count \\
热点拆分切换 & 旧 route 写拒绝，二级 reduce 正确 & route epoch、hot split report \\
\bottomrule
\end{tabular}
\end{center}

\topic{性能验收}
性能验收不要求每个优化都最快，但要求瓶颈可解释。报告至少覆盖端到端时间、parse、aggregate、shuffle write、manifest commit、reduce、recovery check；线程数扩展、chunk size 扫描、队列容量扫描、热点 key 输入、冷/热 I/O 状态。若优化没有改善，报告也应能解释：瓶颈在内存带宽、写出、锁、任务粒度还是长尾 partition。

\topic{资源验收}
资源验收要证明系统不会用无界内存掩盖压力。队列必须有容量，buffer pool 必须有字节上限，in-flight attempt 必须有预算，retry 必须有上限，orphan block 必须能清理。RunReport 应输出峰值内存、队列高水位、in-flight bytes、open temp files、orphan cleanup、checkpoint size 和 replay log size。没有资源上限的系统在小输入上很美，在真实输入上会变成不可诊断的堆积。

\topic{恢复验收}
恢复验收用崩溃点矩阵驱动。Driver 在 reference 写出后、split 调度后、block 写中、block 写完、manifest append 前、manifest append 后、checkpoint 写中、checkpoint 写完分别崩溃。每个点重启后，系统必须按照 manifest 权威恢复，最终 checksum 与 reference 一致，临时文件分类可解释，重复 attempt 不进入结果。恢复不是额外功能，而是提交语义的一部分。

\begin{lstlisting}[numbers=none,caption={最终完成标准摘要}]
correctness:
  all execution modes match reference checksums

faults:
  deterministic injection and replay cover core failure classes

performance:
  stage reports explain dominant costs and scaling boundaries

resources:
  queues, buffers, retries, temp files have bounded policies

recovery:
  crash matrix restores from manifest authority
\end{lstlisting}

这个验收标准把整卷内容收束到一个可运行系统。硬件、缓存、SIMD、线程、锁、原子、无锁、运行时、I/O 和分布式协议，最终都必须服务这些可观察事实。

\topic{模块边界的最终不变量}
最终系统可以分成输入、热批次、运行时、I/O、提交、恢复六个模块。每个模块都要交出一个不变量，而不是只交出一组函数。输入模块保证 RecordId 稳定；热批次模块保证同一 row index 的字段来自同一记录；运行时保证 accepted task 最终进入终态；I/O 模块保证 completion 不等于 commit 且 buffer 有唯一所有者；提交模块保证一个 logical split 只接受一个 attempt；恢复模块保证 manifest 是权威入口。只要这些不变量守住，内部实现可以替换。

\begin{center}
\begin{tabular}{p{0.20\linewidth}p{0.43\linewidth}p{0.25\linewidth}}
\toprule
模块 & 不变量 & 主要证据 \\
\midrule
输入 & RecordId 能定位唯一输入事实 & input checksum、offset、line number \\
热批次 & row 对齐，冷热回查不断链 & BatchView checksum、cold lookup \\
运行时 & 接纳任务必有终态 & task table、timeline \\
I/O & buffer 唯一所有，completion 分层 & CompletionRecord、buffer report \\
提交 & logical split 只接受一个 attempt & manifest、commit table \\
恢复 & manifest 优先于 checkpoint 和目录 & recovery audit \\
\bottomrule
\end{tabular}
\end{center}

这张表是全书后半部分的最终压缩。它让重构有边界：可以换 SIMD backend，但不能破坏 BatchView；可以换线程池，但不能丢 task 终态；可以换 I/O backend，但不能让 completion 直接越过 manifest；可以多进程部署，但不能改变 attempt 和 commit 语义。

\topic{最终系统也要保留小模式}
大输入模式用于吞吐，小模式用于审计。项目应提供一个固定小输入和固定故障脚本，能在数秒内跑完整链路并打印所有关键表：reference、split、attempt、block、manifest、reduce、recovery。这个小模式不追求性能，它的价值是每次重构后快速确认身份链没有断。没有小模式，系统越复杂，越只能依赖大规模黑盒测试。

\begin{lstlisting}[numbers=none,caption={小模式应输出的表}]
reference table
split table
attempt table
block table
manifest table
reduce checksum
recovery audit
\end{lstlisting}

小模式和大模式共用同一代码路径，只是输入小、故障固定、报告更详细。这样调试和生产不会变成两套语义。

\topic{最终交付要能独立复盘}
最终产物应包含一份可独立复盘的运行目录：配置、输入 checksum、reference report、RunReport、manifest、checkpoint、replay log、故障规则和版本信息。几周后重新打开目录，不需要记住当时的口头背景，也能判断结果是否正确、性能结论是否有边界、失败是否按预期发生。系统工程的完成标志之一，就是证据能离开当时的终端窗口独立存在。

\section{习题}

\begin{exercisebox}
习题一：为日志统计系统写端到端数据流。要求包含 InputSplit、HotBatch、map-side combine、ShuffleBlock、ManifestEntry、ReduceOutput 和 Checkpoint。

习题二：设计一个 reference 合同。说明输入坏行、event type 越界、value 溢出、输出排序和错误摘要如何处理。

习题三：构造响应丢失事故。Attempt 0 写出 block 后 reply 被 drop，Attempt 1 提交，Attempt 0 reply 迟到。写出 task table、block table、manifest table 的状态变化。

习题四：设计 shuffle manifest。说明它记录哪些字段，为什么 reduce 不能扫描临时目录。

习题五：设计 checkpoint 内容清单。要求说明哪些属于控制面，哪些属于可校验数据面，恢复时按什么顺序读取。

习题六：设计热点 key 实验。比较普通 hash、map-side combine、热点拆分和二级 reduce，报告网络字节、reduce p99、最终 checksum 和二级合并成本。

习题七：写一个故障注入表。至少包含坏行、短写、丢响应、重复响应、Driver 崩溃、Reduce busy、旧 epoch。每项写注入方式、预期行为和报告字段。

习题八：为最终项目设计 RunReport。字段必须覆盖 correctness、performance、resources、faults、recovery、unverified boundaries。

习题九：把当前接口迁移到 CPU 推理引擎。说明 ModelArtifact、TensorBuffer、KernelTask、KV cache、InferenceRequest 分别复用哪些系统能力。

习题十：写一份最终项目评审报告。每个模块用“输入输出合同、不变量、失败路径、指标、未验证边界”五项说明。
\end{exercisebox}
