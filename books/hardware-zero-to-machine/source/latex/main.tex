\documentclass[UTF8,openany,zihao=5,fontset=none]{ctexbook}

\input{preamble/packages}
\input{preamble/layout}
\input{preamble/macros}

\setcounter{tocdepth}{1}
\setcounter{secnumdepth}{1}
\renewcommand{\thesection}{}
\renewcommand{\theHsection}{\thechapter.\arabic{section}}

\begin{document}

\frontmatter
\input{frontmatter/title}
\input{frontmatter/preface}
\tableofcontents

\mainmatter

\part{器件、开关与组合逻辑}
\begin{partbridge}
本部分集中回答一个问题：怎样从可控电流路径得到稳定的 0 和 1，再把多个 0/1 组合成可计算的逻辑网络。讨论对象是器件、电平、门电路和组合逻辑。
\end{partbridge}
\input{chapters/ch01-electricity-diode-switch}

\part{状态、时钟与同步电路}
\begin{partbridge}
组合逻辑没有记忆。要让硬件按阶段推进，必须引入反馈、锁存器、触发器、时钟、复位、寄存器、计数器和状态机。本部分说明状态怎样稳定保存、怎样按边界更新。
\end{partbridge}
\input{chapters/ch04-feedback-latches-flipflops}

\part{运算部件与数据通路}
\begin{partbridge}
有了稳定状态和组合逻辑，就可以构造算术电路、ALU、标志位、寄存器阵列、内部连线和控制信号。本部分仍然站在纯硬件角度：先让值能够被表示、计算、选择和保存，再讨论可重复动作如何形成。
\end{partbridge}
\input{chapters/ch07-number-arithmetic-circuits}

\part{最小 CPU 与整机硬件}
\begin{partbridge}
前面已经有了计算、状态、选择和控制。现在把它们合成 CPU，再继续向外连接主存、总线、设备、中断控制器和启动硬件，并通过 8086、80386 等经典芯片观察真实处理器的演进。
\end{partbridge}
\input{chapters/ch11-minimal-cpu}

\backmatter
\input{backmatter/checklist}

\end{document}
