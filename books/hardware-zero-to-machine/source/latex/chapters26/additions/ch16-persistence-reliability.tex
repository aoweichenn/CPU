\section{从“写完成”到“掉电后仍存在”}

写系统调用返回、块请求完成、设备缓存接收、介质完成编程是四个不同边界。若应用先写数据再写指针，掉电后不能允许“新指针已持久化而新数据尚未持久化”。日志、写时复制和显式 flush 都是在安排这两个持久化顺序。

\begin{longtable}{L{0.2\linewidth}L{0.34\linewidth}L{0.36\linewidth}}
\toprule
层次 & 完成通常表示 & 尚未保证 \\
\midrule
页缓存 & 数据已被内核接收 & 块层、设备和介质完成 \\
块层完成 & 设备报告请求完成 & 无断电保护缓存中的数据抗掉电 \\
设备 FLUSH 完成 & 之前写入跨过设备规定的持久化边界 & 应用自己的元数据顺序必然正确 \\
应用事务提交 & 日志/数据顺序满足协议 & 未纳入协议的外部系统状态 \\
\bottomrule
\end{longtable}

\section{校验、冗余与故障域}

ECC 修复单个器件或扇区中的有限错误，校验和发现数据路径或静默损坏，镜像与纠删码在设备失效后重建数据；它们解决的故障粒度不同。RAID 不是备份：控制器错误、软件误删、勒索软件和整机电源事故可能同时影响阵列所有成员。

分析可靠性时先画故障域：同一芯片、同一通道、同一背板、同一电源、同一机箱、同一机房。两份数据若共享同一个失效点，就不能简单按两次独立概率相乘。重建期间还会增加介质流量与暴露时间，因此容量、校验强度、备用空间和重建限速必须一起设计。

\section{从应用顺序到介质顺序}

应用写入 A 后再写入 B，只能说明系统调用的发起顺序，不能自动证明介质先持久化 A。CPU store buffer、页缓存回写、块层合并、设备队列调度和介质内部映射都可能改变实际执行次序。要建立掉电后仍成立的约束，必须在每层使用相应的顺序原语，并说明完成响应跨过了哪一道边界。

典型的日志事务先写日志数据，再发出持久化同步，随后写提交记录并再次同步；恢复时只有带有效校验且提交完整的事务才能重放。写时复制则先写新数据块，再写指向新块的元数据，最后以一个可判定的根更新提交新版本。两种办法都不依赖“磁盘大概会按顺序写”，而是把可恢复状态编码进介质布局。

\begin{pdfdiagram}[
  node distance=6mm,
  persistbox/.style={draw, rounded corners, align=center, minimum width=34mm, minimum height=9mm, fill=blue!5},
  persistlabel/.style={font=\scriptsize, text=BookMuted, align=left},
  >=Stealth]
\node[persistbox] (app) {应用数据与元数据};
\node[persistbox, below=of app] (page) {页缓存与回写顺序};
\node[persistbox, below=of page] (block) {块层请求与屏障};
\node[persistbox, below=of block] (dev) {设备队列与易失缓存};
\node[persistbox, below=of dev] (media) {介质编程与掉电保持};
\draw[->] (app) -- (page);
\draw[->] (page) -- (block);
\draw[->] (block) -- (dev);
\draw[->] (dev) -- (media);
\node[persistlabel, right=10mm of app] {应用事务或 \code{fsync}};
\node[persistlabel, right=10mm of page] {回写与块层顺序约束};
\node[persistlabel, right=10mm of block] {\code{FLUSH}/\code{FUA} 协议命令};
\node[persistlabel, right=10mm of dev] {设备定义的完成边界};
\node[persistlabel, right=10mm of media] {掉电后的可恢复状态};
\end{pdfdiagram}
\diagramnote{写请求穿过各层的完成边界。每条箭头上的原语只约束相邻层的顺序或完成语义，应用必须把这些边界组合成可恢复事务。}

FUA 表示该次写要达到设备规定的非易失边界后才完成；FLUSH 通常要求此前相关写跨过该边界。具体语义由接口和设备能力定义，驱动应依据能力位选择命令，并正确处理 flush 失败。带断电保护的设备可把电容供能下的缓存纳入持久化边界，但这不是所有设备缓存的默认性质。

\section{部分写、原子写与撕裂}

主机一次写 4 KiB，并不意味着介质以 4 KiB 原子提交。硬盘扇区、Flash page、FTL 元数据和控制器缓存各有自己的故障原子单位；掉电可能留下旧数据、新数据或部分组合，这称为 torn write。文件系统因此给日志头、提交记录和关键元数据增加校验、序号和双份更新，使恢复能够区分完整新版本与中途状态。

设备声明的原子写范围还受起始对齐、长度和命名空间配置约束。超过范围的请求即使在正常运行中一次完成，也不能据此推断掉电原子性。验证时应查询能力、在边界附近做断电注入，并检查恢复算法是否拒绝校验错误或序号不连续的记录。

\section{端到端数据完整性}

介质 ECC 只能保护介质内部的数据位，不能发现应用缓冲区、DMA 路径、控制器固件或错误地址映射造成的静默损坏。端到端保护让数据从产生处携带校验和，并在消费处重新验证；保护信息还可加入逻辑块地址和版本号，检测“数据内容正确却写到了错误位置”或旧数据被错误重放。

校验失败后的动作必须明确。读取可尝试另一个副本或纠删码重建，并在返回前验证重建结果；若没有可信副本，应把错误返回上层，不能用全零或陈旧缓存掩盖。修复写回前还要确认哪个副本较新，避免用旧副本覆盖新数据。

\section{RAID 重建与可靠性窗口}

镜像读取可从任一健康副本完成，写入则要定义何时向上层报告成功。若只完成一个副本便报告，系统在同步第二副本前存在暴露窗口；若等待全部副本，尾延迟和单盘故障影响会更明显。控制器需要用写意图日志、bitmap 或一致性校验记录未完成的条带更新。

校验 RAID 的小写可能先读旧数据与旧校验，再计算并写回新数据和新校验；中途掉电会产生 write hole。全条带写、非易失写缓存、日志或日志结构布局可缩小这一风险。设备失效后的重建会读取大量幸存数据，增加第二故障和潜在扇区错误暴露概率，因此备用容量、重建优先级、前台服务与温度限制要共同调度。

\section{以故障矩阵验证存储协议}

完整设计应列出故障注入点和恢复不变量，而不是只做正常吞吐测试。可以在数据写后、日志提交前、flush 发出后、设备完成前和根指针更新后分别断电；重启后验证文件系统只能看到旧事务或完整新事务，不能看到指向未初始化数据的元数据。

还应覆盖介质返回不可恢复读错、NVMe 控制器复位、完成丢失、重复完成与请求超时。每种故障都要回答：请求是否可能已经执行，软件能否安全重试，队列 generation 如何区分旧完成，数据校验如何判断结果。对非幂等写，未经查询状态便盲目重试可能产生重复副作用。

\section*{章末练习}

\begin{longtable}{L{0.08\linewidth}L{0.32\linewidth}L{0.5\linewidth}}
\toprule
题号 & 类型 & 题目 \\
\midrule
1 & 完成语义 & 比较 write 返回、块请求完成、FLUSH 完成和应用事务提交四种边界。 \\
2 & 顺序设计 & 设计“数据块先于指针持久化”的命令顺序，并说明每个同步点的作用。 \\
3 & 撕裂恢复 & 一条日志记录由头、数据、校验和提交标记构成。掉电恢复时应怎样判断它可否重放？ \\
4 & 完整性 & 为什么介质 ECC 不能代替端到端校验？加入逻辑块地址能检测哪类额外错误？ \\
5 & RAID & 解释校验 RAID 的 write hole，并给出两种缓解机制。 \\
6 & 故障注入 & 为一次控制器复位设计验收条件，覆盖在途请求、迟到完成和数据一致性。 \\
\bottomrule
\end{longtable}

\section*{参考解答与要点}

\begin{longtable}{L{0.08\linewidth}L{0.32\linewidth}L{0.5\linewidth}}
\toprule
题号 & 类型 & 解答要点 \\
\midrule
1 & 完成语义 & write 常只到页缓存；块完成是设备报告；FLUSH 完成表示此前写跨过设备定义的持久化边界；事务提交还要求应用或文件系统的数据与元数据协议成立。 \\
2 & 顺序设计 & 先写数据并执行使其持久化的同步，确认完成后再写指针/元数据并同步。第一个边界防止新指针指向未持久化数据，第二个边界提交新状态。 \\
3 & 撕裂恢复 & 检查长度、版本/序号、校验和及提交标记；只有内容完整、校验通过且提交状态满足协议的记录才重放，其余按未提交处理。 \\
4 & 完整性 & ECC 主要覆盖介质局部，无法发现错误 DMA、控制器路径或错地址；把地址纳入保护信息可检测正确数据被放到错误逻辑块或错块返回。 \\
5 & RAID & 数据与校验更新不是同一掉电原子动作，掉电会使两者不一致；可用非易失写缓存、写意图日志、全条带写或日志结构布局缓解。 \\
6 & 故障注入 & 停止旧提交并标记 generation；明确每个在途请求是完成、可重试或失败；拒绝旧 generation 完成；恢复后校验队列无重复、数据满足旧或新完整状态。 \\
\bottomrule
\end{longtable}
