\DeclareRobustCommand{\code}[1]{\textcolor{BookCodeInline}{\texttt{\nolinkurl{#1}}}}
\DeclareRobustCommand{\term}[2]{\textbf{#1}\,（#2）}
\DeclareRobustCommand{\filepath}[1]{\textcolor{BookCodeInline}{\texttt{\nolinkurl{#1}}}}
\DeclareRobustCommand{\hwkey}[1]{\textcolor{BookAccent}{\textbf{#1}}}
\DeclareRobustCommand{\hwrisk}[1]{\textcolor{BookRed}{\textbf{#1}}}
\DeclareRobustCommand{\hwtiming}[1]{\textcolor{BookAmber}{\textbf{#1}}}
\DeclareRobustCommand{\hwevidence}[1]{\textcolor{BookTeal}{\textbf{#1}}}

% 正文表格使用清晰但不过重的网格：独立表格线颜色比插图辅助线更深，
% 普通行线与 booktabs 表头线也略加粗，保证缩放阅读和手机端仍能辨列。
\setlength{\arrayrulewidth}{0.45pt}
\setlength{\heavyrulewidth}{0.85pt}
\setlength{\lightrulewidth}{0.50pt}
\setlength{\cmidrulewidth}{0.45pt}
\arrayrulecolor{BookTableRule}
\renewcommand{\arraystretch}{1.08}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{\dimexpr#1-0.35pt\relax}|}
\newcommand{\tablerowrule}{\hline}

\newcommand{\tablechunkintro}[4]{%
  \Needspace{9\baselineskip}% 避免页底只留下分段说明和孤立表头
  \par\addvspace{0.55em}%
  {\noindent\footnotesize\color{BookMuted}%
    \textbf{表格分段 #1/#2：}本段从“#3”到“#4”。各列沿用同一表头；先横向读完一个条目，再沿同一列比较相邻条目。\par}%
  \smallskip
}

% 章末练习采用教材式编号题目，不再把题干和答案塞进表格。
\newcounter{hwexercise}[chapter]
\newcounter{hwanswer}[chapter]
\renewcommand{\thehwexercise}{\thechapter.\arabic{hwexercise}}
\renewcommand{\thehwanswer}{\thechapter.\arabic{hwanswer}}

\newenvironment{chapterexercise}[1]
{%
  \refstepcounter{hwexercise}%
  \par\addvspace{0.95em}%
  \noindent{\color{BookAccent}\bfseries 练习 \thehwexercise}\quad\textbf{#1}\par\nobreak
  \smallskip\begingroup\leftskip=1.65em\rightskip=0pt\noindent\ignorespaces
}
{%
  \par\endgroup
}

\newcommand{\exercisecheck}[2]{%
  \par\smallskip\noindent{\footnotesize\color{BookMuted}\textbf{#1：}#2}\par
}

\newenvironment{chapteranswer}[1]
{%
  \refstepcounter{hwanswer}%
  \par\addvspace{1.0em}%
  \noindent{\color{BookTeal}\bfseries 解答 \thehwanswer}\quad\textbf{#1}\par\nobreak
  \smallskip\begingroup\leftskip=1.65em\rightskip=0pt\noindent\ignorespaces
}
{%
  \par\endgroup
}

\newcommand{\answercheck}[2]{%
  \par\smallskip\noindent{\footnotesize\color{BookMuted}\textbf{#1：}#2}\par
}

\lstdefinestyle{hwcode}{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{BookCodeBg},
  frame=single,
  rulecolor=\color{BookRule},
  columns=fullflexible,
  keepspaces=true,
  breaklines=true,
  showstringspaces=false,
  xleftmargin=0.8em,
  xrightmargin=0.4em,
  aboveskip=0.75em,
  belowskip=0.75em
}
\lstset{style=hwcode}

\newtcolorbox{keyidea}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=BookCodeBg,
  colframe=BookAccent,
  title=核心思想,
  fonttitle=\bfseries,
  coltitle=BookNavy,
  before skip=0.75em,
  after skip=0.75em
}

\newtcolorbox{warningbox}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=white,
  colframe=BookRed,
  title=常见误区,
  fonttitle=\bfseries,
  coltitle=white,
  before skip=0.75em,
  after skip=0.75em
}

\newtcolorbox{classicnote}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=white,
  colframe=BookTeal,
  title=经典教材标注,
  fonttitle=\bfseries,
  coltitle=white,
  before skip=0.85em,
  after skip=0.85em
}

\newtcolorbox{chipcase}[1][]{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.55pt,
  colback=white,
  colframe=BookAmber,
  title=芯片案例,
  fonttitle=\bfseries,
  coltitle=white,
  before skip=0.78em,
  after skip=0.78em,
  #1
}

\newenvironment{partbridge}
{%
  \clearpage
  \thispagestyle{plain}
  \vspace*{0.25em}
  {\noindent\Large\bfseries\color{BookNavy}本部分导读\par}%
  \vspace{0.55em}
}
{%
  \par\clearpage
}

\tikzset{
  hw diagram/.style={
    font=\footnotesize\sffamily,
    text=BookInk,
    >=Latex,
    node distance=10mm and 13mm,
    line cap=round,
    line join=round
  },
  hw block/.style={
    draw=BookRule,
    fill=white,
    rounded corners=2pt,
    line width=0.42pt,
    inner xsep=5pt,
    inner ysep=4pt,
    align=center,
    minimum height=8mm
  },
  hw compute/.style={hw block, draw=BookAccent, fill=BookCodeBg},
  hw state/.style={hw block, draw=BookTeal, fill=BookCodeBg},
  hw memory/.style={hw block, draw=BookAmber, fill=white},
  hw control/.style={hw block, draw=BookRed, fill=white},
  hw bus/.style={
    draw=BookMuted,
    line width=0.62pt,
    -{Stealth[length=2.3mm,width=1.7mm]},
    shorten >=1.5pt,
    shorten <=1.5pt
  },
  hw return/.style={
    draw=BookTeal,
    line width=0.58pt,
    dashed,
    -{Stealth[length=2.1mm,width=1.55mm]},
    shorten >=1.5pt,
    shorten <=1.5pt
  },
  hw danger/.style={
    draw=BookRed,
    line width=0.62pt,
    -{Stealth[length=2.3mm,width=1.7mm]},
    shorten >=1.5pt,
    shorten <=1.5pt
  },
  hw lane/.style={
    draw=BookRule,
    fill=BookCodeBg,
    rounded corners=3pt,
    line width=0.35pt,
    inner xsep=6pt,
    inner ysep=5pt,
    align=center
  },
  hw label/.style={
    font=\scriptsize\sffamily,
    text=BookMuted,
    align=center
  }
}


\newenvironment{circuit}[1][]
{\par\vspace{0.6em plus 0.2em}\goodbreak\noindent\centering\begin{tikzpicture}[circuit logic US,scale=0.95,#1]}
{\end{tikzpicture}\par\vspace{0.4em plus 0.1em}}

\newenvironment{waveform}[1][]
{\par\vspace{0.6em plus 0.2em}\goodbreak\noindent\centering\begin{tikzpicture}[hw diagram,#1]}
{\end{tikzpicture}\par\vspace{0.4em plus 0.1em}}

\newenvironment{block}[1][]
{\par\vspace{0.6em plus 0.2em}\goodbreak\noindent\centering\begin{tikzpicture}[hw diagram,#1]}
{\end{tikzpicture}\par\vspace{0.4em plus 0.1em}}

\newenvironment{pdfdiagram}[1][]
{\par\vspace{0.72em plus 0.18em}\goodbreak\noindent\begin{center}\begin{tikzpicture}[hw diagram,scale=0.94,transform shape,#1]}
{\end{tikzpicture}\end{center}\vspace{0.48em plus 0.12em}}

\newcommand{\diagramnote}[1]{%
  \par\goodbreak
  {\noindent\footnotesize\color{BookMuted}图示：#1\par}%
  \smallskip
}

\newcommand{\topic}[1]{%
  \par\medskip{\normalsize\bfseries\color{BookNavy}#1}\quad\ignorespaces
}

%% --- waveform and circuit helpers ------------------------------------
\colorlet{bookink}{BookInk}
\colorlet{bookaccent}{BookAccent}
\colorlet{booknavy}{BookNavy}
\colorlet{bookteal}{BookTeal}
\colorlet{bookamber}{BookAmber}
\colorlet{bookred}{BookRed}
\colorlet{bookcodebg}{BookCodeBg}

% \ctikzset{scale=0.85}  % enabled only when circuitikz is loaded
\usetikzlibrary{math,patterns}

% 晶体管开关模型 pic：NMOS 输入高导通，PMOS 输入低导通（控制端带小圆圈）。
% 接线点：沟道两端 (0,±0.42)，控制端 (-0.65,0)。
\tikzset{
  hw nmos/.pic={
    \draw[line width=0.55pt] (0,0.42) -- (0,0.14);
    \draw[line width=0.55pt] (0,-0.14) -- (0,-0.42);
    \draw[line width=0.85pt] (-0.07,0.16) -- (-0.07,-0.16);
    \draw[line width=0.55pt] (-0.65,0) -- (-0.07,0);
  },
  hw pmos/.pic={
    \draw[line width=0.55pt] (0,0.42) -- (0,0.14);
    \draw[line width=0.55pt] (0,-0.14) -- (0,-0.42);
    \draw[line width=0.85pt] (-0.07,0.16) -- (-0.07,-0.16);
    \draw[line width=0.55pt] (-0.65,0) -- (-0.16,0);
    \draw[line width=0.55pt,fill=white] (-0.115,0) circle (0.05);
  },
  hw gnd/.pic={
    \draw[line width=0.55pt] (0,0) -- (0,-0.13);
    \draw[line width=0.55pt] (-0.16,-0.13) -- (0.16,-0.13);
    \draw[line width=0.55pt] (-0.10,-0.19) -- (0.10,-0.19);
    \draw[line width=0.55pt] (-0.045,-0.25) -- (0.045,-0.25);
  }
}
