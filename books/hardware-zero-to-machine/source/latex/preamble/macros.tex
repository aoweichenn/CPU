\DeclareRobustCommand{\code}[1]{\textcolor{BookCodeInline}{\texttt{\nolinkurl{#1}}}}
\DeclareRobustCommand{\term}[2]{\textbf{#1}\,（#2）}
\DeclareRobustCommand{\filepath}[1]{\textcolor{BookCodeInline}{\texttt{\nolinkurl{#1}}}}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\lstdefinestyle{hwcode}{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{BookCodeBg},
  frame=single,
  rulecolor=\color{BookRule},
  columns=fullflexible,
  keepspaces=true,
  breaklines=true,
  showstringspaces=false,
  xleftmargin=0.8em,
  xrightmargin=0.4em,
  aboveskip=0.75em,
  belowskip=0.75em
}
\lstset{style=hwcode}

\newtcolorbox{keyidea}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=BookCodeBg,
  colframe=BookAccent,
  title=核心思想,
  fonttitle=\bfseries,
  coltitle=BookNavy,
  before skip=0.75em,
  after skip=0.75em
}

\newtcolorbox{warningbox}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=white,
  colframe=BookRed,
  title=常见误区,
  fonttitle=\bfseries,
  coltitle=BookRed,
  before skip=0.75em,
  after skip=0.75em
}

\newtcolorbox{classicnote}{
  enhanced,
  breakable,
  sharp corners,
  boxrule=0pt,
  leftrule=0.5pt,
  colback=white,
  colframe=BookTeal,
  title=经典教材标注,
  fonttitle=\bfseries,
  coltitle=BookTeal,
  before skip=0.85em,
  after skip=0.85em
}

\newenvironment{partbridge}
{%
  \clearpage
  \thispagestyle{plain}
  \vspace*{0.25em}
  {\noindent\Large\bfseries\color{BookNavy}本部分导读\par}%
  \vspace{0.55em}
}
{%
  \par\clearpage
}

\tikzset{
  hw diagram/.style={
    font=\footnotesize\sffamily,
    text=BookInk,
    >=Latex,
    node distance=10mm and 13mm,
    line cap=round,
    line join=round
  },
  hw block/.style={
    draw=BookRule,
    fill=white,
    rounded corners=2pt,
    line width=0.42pt,
    inner xsep=5pt,
    inner ysep=4pt,
    align=center,
    minimum height=8mm
  },
  hw compute/.style={hw block, draw=BookAccent, fill=BookCodeBg},
  hw state/.style={hw block, draw=BookTeal, fill=BookCodeBg},
  hw memory/.style={hw block, draw=BookAmber, fill=white},
  hw control/.style={hw block, draw=BookRed, fill=white},
  hw bus/.style={
    draw=BookMuted,
    line width=0.48pt,
    -{Latex[length=2.1mm,width=1.7mm]}
  },
  hw label/.style={
    font=\scriptsize\sffamily,
    text=BookMuted,
    align=center
  }
}

\newenvironment{pdfdiagram}[1][]
{\par\smallskip\noindent\begin{center}\begin{tikzpicture}[hw diagram,scale=0.94,transform shape,#1]}
{\end{tikzpicture}\end{center}\smallskip}

\newcommand{\diagramnote}[1]{%
  \par\smallskip
  {\noindent\hspace*{1.4em}\footnotesize\color{BookMuted}图示：#1\par}%
  \smallskip
}

\newcommand{\topic}[1]{%
  \par\smallskip\indent{\normalsize\bfseries\color{BookNavy}#1}\quad\ignorespaces
}
