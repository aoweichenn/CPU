\section{CPU decode 实战：从标量 GEMV 到 packed 低比特微内核}

\subsection{decode 优化先看形态}

7B 推理的 decode 阶段每次只处理一个新 token。对线性层来说，输入是一个 hidden 向量，权重是一个大矩阵，输出是一个新向量。这更像 GEMV，而不是大 batch GEMM。CPU 优化要先承认这个形态：每 token 反复流过大块权重，权重字节和 KV 字节很容易支配延迟。

\begin{keyidea}
CPU decode 微内核的目标不是写一个“通用最快矩阵乘”，而是为单 token 或小 batch 的固定权重读流建立可验证的 packed layout、累加策略、tail path、线程切分和性能账本。
\end{keyidea}

本节沿同一个 linear 语义推进：标量 fp32 reference，普通 int8 baseline，packed int8，int4 packed，SIMD 微内核，线程切分，NUMA 和 profiler。每一步都要能回到 oracle。

本节固定一个 decode 形状作为样板，不再泛泛谈“某个矩阵”：

\begin{lstlisting}[numbers=none,caption={CPU decode 样板形状}]
input hidden K = 4096
output channels O = 4096
batch/token T = 1
semantic: y[out] = sum_{k=0}^{4095} W[out,k] * x[k]
weight layout before packing: row-major [O,K]
packed output block: BN = 8 for SIMD kernel, benchmark block field uses 16
\end{lstlisting}

这个 shape 对应 7B decoder 里的典型 hidden-to-hidden projection。它还不是完整一层，因为没有 RMSNorm、attention、MLP 和 KV cache，但它是 decode 阶段最容易被权重读流支配的核心切片。

\subsection{标量 reference 不是性能 baseline}

reference 的职责是正确，不是代表可接受性能。

\begin{lstlisting}[numbers=none,caption={fp32 reference GEMV}]
for out in 0..O:
  acc = 0
  for k in 0..K:
    acc += weight[out, k] * input[k]
  output[out] = acc
\end{lstlisting}

性能 baseline 可以仍然是标量，但要固定测量条件：连续内存、关闭 debug 检查、固定线程数、固定输入、warmup、重复次数和输出校验。否则 benchmark 比较的是调试开销或缓存偶然性。

\subsection{int8 权重 baseline}

最简单的 int8 权重实现是边读边反量化：

\begin{lstlisting}[numbers=none,caption={int8 权重 + fp32 输入 baseline}]
for out in 0..O:
  acc = 0
  scale = scales[out]
  for k in 0..K:
    w = float(weight_i8[out, k]) * scale
    acc += w * input[k]
  output[out] = acc
\end{lstlisting}

这比 fp32 权重少读字节，但每个权重多了转换和 scale。若 \code{scale} 是 per-channel，它可以在外层读取一次；若是 per-group，内层每隔 group size 切换 scale。这里必须先有 QuantDesc：

\begin{lstlisting}[numbers=none,caption={int8 QuantDesc 最小字段}]
QuantDesc:
  bit_width = 8
  signed = true
  scale_policy = per_channel | per_group
  group_size
  zero_point_policy
  scale_dtype
  axis
\end{lstlisting}

没有 descriptor，kernel 无法知道 scale 怎样对应权重，也无法写通用 oracle。

仓库中的 baseline 落在 \filepath{books/cpu-volume-3/labs/linux_cpu_inference/src/int8_kernels.cpp}：\code{linear\_i8} 使用 \code{int8 weight + fp32 input + fp32 accumulator}。它不是完整整数推理，但它有清晰合同：

\begin{lstlisting}[numbers=none,caption={lcqi 当前 int8 scalar 语义}]
row_base = out * input_size
acc = bias[out]
for k in 0..input_size:
  acc += float(weights[row_base + k]) * scale * input[k]
output[out] = acc
\end{lstlisting}

对 \code{4096x4096}，每次调用至少读取约 \code{16 MiB} int8 权重、\code{16 KiB} fp32 input、写 \code{16 KiB} fp32 output。scale 当前是 per-layer 标量，所以它不是主要读流。这个账本解释了为什么 decode GEMV 首先要盯权重读流和 SIMD 转换，而不是盲目增加复杂图优化。

\subsection{packed int8：先改变地址流}

原始权重按 \code{[out,k]} 行主序存储，对单输出行扫描友好。但 SIMD 微内核通常想一次计算多个 \code{out}，让同一个输入 \code{input[k]} 广播到多个输出 accumulator。为了让多个输出通道的权重连续，需要把小面板打包。

\begin{lstlisting}[numbers=none,caption={packed int8 panel 示意}]
for out_block in 0..O step BN:
  for k in 0..K:
    for oi in 0..BN:
      packed.append(weight_i8[out_block + oi, k])
\end{lstlisting}

打包后，内核的热循环读取 \code{packed[k, oi]} 是连续的：

\begin{lstlisting}[numbers=none,caption={packed GEMV 热循环}]
for out_block in 0..O step BN:
  acc[BN] = 0
  for k in 0..K:
    x = input[k]
    wv = load packed weights for BN outputs at this k
    acc += x * dequant(wv)
  store acc
\end{lstlisting}

packing 的成本应发生在模型 prepare 阶段，而不是每次请求 decode 阶段。plan report 要记录 packed weight 字节数、packing 时间和 layout id。否则上线后可能发现首个请求很慢，却不知道慢在 prepare。

当前 packed layout 使用 \code{out\_block -> k -> lane} 顺序。原始 row-major 在固定 \code{k=0} 时，连续 8 个输出通道的地址是：

\begin{lstlisting}[numbers=none,caption={原始 row-major 多输出地址}]
W[0,0]    addr = 0
W[1,0]    addr = 4096
W[2,0]    addr = 8192
...
W[7,0]    addr = 28672
\end{lstlisting}

packed 后，固定 \code{k=0} 的 8 个输出通道变成连续地址：

\begin{lstlisting}[numbers=none,caption={packed panel 多输出地址}]
packed block out0..7, k=0:
  paddr 0..7 = W[0,0], W[1,0], ..., W[7,0]

packed block out0..7, k=1:
  paddr 8..15 = W[0,1], W[1,1], ..., W[7,1]
\end{lstlisting}

\begin{systemdiagram}{4096x4096 decode GEMV 的地址流变化}
\begin{pdfdiagram}[x=1cm,y=1cm]
  \node[book accent node,text width=2.55cm] (raw) at (0,1.6) {row-major W\\\code{addr=out*K+k}};
  \node[book label,text width=3.2cm] (stride) at (3.25,1.6) {同一 \code{k} 取 8 个 \code{out}\\地址相隔 \code{4096} 元素};
  \node[book teal node,text width=2.65cm] (pack) at (6.55,1.6) {packed panel\\\code{out\_block,k,lane}};
  \node[book purple node,text width=2.65cm] (simd) at (9.75,1.6) {AVX2 kernel\\load 8 int8 lanes};

  \draw[book strong arrow] (raw) -- (stride);
  \draw[book strong arrow] (stride) -- (pack);
  \draw[book strong arrow] (pack) -- (simd);

  \node[book label,text width=2.6cm] at (0,-0.15) {适合单输出行扫描};
  \node[book label,text width=2.9cm] at (3.25,-0.15) {不适合一次更新多个输出};
  \node[book label,text width=2.9cm] at (6.55,-0.15) {复制成连续 lane};
  \node[book label,text width=2.9cm] at (9.75,-0.15) {扩展到 fp32 后累加};
\end{pdfdiagram}
\diagramnote{本图要证明的命题是：当前 packed int8 的直接收益是把跨步多输出读取变成连续 lane 读取。}
\end{systemdiagram}

\subsection{int4 packed：字节减少，解包增加}

int4 把两个 4-bit 权重放进一个 byte。常见 signed int4 范围是 \code{-8..7} 或通过 zero point 表示。解包需要取低四位和高四位，再做符号扩展或减 zero point。

\begin{lstlisting}[numbers=none,caption={int4 解包语义}]
byte b contains two values:
  lo = b & 0x0f
  hi = (b >> 4) & 0x0f

signed value:
  if nibble >= 8:
    value = nibble - 16
  else:
    value = nibble
\end{lstlisting}

int4 的优势是权重字节减半，风险是解包、scale metadata 和误差。group quant 下每 \code{G} 个权重共享一个 scale。若 \code{K} 不能整除 \code{G}，最后一组是 tail group。byte oracle 必须覆盖 nibble 顺序、符号扩展、group tail 和 scale 索引。

\subsection{累加器：低比特不等于低精度累加}

低比特权重不代表可以用低精度累加。常见策略有两类：

\begin{itemize}
  \item 权重低比特，输入 fp16/fp32，反量化后用 fp32 accumulator。
  \item 权重和激活都量化，用 int32 accumulator，再 requantize 或转回 fp32。
\end{itemize}

第一类实现简单，适合学习和 CPU baseline；第二类更接近完整整数推理，但需要处理激活 scale、zero point 修正、bias scale 和输出 requantize。第三册前面量化章节已经讲过 zero point 修正，CPU 微内核进入整数路径时必须复用同一合同。

\begin{warningbox}
不要把 int4 权重直接乘成 int8 accumulator。归约维度 \code{K=4096} 时，误差和溢出风险都不可接受。accumulator dtype 是 kernel descriptor 的核心字段。
\end{warningbox}

\subsection{SIMD 微内核的寄存器账本}

假设一次计算 \code{BN=16} 个输出通道。内核维护 16 个 accumulator lane。每个 \code{k}：

\begin{enumerate}
  \item 读取一个 \code{input[k]}。
  \item 广播到 SIMD 寄存器。
  \item 读取 packed 权重的 16 个值。
  \item 转换或解包到可乘格式。
  \item 乘加到 accumulator。
\end{enumerate}

\begin{lstlisting}[numbers=none,caption={抽象 SIMD GEMV 微内核}]
acc = zero_vector(BN)

for k in 0..K:
  x = broadcast(input[k])
  w = load_packed_weight_vector(k)
  wf = convert_or_dequant(w, scale_for(k))
  acc = fma(x, wf, acc)

store output block
\end{lstlisting}

这个账本要和 ISA 绑定。AVX2、AVX-512 的向量宽度、转换指令、整数 dot 能力和 mask tail 都不同。因此代码结构应分成 capability、descriptor、kernel 实现和 fallback，不要在业务调用点写一堆 ISA 分支。

当前 AVX2 实现位于 \filepath{books/cpu-volume-3/labs/linux_cpu_inference/src/int8_kernels_avx2.cpp}。它一次处理 8 个输出通道，用一个 \code{\_\_m256} accumulator 保存 8 个 fp32 lane：

\begin{lstlisting}[numbers=none,caption={当前 AVX2 kernel 的核心语义}]
packed_i8   = load_8_i8(packed_weights + packed_index)
weights_i32 = cvtepi8_epi32(packed_i8)
weights_f32 = cvtepi32_ps(weights_i32)
input_scaled = broadcast(input[k] * scale)
acc = fmadd(weights_f32, input_scaled, acc)
\end{lstlisting}

这段代码的价值是把 \code{8} 个输出通道的标量乘加合并成 8-lane AVX2 累加账本；它的限制也很明确：它先把 int8 扩展成 fp32，再做 fp32 乘加，并没有使用 VNNI/AMX 这类整数 dot 指令，也没有做 int32 accumulator + requantize。

\subsection{tail path：最后一块经常出错}

若输出通道 \code{O} 不是 \code{BN} 的倍数，最后一个 block 需要 tail。常见策略：

\begin{itemize}
  \item 单独标量处理 tail，简单但慢一点。
  \item SIMD mask store，要求 ISA 支持。
  \item padding packed weight 到 \code{BN} 对齐，输出只写有效部分。
\end{itemize}

若归约维度 \code{K} 不是 group size、解包块或向量宽度的倍数，也需要 tail。对 int4，\code{K} 为奇数还会出现半个 byte 的语义问题。虽然真实 7B 的很多维度规整，测试必须包含不规整维度，因为模型变体、lm head、adapter 或小 fixture 都可能触发。

\subsection{线程切分：按输出通道还是按层}

decode 的单个 linear 可以按输出通道切分：不同线程计算不同 \code{out} block，共享同一 input，读取不同权重。优点是没有输出写冲突；缺点是每个线程都读一遍 input，且小层线程开销可能过大。

\begin{lstlisting}[numbers=none,caption={按输出通道切分}]
parallel_for out_block_range:
  run_packed_gemv(input, packed_weight_range, output_range)
\end{lstlisting}

也可以在线路更高层调度：不同层或不同算子顺序依赖强，不适合随意并行；不同 request 的 decode step 可以并行，但会增加权重竞争和 cache 压力。最稳的起点是单算子内按输出 block 切分，并记录线程数和分区。

\begin{center}
\begin{tabular}{p{0.22\linewidth}p{0.34\linewidth}p{0.28\linewidth}}
\toprule
切分方式 & 优点 & 风险 \\
\midrule
按输出通道 & 简单，无输出冲突 & 权重带宽争用、线程开销 \\
按 batch/session & 可提高吞吐 & 状态复杂，p95 可能变差 \\
按层流水 & 理论并行 & decoder 层依赖强，收益有限 \\
\bottomrule
\end{tabular}
\end{center}

\subsection{NUMA 和权重放置}

多路 CPU 或 NUMA 机器上，权重放在哪个 NUMA node 会影响带宽。若线程在 node 0，权重页却在 node 1，远端内存访问会增加延迟。最小工程策略：

\begin{itemize}
  \item prepare 阶段按计划触碰 packed weight，让页分配到目标 node。
  \item 线程池固定亲和，避免跨 node 随机迁移。
  \item plan report 记录线程数、亲和策略和 NUMA 策略。
  \item benchmark 报告硬件拓扑，不把不同机器数字混比。
\end{itemize}

NUMA 优化不要太早做，但 trace 要保留足够字段。否则后期发现 p95 抖动，只能猜测是调度还是内存放置。

\subsection{perf counters：用证据判断瓶颈}

CPU 优化不能只看耗时。至少要观察：

\begin{itemize}
  \item cycles 和 instructions，判断 IPC。
  \item cache miss 和 LLC miss，判断权重/KV 读流。
  \item branch misses，判断 tail 或分支复杂度。
  \item vector instruction 比例，确认 SIMD path 真的执行。
  \item memory bandwidth，判断是否接近带宽上限。
\end{itemize}

如果引入 SIMD 后 instructions 降了但 LLC miss 上升，可能 packing layout 破坏了局部性。如果线程数增加后 tokens/s 不升反降，可能已经带宽饱和或 NUMA 远端访问增加。如果 int4 比 int8 不快，可能解包和 scale 成本抵消了字节收益。

本次环境的 \code{perf stat} 没有暴露可用 PMU events，\code{perf stat -e cycles:u,instructions:u} 返回：

\begin{lstlisting}[numbers=none,caption={当前实验环境的 perf 边界，不伪造计数器}]
Error:
No supported events found.
The cycles:u event is not supported.
\end{lstlisting}

这个结果记录在下面的 perf artifact。它不是通过项，而是缺口证据：当前样板章已经有源码、CSV 和反汇编，但还没有 cycles、IPC、cache miss、branch miss。后续必须在有 PMU 权限的实验环境上补齐。

\begin{lstlisting}[numbers=none,caption={perf artifact}]
books/cpu-volume-3/results/
  lcqi-x86-avx2-perf-boundary-2026-06-25.txt
\end{lstlisting}

\subsection{当前 lab 的真实验收口径}

本仓库的 \code{linux\_cpu\_inference} 已经把本节的一部分落成代码：同一个 int8 linear 语义下有三个 benchmark backend：

\begin{itemize}
  \item \code{scalar}：普通 row-major 标量循环，是 correctness 和耗时基线。
  \item \code{packed\_scalar}：使用 output block packing，但仍用标量累加，用来证明 packed layout、padding 和 tail 语义。
  \item \code{avx2}：x86-64 目标下编译的 8 输出通道 packed kernel。
\end{itemize}

benchmark 不再把跨平台源码混成一个数字，而是按 backend 逐行输出：

\begin{lstlisting}[numbers=none,caption={LCQI kernel benchmark CSV 字段}]
target_arch,input_size,output_size,output_block,repeat,
backend,available,average_us,max_abs_diff,checksum
\end{lstlisting}

这种格式有两个好处。第一，\code{available=0} 可以明确说明某个 ISA 后端没有在当前实验环境实测，不能把源码存在当作性能证据。第二，所有 backend 都和同一份 scalar 输出比较 \code{max\_abs\_diff}，避免只看速度。

当前 x86-64 Release 样本如下，artifact 为 \filepath{books/cpu-volume-3/results/lcqi-x86-avx2-benchmark-2026-06-25.csv}：

\begin{center}
\begin{tabular}{rrrr}
\toprule
shape & scalar us & packed scalar us & AVX2 us \\
\midrule
128x128 & 3.904 & 5.453 & 1.558 \\
256x256 & 17.843 & 21.381 & 6.043 \\
512x512 & 81.341 & 87.787 & 23.733 \\
513x257 & 40.719 & 50.331 & 12.600 \\
1024x4096 & 1394.170 & 1346.740 & 379.846 \\
\bottomrule
\end{tabular}
\end{center}

新增固定 decode 样板 \code{4096x4096} 后，CSV artifact 是 \filepath{books/cpu-volume-3/results/lcqi-x86-avx2-decode4096-2026-06-25.csv}：

\begin{center}
\begin{tabular}{lrrr}
\toprule
backend & average us & max abs diff & 解释 \\
\midrule
scalar & 5870.92 & 0 & row-major 标量基线 \\
packed scalar & 5734.72 & 0 & 证明 layout，略快于标量基线 \\
AVX2 & 1559.09 & 0 & SIMD 路径，约 3.77x 快于 scalar \\
\bottomrule
\end{tabular}
\end{center}

这些数字的解释要克制。packed scalar 只略快，说明 packing 本身不是魔法；它的主要价值是给 SIMD 消费连续 lane。AVX2 变快，说明连续 lane + 向量扩展/乘加击中了当前瓶颈的一部分。它还没有证明达到了硬件上限，因为缺少 PMU 计数器、内存带宽、CPU 频率和 NUMA 信息。

\subsection{把 4096x4096 实测换成带宽和吞吐}

单次 \code{4096x4096} int8 GEMV 至少读取 \code{16 MiB} 权重，输入和输出各约 \code{16 KiB}。忽略很小的 input/output，按权重读流估算有效带宽：

\[
effective\_bandwidth \approx \frac{16MiB}{time}
\]

\begin{center}
\begin{tabular}{lrrr}
\toprule
backend & average us & 有效权重带宽 & 相对 scalar \\
\midrule
scalar & 5870.92 & \code{2.86 GB/s} & \code{1.00x} \\
packed scalar & 5734.72 & \code{2.93 GB/s} & \code{1.02x} \\
AVX2 & 1559.09 & \code{10.76 GB/s} & \code{3.77x} \\
\bottomrule
\end{tabular}
\end{center}

这个带宽数字明显低于现代 CPU 的内存带宽，所以当前 micro-kernel 还不是纯 DRAM 带宽上限。瓶颈更可能在标量循环开销、int8 到 fp32 转换链、FMA 指令吞吐、未展开的热循环、编译器调度、cache/TLB 和缺少多线程。换句话说，AVX2 已经证明 SIMD path 生效，但还没有证明 micro-kernel 接近硬件 roofline。

如果把它放回 7B 账本，单个 \code{4096x4096} projection 的 AVX2 延迟约 \code{1.56 ms}。一层 decoder 有 QKV、O 和 MLP 多个 projection，完整 32 层绝不可能简单乘一个小常数就变成可用 tokens/s。这个样板的价值是拆开一块热路径，不是宣称完整模型速度。

反汇编 artifact 如下。热循环关键片段随后列出：

\begin{lstlisting}[numbers=none,caption={objdump artifact}]
books/cpu-volume-3/results/
  lcqi-x86-avx2-linear-i8-objdump-2026-06-25.txt
\end{lstlisting}

\begin{lstlisting}[numbers=none,caption={AVX2 objdump 摘要}]
vmulss      (%rbx,%rax,4), %xmm3, %xmm0  // input[k] * scale
vpmovsxbd   (%rsi,%rax,8), %ymm1         // load 8 int8 and extend
vcvtdq2ps   %ymm1, %ymm1                 // int32 -> fp32
vbroadcastss %xmm0, %ymm0                // broadcast scaled input
vfmadd231ps %ymm0, %ymm1, %ymm2          // acc += weight * input
\end{lstlisting}

这段反汇编证明当前路径确实进入 AVX2 向量计算；也证明它还不是成熟 int8 dot-product kernel。一个更强实现应减少 \code{vpmovsxbd/vcvtdq2ps/vfmadd} 链路，尝试 VNNI/AMX 整数 dot、更多 unroll、prefetch、线程分区和 per-channel/per-group scale 的更低开销组织。

\subsection{从反汇编看下一步优化点}

当前热循环每个 \code{k} 至少包含：读取 input、读取 8 个 int8 权重、符号扩展到 int32、转 fp32、乘 scale、乘 input、累加。它的优点是语义直观，oracle 容易对齐；缺点是每 8 个权重要走一串转换指令。

\begin{center}
\begin{tabular}{p{0.26\linewidth}p{0.32\linewidth}p{0.28\linewidth}}
\toprule
优化方向 & 可能收益 & 必须补的证据 \\
\midrule
unroll \code{k} 循环 & 减少 loop overhead，提高调度空间 & objdump、instructions、latency \\
更多输出 block & 提高 input 复用 & register pressure、spill 检查 \\
整数 dot path & 减少 int8->fp32 转换链 & accumulator 语义、requant oracle \\
prefetch packed weight & 降低 miss stall & LLC miss、bandwidth counter \\
多线程 out block & 提高吞吐 & p50/p95、带宽饱和点、NUMA \\
per-channel scale 外提 & 降低内层 scale 成本 & scale layout、误差和反汇编 \\
\bottomrule
\end{tabular}
\end{center}

这些优化不能一起上。下一轮应该只选一个变量，例如 \code{k} unroll 或整数 dot path，保持同一 CSV schema、同一 golden、同一 shape sweep，再比较耗时、反汇编和 perf counters。否则数字变快了也不知道是哪一项起作用。

\subsection{本章作业：把微内核打成可复查证据}

本章作业分三级。不要跳级；每一级都要保留代码、命令、CSV、反汇编和报告。

\topic{A级：正确性闭环。} 实现或复查 \code{fp32 reference}、\code{int8 scalar}、\code{packed scalar} 三条路径。固定小 shape \code{T=2,K=8,N=6}，手工写出一个输入、一个权重矩阵和期望输出。要求：

\begin{itemize}
  \item \code{packed roundtrip} 能把 packed bytes 映射回原始 \code{W[out,k]}。
  \item \code{max\_abs\_diff} 对 scalar reference 为 0 或在明确阈值内。
  \item 故意把 \code{N=6} 放进 \code{BN=8} 的 tail path，证明无效 lane 不写输出。
  \item 报告里画出 \code{W[0,0],W[1,0],...,W[7,0]} 从跨步地址变成连续 packed 地址。
\end{itemize}

\topic{B级：SIMD 证据。} 接入一个 ISA path，可以是 AVX2 或 AVX-512。要求：

\begin{itemize}
  \item capability detection 或构建配置能说明当前机器是否可用。
  \item 不可用时 \code{available=0}，不能把交叉编译或未运行结果写成性能。
  \item \code{objdump} 标出目标向量指令，例如 x86 的向量 load、convert 和 FMA。
  \item shape sweep 至少覆盖 \code{128,256,512,1024,4096,513x257}。
\end{itemize}

\topic{C级：性能解释。} 把 \code{4096x4096} 实测换算成有效权重带宽，并和理论内存带宽或外部框架坐标对比。要求：

\begin{itemize}
  \item 报告 scalar、packed scalar、SIMD 的 average us、p95、checksum、max diff。
  \item 报告 pack time，不能把 prepare 成本偷偷藏进或移出 decode。
  \item 尝试 \code{perf stat}；若失败，保留失败输出并使用替代证据。
  \item 对比 oneDNN/OpenBLAS/ggml/llama.cpp 至少一个，并说明 dtype、线程数、shape 是否一致。
\end{itemize}

\subsection{本章报告模板}

\begin{lstlisting}[numbers=none,caption={CPU microkernel report 模板}]
CpuKernelReport:
  change:
    kernel_name
    git_commit
    compiler
    flags
    isa

  semantics:
    formula
    input_shape
    weight_shape
    dtype
    quant_desc
    accumulator_dtype

  correctness:
    reference_path
    packed_roundtrip
    tested_shapes
    max_abs_diff
    failed_cases

  performance:
    warmup
    repeat
    scalar_us
    packed_scalar_us
    simd_us
    p95_us
    effective_weight_bandwidth
    pack_time_us

  evidence:
    benchmark_csv
    objdump_file
    perf_file_or_failure_reason
    external_comparison

  limits:
    missing_isa_paths
    missing_perf_counters
    unsupported_quant_profiles
    next_single_variable_experiment
\end{lstlisting}

这份报告写完，读者才真正拥有一个可复查的 kernel 证据。只说“我写了 int8 matmul”不够；必须说明语义、layout、反汇编、计数器、误差、外部对照和未覆盖边界。

\subsection{oneDNN 对照：只能比较坐标，不能混淆 dtype}

本次还加入了可选 oneDNN 对标目标 \code{lcqi\_onednn\_bench}。它在安装 \code{onednn-devel} 的机器上编译，执行 \code{1x4096} 乘 \code{4096x4096} 的 f32 matmul。CSV artifact 是 \filepath{books/cpu-volume-3/results/lcqi-onednn-f32-4096-2026-06-23.csv}：

\begin{center}
\begin{tabular}{lrrr}
\toprule
library & dtype & shape & average us \\
\midrule
oneDNN matmul & f32 & \code{1x4096 * 4096x4096} & 17187.8 \\
LCQI scalar & int8 weight + fp32 input & \code{4096x4096} & 5868.84 \\
LCQI AVX2 & int8 weight + fp32 input & \code{4096x4096} & 1559.09 \\
\bottomrule
\end{tabular}
\end{center}

这张表不能读成“LCQI 已经比 oneDNN 强”，因为 dtype、权重表示、primitive 选择和线程策略不同。它只能说明本仓库已经有了外部 primitive 坐标，并暴露下一步对标要求：要么实现同 dtype f32 GEMV/GEMM 与 oneDNN 比，要么找 oneDNN 支持的同量化路径；同时固定线程数、ISA、warmup、输出误差和 perf counters。

\subsection{ggml/llama.cpp 对标应比较什么}

成熟推理系统的 CPU decode 不是只靠一个 micro-kernel。ggml/llama.cpp 这类项目还包含 tensor descriptor、低比特 block 格式、线程池、NUMA/亲和、KV cache、sampler、模型加载和 prompt/runtime 调度。对标时至少拆三层：

\begin{center}
\begin{tabular}{p{0.20\linewidth}p{0.34\linewidth}p{0.30\linewidth}}
\toprule
层级 & 比较内容 & 不合格比较 \\
\midrule
单算子 & 同 shape、同 dtype、同线程数、同误差 & 拿 int8 对 f32 直接宣布胜负 \\
decoder block & RMSNorm、QKV、RoPE、KV、MLP、lm head & 只比较一个 linear kernel \\
端到端 & TTFT、decode tok/s、p95、内存峰值、质量 & 只报平均 tokens/s \\
\bottomrule
\end{tabular}
\end{center}

本章当前有两层真实 GGUF 证据。第一层是单算子：LCQI 可以读取 llama.cpp smoke 使用的同一个 \code{SmolLM2-135M-Instruct-Q4\_K\_M.gguf}，解析 GGUF tensor 表，选取真实 \code{Q4\_K} 矩阵，运行 \code{Q4\_K} 权重加 \code{Q8\_K} 激活的 matvec。第二层是端到端 reference path：LCQI 现在能读取同一个 GGUF 里的 tokenizer tokens/merges、\code{F32}、\code{Q8\_0}、\code{Q5\_0}、\code{Q6\_K} 和 \code{Q4\_K} 混合权重，加载 30 层 SmolLM2，执行 RMSNorm、normal RoPE、GQA KV cache、SwiGLU 和 tied lm head，并从文本 prompt 生成 token。第二层当前是 \code{f32\_dequantized\_reference}，不是高性能量化直算引擎：它的作用是给后续优化提供可复查正确性基线。

caw 上的 artifact 写入第三册 results 目录中的 \filepath{lcqi-smollm2-q4-k-benchmark.txt}。这次不是换一个方便的模型单独测，而是复用 llama.cpp smoke 同一个 GGUF 文件；模型身份、文件 hash 和 pinned llama.cpp commit 必须一起进入报告，避免“文件名相同、内容不同”的假对照。

\begin{lstlisting}[numbers=none,caption={SmolLM2 Q4\_K\_M GGUF 身份与 caw 单算子证据}]
artifact=books/cpu-volume-3/results/lcqi-smollm2-q4-k-benchmark.txt
model_source=bartowski/SmolLM2-135M-Instruct-GGUF
model_file=SmolLM2-135M-Instruct-Q4_K_M.gguf
model_expected_bytes=105454432
model_actual_bytes=105454432
model_expected_sha256=2e8040ceae7815abe0dcb3540b9995eaa1fa0d2ca9e797d0a635ae4433c68c2d
model_actual_sha256=2e8040ceae7815abe0dcb3540b9995eaa1fa0d2ca9e797d0a635ae4433c68c2d
model_identity_match=true
llama_cpp_commit=b3fed31b99f9bd37725833674252bccb429bb183
tensor_name=blk.11.ffn_down.weight
tensor_shape_ne0_ne1=1536x576
tensor_bytes=497664
f32_dequantized_bytes=3538944
repeat=30
scalar_q4_q8_matvec_average_us=43.030
scalar_q4_q8_total_average_us=46.133
auto_q4_q8_backend=avx2_maddubs
auto_f32_matvec_average_us=324.783
auto_q4_f32_input_average_us=283.990
auto_q8_quantize_input_average_us=3.400
auto_q4_q8_matvec_average_us=37.205
auto_q4_q8_total_average_us=40.604
auto_speedup_q4_q8_total_vs_f32=7.999
auto_vs_scalar_matvec_speedup=1.157
auto_vs_scalar_total_speedup=1.136
q4_f32_vs_f32_max_abs_diff=0.000
q4_q8_vs_q4_f32_max_abs_diff=0.011
llama_cpp_pp5_tps=1450.48
llama_cpp_tg4_tps=216.82
\end{lstlisting}

读这组数时要分层。LCQI 的 \code{40.604 us} 是单个真实 \code{Q4\_K} tensor matvec 加一次 \code{Q8\_K} input 量化的总耗时，证明量化权重加 \code{Q8\_K} 激活路径已经比“先解成 f32 权重再算”的基线快 \code{7.999x}；llama.cpp 的 \code{pp5=1450.48 tok/s} 和 \code{tg4=216.82 tok/s} 是完整引擎端到端参考，包含模型图、tokenizer、KV cache、线程池、调度和 sampler。二者不能直接相除，但它们共同给出下一步目标：LCQI 还要把这个单算子能力扩展到完整 decoder block，再进入端到端 tokens/s 验收。没有 correctness 对照的 tokens/s，只是吞吐数字，不是 AI Infra 证据。

这轮 AVX2 优化必须诚实看：\code{avx2\_maddubs} 把 \code{Q4\_K x Q8\_K} 单 matvec 从 \code{43.030 us} 降到 \code{37.205 us}，只有 \code{1.157x}；把输入量化也算进去，总耗时从 \code{46.133 us} 到 \code{40.604 us}，只有 \code{1.136x}。原因是当前优化只替换了 32 元素子块里的 nibble 乘加，外层仍然是逐 row、逐 block 扫描；每个 block 仍要解 scale/min、做浮点缩放、写一个 row 的输出，也没有像 llama.cpp 那样把多行、多 block、线程调度、prefetch 和更宽 ISA 合并起来。因此这次证据的结论不是“已经接近 llama.cpp”，而是“第一个可复查的 Q4\_K AVX2 内核已进入默认路径，下一步瓶颈转移到多行调度、scale/min 解包和完整 decoder block 集成”。

caw 上的完整 reference decode artifact 写入 \filepath{books/cpu-volume-3/results/lcqi-smollm2-gguf-reference-decode-caw.txt}。这条命令不是调用 llama.cpp，而是调用 LCQI 新增的 \cmd{lcqi\_llama\_gguf}：

\begin{lstlisting}[numbers=none,caption={LCQI 自研 SmolLM2/GGUF reference decode 证据}]
mode llama_gguf_reference
weight_execution f32_dequantized_reference
architecture llama
model_name Smollm2 135M 8k Lc100K Mix1 Ep2
layers 30
hidden_size 576
query_heads 9
kv_heads 3
head_dim 64
vocab_size 49152
tie_lm_head_to_embedding 1
generated_text <|im_start|>system
You are a helpful AI assistant named SmolLM, trained by Hugging Face<|im_end|>
<|im_start|>user
Hello<|im_end|>
<|im_start|>assistant
Hello!
benchmark_prompt_tokens 31
benchmark_generated_tokens 2
benchmark_prefill_steps 31
benchmark_decode_steps 1
benchmark_first_token_tokens_per_second 0.78061
benchmark_decode_tokens_per_second 22.1886
benchmark_kv_cache_bytes 1520640
benchmark_quantized_weight_bytes 103668480
benchmark_f32_weight_bytes 538060032
benchmark_tensors_loaded 272
\end{lstlisting}

这组数只能说明 LCQI 自研路径已经能端到端生成，不能单独回答“和 llama.cpp 差多少”。严格对比必须先消除 prompt、chat template、tokenizer 和生成长度差异。因此本章又在 caw 上补了一次同输入测量，原始 artifact 写入 \filepath{books/cpu-volume-3/reports/lcqi-smollm2-same-input-compare-caw.txt}，分析摘要写入 \filepath{books/cpu-volume-3/reports/lcqi-smollm2-same-input-compare-analysis.md}。这次固定同一个模型文件、同一个用户 prompt \code{Hello}、同一个展开后的 SmolLM2 chat prompt、同样 \code{max-new=2}，并用 llama.cpp 的 \cmd{llama-tokenize} 证明 31 个 token id 完全一致：

\begin{lstlisting}[numbers=none,caption={caw 同输入 LCQI 与 llama.cpp 对比证据}]
host=caw
nproc=20
model_file=SmolLM2-135M-Instruct-Q4_K_M.gguf
model_actual_sha256=2e8040ceae7815abe0dcb3540b9995eaa1fa0d2ca9e797d0a635ae4433c68c2d
llama_cpp_commit=b3fed31b99f9bd37725833674252bccb429bb183
user_prompt=Hello
max_new_tokens=2
prompt_tokens_lcqi_median=31.0
prompt_tokens_llama_tokenize=31.0
same_input_prompt_token_count_match=true
same_input_prompt_token_ids_match=true
lcqi_prefill_ms_median=1278.470
lcqi_prefill_tps_median=24.2477
lcqi_decode_step_ms_median=44.6356
lcqi_decode_tps_median=22.4037
llama_simple_prompt_eval_ms_median=22.920
llama_simple_prompt_eval_tps_median=1352.47
llama_simple_eval_step_ms_median=2.840
llama_simple_eval_tps_median=351.62
prefill_ms_ratio_lcqi_over_llama_simple=55.779668
decode_step_ms_ratio_lcqi_over_llama_simple=15.716761
lcqi_f32_dequantized_weight_inflation_ratio=5.190199
llama_bench_same_token_count_prefill_tps=1463.87
llama_bench_same_token_count_decode_tps=508.91
\end{lstlisting}

这次同输入数据把上一轮差距拆清楚了。第一，prefill 慢约 \code{55.78x}，主要因为 LCQI 在 \code{llama\_gguf\_generate\_greedy} 中把 31 个 prompt token 逐个当 decode step 跑，没有像 llama.cpp 那样把 prompt 批量送进 graph/backend。第二，decode step 慢约 \code{15.72x}，主要因为当时 LCQI 的 GGUF 路径仍是 \code{f32\_dequantized\_reference}：加载时把 103.7 MB 量化权重解成约 538 MB f32 权重，decoder block 里的 Q/K/V、attention output、SwiGLU gate/up/down 和 tied lm head 仍走通用 f32 matvec。第三，llama.cpp 保留 GGUF 量化 block 热路径，使用 ggml graph 调度、成熟 CPU kernel、线程协调、prompt batching 和更紧凑的 KV cache。差距不是输入不一致，也不是“少一个 AVX2 指令”，而是权重表示、prompt 批处理、矩阵内核、线程调度、KV 表示和整图执行方式都还没有进入成熟引擎状态。

下一步不能只停在 \code{lcqi\_gguf\_q4\_bench} 这种单算子 benchmark。因此 LCQI 把 shape 兼容的 \code{Q4\_K} linear tensor 接入了真实 \code{llama\_gguf.cpp} decoder：loader 不再把这部分权重展开成 f32，而是保存 GGUF 原始 block bytes；前向时把对应输入量化成 \code{Q8\_K} scratch，再调用 \code{matvec\_q4\_k\_q8}。不兼容的 \code{Q4\_K}、\code{Q6\_K}、\code{Q5\_0}、\code{Q8\_0} 和 \code{F32} tensor 继续走 f32 fallback。caw 上新的同输入 A/B artifact 写入 \filepath{books/cpu-volume-3/reports/lcqi-smollm2-q4-direct-compare-caw.txt}，分析摘要写入 \filepath{books/cpu-volume-3/reports/lcqi-smollm2-q4-direct-analysis.md}。

\begin{lstlisting}[numbers=none,caption={Q4\_K direct 接入真实 decoder 后的 caw A/B 证据}]
same_input_prompt_token_ids_match=true
lcqi_q4_direct_off_prefill_ms_median=423.556
lcqi_q4_direct_on_prefill_ms_median=396.926
lcqi_q4_direct_off_decode_step_ms_median=17.2102
lcqi_q4_direct_on_decode_step_ms_median=16.4401
lcqi_q4_direct_off_w_down_ms_median=98.1026
lcqi_q4_direct_on_w_down_ms_median=67.5007
lcqi_q4_direct_prefill_speedup_off_over_on=1.067091
lcqi_q4_direct_decode_step_speedup_off_over_on=1.046843
lcqi_q4_direct_w_down_speedup_off_over_on=1.453357
lcqi_q4_direct_f32_weight_bytes_saved=56623104
lcqi_direct_quantized_weight_byte_share=0.076809
benchmark_hotspot_q4_k_direct_calls_median=512
benchmark_hotspot_f32_fallback_calls_median=6208
current_prefill_ms_ratio_lcqi_over_llama_simple=16.234192
current_decode_step_ms_ratio_lcqi_over_llama_simple=5.252428
\end{lstlisting}

这组数要保守读。和上一轮报告相比，当前 LCQI prefill 中位数从 \code{1278.47 ms} 到 \code{396.93 ms}，decode step 从 \code{44.64 ms} 到 \code{16.44 ms}；但严格归因到本次 \code{Q4\_K} direct 的同二进制 A/B，保守提升是 prefill \code{1.067x}、decode step \code{1.047x}、\code{w\_down} 热点 \code{1.453x}。原因也在报告里：这次只有 \code{7.68\%} 的加载权重字节进入 direct \code{Q4\_K} 路径，实际 direct call 是 \code{512} 次，而 f32 fallback call 仍有 \code{6208} 次。最大剩余热点仍是 f32 fallback 的 \code{w\_gate}、\code{w\_up}、attention projection 和 tied embedding \code{lm\_head}。所以这轮优化的意义不是“已经追平 llama.cpp”，而是第一次把量化 block matvec 从孤立 benchmark 接进真实 decoder，并用 caw A/B 证明它确实减少了 \code{w\_down} 和 materialized f32 权重。下一阶段如果要继续缩小 \code{16.23x}/\code{5.25x} 的差距，应优先扩大 GGUF 量化 tensor 覆盖，或做 batched prefill；继续只磨单个 \code{Q4\_K} 子块 SIMD，收益会被 fallback 路径稀释。

\subsection{微内核进入默认路径的验收}

一个 CPU decode 微内核进入默认路径前，至少要满足：

\begin{rubricbox}
\begin{itemize}
  \item descriptor 明确 dtype、layout、group size、accumulator、tail policy 和 ISA。
  \item packed weight oracle 能把 packed bytes 映射回原始权重。
  \item operator oracle 覆盖整齐尺寸、输出 tail、归约 tail、非对齐输入和 group tail。
  \item layer oracle 覆盖至少一个 tiny decoder block。
  \item performance report 分开 pack time、kernel time、decode step time 和内存峰值。
  \item unsupported ISA 有明确 fallback 或 plan 阶段拒绝。
  \item 反汇编或 profiler 证明确实走到目标 SIMD path。
\end{itemize}
\end{rubricbox}

这条门槛比“benchmark 快了”严格，但它能保护项目长期演进。否则下一次改量化、改 layout、接 GPU fallback 时，很容易破坏 CPU 路径而不自知。

\subsection{硬验收}

读完本节，应该能把 CPU decode 优化拆成可实现任务：

\begin{itemize}
  \item 从 fp32 reference 建立正确性基准，再写 int8/int4 baseline。
  \item 理解 packed layout 改变的是多个输出通道的权重地址流。
  \item 知道 int4 的 nibble 顺序、符号扩展、group scale 和 tail 为什么必须测试。
  \item 能说明 accumulator dtype 为什么是 kernel descriptor 的核心字段。
  \item 能把 SIMD 微内核写成 input broadcast、packed load、dequant、FMA、store 的账本。
  \item 能设计线程切分、NUMA 策略、perf counters 和默认路径验收。
\end{itemize}
