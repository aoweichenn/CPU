二分查找深度题卡 按题型拆成章内大节，每个大节内部固定分成原理、分流、案例矩阵、案例推导和实验复盘几个小节。这样读者先建立结构，再读案例，不会被题号清单打断。

\section{二分查找与答案空间}
这一节把同一题型整理成少数几个层次，而不是把题目标题堆满页面。阅读顺序是先理解原理，再看场景分流，再用案例矩阵建立索引，最后进入单题推导和实验复盘。

\subsection{原理和适用条件}

先线性扫描或线性试答案，再找出能把候选一分为二的单调谓词。二分的核心不是 mid 写法，而是被排除的一半确实不可能包含答案。

\begin{principlebox}{图示：二分查找与答案空间推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 慢点在于每次只排除一个候选。二分能成立，是因为一次比较可以排除一半下标，或者一次可行性检查可以排除一半答案范围。\\
结构选择 & 二分前必须先定义谓词：某个位置是否已经满足边界条件，或者某个答案是否可行。数组二分找的是边界，答案二分找的是最小可行值或最大可行值。\\
不变量 & 建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。\\
代码落地 & 中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。\\
\end{tabular}
\end{principlebox}

\subsection{题型分流}

\begin{principlebox}{题型分流：二分查找与答案空间}
\textbf{基础边界查找。}
判断条件：数组有序，答案是第一个满足某条件的位置。处理方式：把查找目标改写成 lower bound 或 upper bound。代表案例：LeetCode 704 二分查找、LeetCode 35 搜索插入位置、LeetCode 34 查找首尾位置。风险点：空数组、重复值和返回边界是关键。

\textbf{旋转和局部有序。}
判断条件：整体不完全有序，但每次比较能确定一半有序或一侧含答案。处理方式：判断有序半边或趋势方向，再排除不含答案的一侧。代表案例：LeetCode 33 搜索旋转排序数组、LeetCode 153 寻找旋转排序数组最小值、LeetCode 162 寻找峰值。风险点：重复元素会破坏一半有序判断，可能退化。

\textbf{答案空间二分。}
判断条件：答案值越大越容易或越难满足要求，存在可行边界。处理方式：定义检查函数，在答案范围上找最小可行或最大可行值。代表案例：LeetCode 875 爱吃香蕉、LeetCode 1011 船运包裹、LeetCode 410 分割数组最大值、LeetCode 1631 最小体力路径。风险点：检查函数本身常包含贪心或搜索，必须单独证明。

\textbf{矩阵和二维排除。}
判断条件：行列有序或某个角点具备单调排除能力。处理方式：从可排除一行或一列的位置开始扫描，或把矩阵视作有序序列二分。代表案例：LeetCode 240 搜索二维矩阵 II、LeetCode 74 搜索二维矩阵。风险点：从错误角落出发无法排除足够候选。

\end{principlebox}

\subsection{案例矩阵}

本节案例覆盖 LeetCode 33 搜索旋转排序数组、LeetCode 34 排序数组中查找首尾位置、LeetCode 35 搜索插入位置、LeetCode 69 x 的平方根、LeetCode 74 搜索二维矩阵、LeetCode 81 搜索旋转排序数组 II、LeetCode 153 寻找旋转排序数组中的最小值、LeetCode 154 寻找旋转排序数组中的最小值 II、LeetCode 162 寻找峰值、LeetCode 240 搜索二维矩阵 II、LeetCode 410 分割数组的最大值、LeetCode 540 有序数组中的单一元素、LeetCode 704 二分查找、LeetCode 875 爱吃香蕉的珂珂、LeetCode 1011 在 D 天内送达包裹。阅读时不要按题号背答案，而要先判断题面落在哪个分流场景：查询目标是什么，输入约束给了什么单调性或等价关系，暴力慢在哪里，优化结构保存了什么状态。

\begin{principlebox}{案例矩阵}
\textbf{LeetCode 33 搜索旋转排序数组。}
旋转数组每次二分至少有一半保持有序。单点风险：未旋转、长度一、目标不存在、边界等号、存在重复元素时退化。

\textbf{LeetCode 34 排序数组中查找首尾位置。}
首尾位置可以拆成两个边界：第一个大于等于和第一个大于。单点风险：目标不存在、目标在开头或结尾、数组为空、全是目标。

\textbf{LeetCode 35 搜索插入位置。}
题目等价于寻找第一个大于等于目标的位置。单点风险：目标小于所有元素、目标大于所有元素、重复值、空数组。

\textbf{LeetCode 69 x 的平方根。}
答案是最大的整数 r，使得 r 的平方不超过 x。单点风险：x 为零、一、非完全平方、接近 int 最大值。

\textbf{LeetCode 74 搜索二维矩阵。}
矩阵每行有序且下一行首元素大于上一行末元素，可视为一维有序数组。单点风险：空矩阵、单行、单列、目标小于全部或大于全部。

\textbf{LeetCode 81 搜索旋转排序数组 II。}
旋转数组存在重复值时，二分可能无法判断哪一半有序。单点风险：全相同元素、目标不存在、重复值跨过旋转点、退化到线性。

\textbf{LeetCode 153 寻找旋转排序数组中的最小值。}
最小值是旋转断点，右端点可帮助判断中点在哪一段。单点风险：未旋转、长度一、旋转一位、无重复条件。

\textbf{LeetCode 154 寻找旋转排序数组中的最小值 II。}
重复元素会让 mid 和 right 相等时无法判断最小值方向。单点风险：全相等、重复值跨断点、最小值出现多次。

\textbf{LeetCode 162 寻找峰值。}
比较 mid 和 mid 加一 的斜率能判断某侧一定存在峰值。单点风险：长度一、峰在边界、多个峰、相邻不相等假设。

\textbf{LeetCode 240 搜索二维矩阵 II。}
右上角同时具有向左变小、向下变大的排除能力。单点风险：空矩阵、单行、单列、目标在角落、重复值。

\textbf{LeetCode 410 分割数组的最大值。}
连续分成 m 段后最大段和越大，越容易完成分割。单点风险：m 为一、m 等于数组长度、单个元素超过猜测上限、总和溢出。

\textbf{LeetCode 540 有序数组中的单一元素。}
除一个元素外其余元素都成对出现，单一元素会改变配对下标的奇偶规律。单点风险：单一元素在开头或结尾、数组长度一、mid 加一越界。

\textbf{LeetCode 704 二分查找。}
基础题的重点是固定区间语义。单点风险：空数组、长度一、目标在边界、目标不存在。

\textbf{LeetCode 875 爱吃香蕉的珂珂。}
速度越大，总耗时越小，答案具有单调可行性。单点风险：速度下界、堆很大、h 等于堆数、乘法加法溢出。

\textbf{LeetCode 1011 在 D 天内送达包裹。}
容量越大，需要天数越少，存在最小可行容量。单点风险：D 为一、D 等于包裹数、单个包裹超过猜测容量。

\end{principlebox}

\subsection{案例推导}

\noindent\textbf{LeetCode 33 搜索旋转排序数组。} 题目模型：旋转数组每次二分至少有一半保持有序。推导重点：判断哪一半有序，再看目标是否落在有序半边范围内。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“旋转数组每次二分至少有一半保持有序”，如果判断哪一半有序，再看目标是否落在有序半边范围内能给出单调谓词，才进入二分。一句话模型是：旋转数组每次二分至少有一半保持有序。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到旋转数组每次二分至少有一半保持有序对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：判断哪一半有序，再看目标是否落在有序半边范围内。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用未旋转、长度一、目标不存在、边界等号、存在重复元素时退化做对拍，再回答变体：若重复元素很多，需要在无法判断有序半边时收缩边界。

\noindent\textbf{LeetCode 34 排序数组中查找首尾位置。} 题目模型：首尾位置可以拆成两个边界：第一个大于等于和第一个大于。推导重点：不要找到一个目标后向两边线性扩展，否则重复元素很多时退化。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“首尾位置可以拆成两个边界：第一个大于等于和第一个大于”，如果不要找到一个目标后向两边线性扩展，否则重复元素很多时退化能给出单调谓词，才进入二分。一句话模型是：首尾位置可以拆成两个边界：第一个大于等于和第一个大于。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到首尾位置可以拆成两个边界：第一个大于等于和第一个大于对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：不要找到一个目标后向两边线性扩展，否则重复元素很多时退化。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用目标不存在、目标在开头或结尾、数组为空、全是目标做对拍，再回答变体：这个模型能迁移到计数某值出现次数和插入区间边界。

\noindent\textbf{LeetCode 35 搜索插入位置。} 题目模型：题目等价于寻找第一个大于等于目标的位置。推导重点：统一用 lower bound 语义，不在相等时提前返回也能保持边界一致。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“题目等价于寻找第一个大于等于目标的位置”，如果统一用 lower bound 语义，不在相等时提前返回也能保持边界一致能给出单调谓词，才进入二分。一句话模型是：题目等价于寻找第一个大于等于目标的位置。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到题目等价于寻找第一个大于等于目标的位置对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：统一用 lower bound 语义，不在相等时提前返回也能保持边界一致。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用目标小于所有元素、目标大于所有元素、重复值、空数组做对拍，再回答变体：手写二分时用左闭右开可直接返回 left。

\noindent\textbf{LeetCode 69 x 的平方根。} 题目模型：答案是最大的整数 r，使得 r 的平方不超过 x。推导重点：在整数范围上二分右边界，比较时用除法或 long long 避免平方溢出。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“答案是最大的整数 r，使得 r 的平方不超过 x”，如果在整数范围上二分右边界，比较时用除法或 long long 避免平方溢出能给出单调谓词，才进入二分。一句话模型是：答案是最大的整数 r，使得 r 的平方不超过 x。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到答案是最大的整数 r，使得 r 的平方不超过 x对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：在整数范围上二分右边界，比较时用除法或 long long 避免平方溢出。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用x 为零、一、非完全平方、接近 int 最大值做对拍，再回答变体：若要求浮点精度，可以二分实数区间或用牛顿迭代。

\noindent\textbf{LeetCode 74 搜索二维矩阵。} 题目模型：矩阵每行有序且下一行首元素大于上一行末元素，可视为一维有序数组。推导重点：把下标 mid 映射到 row 和 col，再做标准二分。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“矩阵每行有序且下一行首元素大于上一行末元素，可视为一维有序数组”，如果把下标 mid 映射到 row 和 col，再做标准二分能给出单调谓词，才进入二分。一句话模型是：矩阵每行有序且下一行首元素大于上一行末元素，可视为一维有序数组。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到矩阵每行有序且下一行首元素大于上一行末元素，可视为一维有序数组对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：把下标 mid 映射到 row 和 col，再做标准二分。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用空矩阵、单行、单列、目标小于全部或大于全部做对拍，再回答变体：若只是行列分别有序但行之间不连续，应改用右上角排除法。

\noindent\textbf{LeetCode 81 搜索旋转排序数组 II。} 题目模型：旋转数组存在重复值时，二分可能无法判断哪一半有序。推导重点：当左右和中点值相同导致信息不足时，收缩边界；否则仍按有序半边排除。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“旋转数组存在重复值时，二分可能无法判断哪一半有序”，如果当左右和中点值相同导致信息不足时，收缩边界；否则仍按有序半边排除能给出单调谓词，才进入二分。一句话模型是：旋转数组存在重复值时，二分可能无法判断哪一半有序。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到旋转数组存在重复值时，二分可能无法判断哪一半有序对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：当左右和中点值相同导致信息不足时，收缩边界；否则仍按有序半边排除。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用全相同元素、目标不存在、重复值跨过旋转点、退化到线性做对拍，再回答变体：这题说明二分依赖信息量，重复值会削弱每次排除一半的能力。

\noindent\textbf{LeetCode 153 寻找旋转排序数组中的最小值。} 题目模型：最小值是旋转断点，右端点可帮助判断中点在哪一段。推导重点：若 mid 大于 right，最小值在右侧；否则在左侧含 mid。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“最小值是旋转断点，右端点可帮助判断中点在哪一段”，如果若 mid 大于 right，最小值在右侧；否则在左侧含 mid能给出单调谓词，才进入二分。一句话模型是：最小值是旋转断点，右端点可帮助判断中点在哪一段。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到最小值是旋转断点，右端点可帮助判断中点在哪一段对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：若 mid 大于 right，最小值在右侧；否则在左侧含 mid。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用未旋转、长度一、旋转一位、无重复条件做对拍，再回答变体：有重复时遇到相等只能收缩 right，最坏退化。

\noindent\textbf{LeetCode 154 寻找旋转排序数组中的最小值 II。} 题目模型：重复元素会让 mid 和 right 相等时无法判断最小值方向。推导重点：相等时只能安全地丢掉一个 right，因为它不是唯一必要候选。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“重复元素会让 mid 和 right 相等时无法判断最小值方向”，如果相等时只能安全地丢掉一个 right，因为它不是唯一必要候选能给出单调谓词，才进入二分。一句话模型是：重复元素会让 mid 和 right 相等时无法判断最小值方向。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到重复元素会让 mid 和 right 相等时无法判断最小值方向对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：相等时只能安全地丢掉一个 right，因为它不是唯一必要候选。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用全相等、重复值跨断点、最小值出现多次做对拍，再回答变体：这题说明二分依赖的信息不足时会退化，不是所有旋转题都严格对数。

\noindent\textbf{LeetCode 162 寻找峰值。} 题目模型：比较 mid 和 mid 加一 的斜率能判断某侧一定存在峰值。推导重点：上升则右侧有峰，下降则左侧含 mid 有峰。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“比较 mid 和 mid 加一 的斜率能判断某侧一定存在峰值”，如果上升则右侧有峰，下降则左侧含 mid 有峰能给出单调谓词，才进入二分。一句话模型是：比较 mid 和 mid 加一 的斜率能判断某侧一定存在峰值。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到比较 mid 和 mid 加一 的斜率能判断某侧一定存在峰值对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：上升则右侧有峰，下降则左侧含 mid 有峰。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用长度一、峰在边界、多个峰、相邻不相等假设做对拍，再回答变体：二维峰值需要按行列最大值进行更复杂二分。

\noindent\textbf{LeetCode 240 搜索二维矩阵 II。} 题目模型：右上角同时具有向左变小、向下变大的排除能力。推导重点：当前值大于目标就左移，小于目标就下移。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“右上角同时具有向左变小、向下变大的排除能力”，如果当前值大于目标就左移，小于目标就下移能给出单调谓词，才进入二分。一句话模型是：右上角同时具有向左变小、向下变大的排除能力。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到右上角同时具有向左变小、向下变大的排除能力对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：当前值大于目标就左移，小于目标就下移。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用空矩阵、单行、单列、目标在角落、重复值做对拍，再回答变体：从左上角无法一次排除一行或一列。

\noindent\textbf{LeetCode 410 分割数组的最大值。} 题目模型：连续分成 m 段后最大段和越大，越容易完成分割。推导重点：二分最大段和上限，检查函数贪心累计，超过上限就新开一段。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“连续分成 m 段后最大段和越大，越容易完成分割”，如果二分最大段和上限，检查函数贪心累计，超过上限就新开一段能给出单调谓词，才进入二分。一句话模型是：连续分成 m 段后最大段和越大，越容易完成分割。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到连续分成 m 段后最大段和越大，越容易完成分割对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：二分最大段和上限，检查函数贪心累计，超过上限就新开一段。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用m 为一、m 等于数组长度、单个元素超过猜测上限、总和溢出做对拍，再回答变体：也可用 DP 求精确最优，但答案二分更适合大输入。

\noindent\textbf{LeetCode 540 有序数组中的单一元素。} 题目模型：除一个元素外其余元素都成对出现，单一元素会改变配对下标的奇偶规律。推导重点：把 mid 调整到偶数位，若 nums[mid] 等于 nums[mid+1]，单一元素在右侧，否则在左侧。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“除一个元素外其余元素都成对出现，单一元素会改变配对下标的奇偶规律”，如果把 mid 调整到偶数位，若 nums[mid] 等于 nums[mid+1]，单一元素在右侧，否则在左侧能给出单调谓词，才进入二分。一句话模型是：除一个元素外其余元素都成对出现，单一元素会改变配对下标的奇偶规律。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到除一个元素外其余元素都成对出现，单一元素会改变配对下标的奇偶规律对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：把 mid 调整到偶数位，若 nums[mid] 等于 nums[mid+1]，单一元素在右侧，否则在左侧。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用单一元素在开头或结尾、数组长度一、mid 加一越界做对拍，再回答变体：异或也能求值，但二分利用有序性达到对数时间。

\noindent\textbf{LeetCode 704 二分查找。} 题目模型：基础题的重点是固定区间语义。推导重点：左闭右开写法能统一 lower bound 和存在性检查。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“基础题的重点是固定区间语义”，如果左闭右开写法能统一 lower bound 和存在性检查能给出单调谓词，才进入二分。一句话模型是：基础题的重点是固定区间语义。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到基础题的重点是固定区间语义对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：左闭右开写法能统一 lower bound 和存在性检查。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用空数组、长度一、目标在边界、目标不存在做对拍，再回答变体：所有复杂二分都应该能退回到这道题的边界不变量。

\noindent\textbf{LeetCode 875 爱吃香蕉的珂珂。} 题目模型：速度越大，总耗时越小，答案具有单调可行性。推导重点：二分速度，检查函数用向上取整累加小时数。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“速度越大，总耗时越小，答案具有单调可行性”，如果二分速度，检查函数用向上取整累加小时数能给出单调谓词，才进入二分。一句话模型是：速度越大，总耗时越小，答案具有单调可行性。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到速度越大，总耗时越小，答案具有单调可行性对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：二分速度，检查函数用向上取整累加小时数。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用速度下界、堆很大、h 等于堆数、乘法加法溢出做对拍，再回答变体：船运包裹和分割数组都是同类最小可行上限。

\noindent\textbf{LeetCode 1011 在 D 天内送达包裹。} 题目模型：容量越大，需要天数越少，存在最小可行容量。推导重点：下界是最大包裹，上界是总重量，检查函数按顺序贪心装船。

\textbf{读题拆解。} 读题时先找候选空间和目标边界。本题可以压成“容量越大，需要天数越少，存在最小可行容量”，如果下界是最大包裹，上界是总重量，检查函数按顺序贪心装船能给出单调谓词，才进入二分。一句话模型是：容量越大，需要天数越少，存在最小可行容量。这里要先确认答案对象、合法性条件和优化目标，不要把题型名字当成推导。

\textbf{暴力基线。} 慢版本按顺序扫描下标或逐个试答案值，直到遇到目标边界。它能保证找到容量越大，需要天数越少，存在最小可行容量对应的答案，但每次只排除一个候选，浪费了单调性。这个慢版本的任务是保证不漏答案；它不追求快，只负责给优化版提供对拍基准。随后再引入优化抓手：下界是最大包裹，上界是总重量，检查函数按顺序贪心装船。

\textbf{手算样例。} 手算时列出 left、mid、right 和谓词真假。每一步都要写出被丢弃的一侧为什么不含答案。手算表至少记录输入位置、状态变化、答案更新和停止条件。只写最终答案看不出边界，逐轮记录才能发现状态更新顺序是否错。

\textbf{状态升级。} 把逐个试候选改成维护答案所在区间；谓词为真或假时，必须能一次排除一半候选。状态字段建议写成：left、right、mid、谓词返回值、答案边界含义；每一轮更新都要保留第一个可行值或目标位置。不变量要落到代码上：建议固定左闭右开区间。循环中始终维护答案在 \code{[left, right)} 内，左侧一定不可行或小于目标，右侧外部已经被排除。

\textbf{代码落点。} 检查函数单独写出，mid 不溢出，循环退出条件和返回值配套；每个分支都要解释丢弃区间。C++ 侧还要落实：中点写成 \code{left + (right - left) / 2}。判断平方、乘法、总量时避免溢出。用 \code{std::lower_bound} 能表达清楚时优先用它，但面试要能手写同等语义。

\textbf{正确性检查。} 证明从排除一半开始：被丢弃区间和目标边界的关系已经由谓词保证，不是靠经验猜测。证明时把算法和暴力版对齐：暴力能覆盖的合法候选仍然会被考虑，优化结构只替代重复检查，不改变答案集合。

\textbf{边界和变体。} 先用D 为一、D 等于包裹数、单个包裹超过猜测容量做对拍，再回答变体：它和分割数组最大值都是连续分段最大和模型。

\subsection{实验复盘}

追问通常会要求你写出单调谓词和答案边界。请准备说明 left、right 的语义，为什么 mid 被排除或保留，以及返回值是否还需要二次验证。

复盘时先挑一个最小样例手算暴力版本，再挑一个会打破错误模板的反例，最后用本节案例矩阵中的相邻题互相比较。能说清楚相邻题为什么不能共用同一状态，才算掌握这一类题。
