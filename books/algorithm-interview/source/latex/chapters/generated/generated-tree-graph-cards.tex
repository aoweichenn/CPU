\section{tree-graph 深度题卡}

\subsection{二叉树与树形状态}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：二叉树与树形状态推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。\\
结构选择 & 树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。\\
不变量 & 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。\\
代码落地 & 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。\\
\end{tabular}
\end{principlebox}

\subsection{94 二叉树中序遍历}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。中序访问顺序是左子树、根、右子树，BST 中会得到有序序列。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“用显式栈模拟一路向左，再回退访问根并转向右子树。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“用显式栈模拟一路向左，再回退访问根并转向右子树。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、链式左树、链式右树。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、链式左树、链式右树。Morris 遍历可做到常数额外空间，但会临时改树结构。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{98 验证二叉搜索树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。BST 约束是全局上下界，不是只比较父子节点。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“中序严格递增或显式传递上下界都可以；中序版本要保存前一个访问值。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“中序严格递增或显式传递上下界都可以；中序版本要保存前一个访问值。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：重复值、int 最小最大值、局部合法但全局非法。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：重复值、int 最小最大值、局部合法但全局非法。若允许重复值，需要明确重复放左还是放右。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{102 二叉树层序遍历}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。队列在每轮开始时保存当前层所有节点。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“记录 level size 固定层边界，避免下一层节点混入当前层。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“记录 level size 固定层边界，避免下一层节点混入当前层。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、只有根、最后一层不满、左右顺序。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、只有根、最后一层不满、左右顺序。锯齿形层序只是在每层输出顺序上增加方向控制。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{103 二叉树锯齿形层序遍历}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。BFS 层边界不变，变化的是当前层写入结果的方向。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“可以层内反转，也可以预分配数组按方向写入目标下标。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“可以层内反转，也可以预分配数组按方向写入目标下标。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：单层、偶数层、奇数层、空树。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：单层、偶数层、奇数层、空树。若要求从底向上输出，收集后反转层列表即可。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{104 二叉树最大深度}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。深度可以看成层数，也可以作为栈中携带的状态。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“显式栈保存节点和深度，避免极端链式树递归栈过深。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“显式栈保存节点和深度，避免极端链式树递归栈过深。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树返回零、根深度是一、链式树。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树返回零、根深度是一、链式树。最小深度不能简单取左右最小，因为空子树不构成叶子路径。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{105 前序中序构造二叉树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。前序给根，中序把左右子树切开。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“用哈希表记录中序下标，避免每次线性寻找根位置。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“用哈希表记录中序下标，避免每次线性寻找根位置。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：节点值唯一、空子树、左右子树长度计算、输入不合法。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：节点值唯一、空子树、左右子树长度计算、输入不合法。后序中序构造类似，只是根来自后序末尾。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{106 中序后序构造二叉树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。后序末尾是根，中序位置确定左右子树规模。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“构造时要小心后序左右区间边界，避免切分错位。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“构造时要小心后序左右区间边界，避免切分错位。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、全左链、全右链。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、全左链、全右链。若给前序和后序，二叉树通常不能唯一确定，除非额外限制。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{108 有序数组转二叉搜索树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。有序数组中点作为根能让左右规模接近。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“每次选择区间中点，左右子区间构造左右子树。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“每次选择区间中点，左右子区间构造左右子树。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空区间、偶数长度选左中还是右中、高度平衡定义。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空区间、偶数长度选左中还是右中、高度平衡定义。工程上递归可读，若严格避免递归可用队列保存区间和父指针。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{110 平衡二叉树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每个节点需要知道子树高度以及是否已经失衡。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“后序汇总时一旦子树失衡就向上返回失败标记，避免重复算高度。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“后序汇总时一旦子树失衡就向上返回失败标记，避免重复算高度。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、左右高度差正好一、链式树。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、左右高度差正好一、链式树。可以把返回值设计成 optional 高度，空表示不平衡。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{111 二叉树最小深度}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。最小深度是到最近叶子的节点数，不是左右深度简单取最小。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“BFS 第一次遇到叶子就是最小深度，因为按层扩展。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“BFS 第一次遇到叶子就是最小深度，因为按层扩展。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：只有左子树、只有右子树、根为空、根是叶子。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：只有左子树、只有右子树、根为空、根是叶子。DFS 写法必须特殊处理空子树不能作为最小路径。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{112 路径总和}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。路径必须从根到叶子，状态是剩余目标值。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“沿路径减去当前节点值，到叶子时检查剩余是否等于叶子值。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“沿路径减去当前节点值，到叶子时检查剩余是否等于叶子值。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：负数节点、目标为零、非叶子提前达到目标、空树。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：负数节点、目标为零、非叶子提前达到目标、空树。若要求列出所有路径，需要保存路径数组并在回退时弹出。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{113 路径总和 II}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。相比判定题，状态还要保存当前路径。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“到叶子且和满足时复制路径；回溯时恢复路径。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“到叶子且和满足时复制路径；回溯时恢复路径。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：多个答案、负数、空树、路径数组复制时机。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：多个答案、负数、空树、路径数组复制时机。若路径不要求从根到叶，模型会变成前缀和加哈希。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{114 二叉树展开为链表}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。目标是按前序顺序把树原地改成右指针链。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“显式栈按右后左先入顺序处理，逐个接到前驱右侧。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“显式栈按右后左先入顺序处理，逐个接到前驱右侧。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、原有右子树不能丢、左指针置空。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、原有右子树不能丢、左指针置空。也可用寻找左子树最右节点的原地方法。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{124 二叉树最大路径和}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。路径可以从任意节点到任意节点，但不能分叉向父节点贡献两边。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“每个节点向父亲贡献单边最大增益，全局答案可用左右两边加当前节点更新。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“每个节点向父亲贡献单边最大增益，全局答案可用左右两边加当前节点更新。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：全负数、单节点、左右增益为负时取零、答案初始值。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全负数、单节点、左右增益为负时取零、答案初始值。若路径必须从根到叶，状态和答案位置完全不同。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{199 二叉树右视图}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每层从右侧能看到的就是该层最后访问或最先访问的右侧节点。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“BFS 固定层边界，取每层最后一个；或 DFS 按右优先首次到达深度。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“BFS 固定层边界，取每层最后一个；或 DFS 按右优先首次到达深度。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、左链、右链、左右交错。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、左链、右链、左右交错。若求左视图，只改变层内取值方向。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{208 实现 Trie}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。前缀树把字符串公共前缀共享成路径。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“插入和查询都逐字符沿边移动，不存在的边按需要创建。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“插入和查询都逐字符沿边移动，不存在的边按需要创建。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空字符串约定、单词结束标记、前缀存在不等于单词存在。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空字符串约定、单词结束标记、前缀存在不等于单词存在。若字符集很大，用哈希子节点；小写字母用数组更快。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{211 添加与搜索单词}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。点号通配符让查询可能分叉，但前缀树仍能剪掉不匹配前缀。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“普通字符沿唯一边走，点号枚举当前节点所有孩子。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“普通字符沿唯一边走，点号枚举当前节点所有孩子。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：连续通配符、空结果、单词结束标记、字符集大小。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：连续通配符、空结果、单词结束标记、字符集大小。大量通配符会接近遍历整棵 Trie，需要规模约束。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{226 翻转二叉树}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每个节点交换左右孩子即可，遍历顺序不影响最终结构。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“显式队列或栈逐个节点交换，避免递归深度。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“显式队列或栈逐个节点交换，避免递归深度。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、只有一侧子树、重复值。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、只有一侧子树、重复值。这题简单但适合训练树节点指针修改。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{230 二叉搜索树中第 K 小}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。BST 中序遍历按升序访问节点。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“显式栈中序，访问第 k 个节点时返回。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“显式栈中序，访问第 k 个节点时返回。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：k 为一、k 为节点数、树不平衡、是否有重复值。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：k 为一、k 为节点数、树不平衡、是否有重复值。若频繁查询，可在节点维护子树大小成为顺序统计树。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{235 二叉搜索树最近公共祖先}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。BST 的值域关系能决定两个目标在当前节点的哪一侧。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“两个值都小去左，都大去右，否则当前节点就是分叉点。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“两个值都小去左，都大去右，否则当前节点就是分叉点。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：一个节点是另一个祖先、目标顺序、重复值约定。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：一个节点是另一个祖先、目标顺序、重复值约定。普通二叉树不能用值域剪枝，需要父指针或后序状态。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{236 二叉树最近公共祖先}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。普通树没有有序性，只能依靠祖先关系。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“父指针集合或后序返回目标命中状态都可行。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“父指针集合或后序返回目标命中状态都可行。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：目标不存在、p 等于 q、一个是另一个祖先、比较节点地址。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：目标不存在、p 等于 q、一个是另一个祖先、比较节点地址。多次 LCA 查询可预处理倍增祖先表。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{337 打家劫舍 III}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。树上相邻节点不能同时选，每个节点需要偷和不偷两个状态。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“后序汇总：偷当前则孩子不能偷，不偷当前则孩子可偷可不偷。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“后序汇总：偷当前则孩子不能偷，不偷当前则孩子可偷可不偷。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、负值是否可能、链式树、递归深度。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、负值是否可能、链式树、递归深度。可用显式栈后序保存节点状态，避免调用栈。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{450 删除二叉搜索树中的节点}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。删除节点后仍要保持 BST 中序有序。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“若有两个孩子，用后继或前驱替换当前值，再删除那个后继节点。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“若有两个孩子，用后继或前驱替换当前值，再删除那个后继节点。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：删除叶子、一个孩子、两个孩子、删除根节点。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：删除叶子、一个孩子、两个孩子、删除根节点。工程实现要清楚是移动值还是重连节点。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{543 二叉树直径}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。直径经过某节点时等于左高加右高，但向父亲只能贡献单边高度。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“二叉树与树形状态”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法对每个节点反复扫描子树，或者只检查父子局部关系。树题真正考的是状态如何从父到子、从子到父或按层传播。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于同一棵子树被重复求高度、最大值、是否包含目标等信息。若每个节点能一次性向父节点汇报足够信息，就不应重复下钻。 放到本题里，要重点观察“后序遍历同时更新全局直径并返回高度。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。树形 DP 把每个节点看成子问题根；BFS 把层次边界显式放进队列；BST 题利用中序有序或上下界约束。工程实现优先考虑显式栈避免深树调用栈风险。 本题的优化应当围绕“后序遍历同时更新全局直径并返回高度。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 返回值或栈元素必须有语义：当前子树高度、当前路径和、当前节点合法上下界、父指针、层号。不要只说遍历。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常是对子树归纳。左右子树已经给出稳定答案，父节点只需按题目约束合并这两个答案。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 示例中可以用指针节点，但比较节点身份时比较指针。极端深树用 \code{std::vector<TreeNode*>} 显式栈或队列。最大路径类题要用足够小的初值，不能默认零。本题还要特别注意：空树、单节点、链式树、边数还是节点数定义。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空树、单节点、链式树、边数还是节点数定义。最大路径和是同型题，但贡献值和全局更新有负数处理。实验包含空树、单节点、链式树、完全树、含重复值、全负值、目标节点不存在和极端不平衡树。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{图建模、BFS 与 DFS}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：图建模、BFS 与 DFS推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。\\
结构选择 & 无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。\\
不变量 & 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。\\
代码落地 & 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。\\
\end{tabular}
\end{principlebox}

\subsection{127 单词接龙}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每个单词是节点，一次修改一个字符形成边。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“通配模式桶加速邻居查询，BFS 第一次到达终点就是最短转换长度。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“通配模式桶加速邻居查询，BFS 第一次到达终点就是最短转换长度。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：终点不在词表、起点和终点相同、桶重复扫描、单词长度一致。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：终点不在词表、起点和终点相同、桶重复扫描、单词长度一致。双向 BFS 可以进一步降低搜索空间。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{130 被围绕的区域}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。只有不连通到边界的 O 才会被翻转。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“从边界 O 出发标记安全区域，再翻转剩余 O。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“从边界 O 出发标记安全区域，再翻转剩余 O。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：全边界、无 O、单行单列、内部岛屿。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全边界、无 O、单行单列、内部岛屿。这是反向思维：找不能被翻转的区域比直接判断每块区域更简单。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{133 克隆图}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每个原节点需要唯一对应一个新节点，边按原图连接。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“哈希表保存原节点到克隆节点的映射，BFS 或 DFS 遍历图。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“哈希表保存原节点到克隆节点的映射，BFS 或 DFS 遍历图。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：空图、自环、重边、环结构、不能按节点值唯一假设。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空图、自环、重边、环结构、不能按节点值唯一假设。若图节点值不唯一，必须用地址作为键。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{200 岛屿数量}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。陆地格子是节点，四方向相邻是边，岛屿是连通块。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“扫描遇到未访问陆地就启动搜索并标记整块。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“扫描遇到未访问陆地就启动搜索并标记整块。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：空网格、全水、全陆、斜对角不连通、是否允许修改输入。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空网格、全水、全陆、斜对角不连通、是否允许修改输入。也可用并查集合并相邻陆地，适合动态岛屿扩展。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{207 课程表}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。课程是节点，先修关系是有向边，问题是判断是否有环。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“入度拓扑排序不断学习当前无依赖课程。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“入度拓扑排序不断学习当前无依赖课程。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：边方向、孤立课程、环、自依赖、重复边。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：边方向、孤立课程、环、自依赖、重复边。DFS 三色标记也能判环，但要明确访问状态。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{210 课程表 II}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。在判环基础上还要输出一个可行拓扑序。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“每弹出一个入度为零课程，就把它加入顺序并删除出边。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“每弹出一个入度为零课程，就把它加入顺序并删除出边。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：有环返回空数组、多个可行顺序、边方向。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：有环返回空数组、多个可行顺序、边方向。若需要字典序最小拓扑序，把队列换成小顶堆。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{417 太平洋大西洋水流问题}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。从每个格子向海搜索很慢，反向从海边沿非递减高度搜索。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“分别标记能到太平洋和大西洋的格子，交集就是答案。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“分别标记能到太平洋和大西洋的格子，交集就是答案。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：单行单列、平台高度、边界重复入队、方向条件反向。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：单行单列、平台高度、边界重复入队、方向条件反向。反向搜索是很多可达性题的重要技巧。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{695 岛屿的最大面积}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。面积就是一个连通块中陆地格子的数量。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“每发现新陆地就搜索完整连通块并计数，取最大值。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“每发现新陆地就搜索完整连通块并计数，取最大值。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：全水、全陆、细长岛、是否允许修改输入。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全水、全陆、细长岛、是否允许修改输入。与岛屿数量相比，只是连通块聚合值不同。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{752 打开转盘锁}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。四位密码是状态，每次转动一位形成边。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“BFS 从起点扩展，死亡状态不能入队，访问状态避免重复。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“BFS 从起点扩展，死亡状态不能入队，访问状态避免重复。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：起点死亡、目标就是起点、数字环绕、死亡集合很大。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：起点死亡、目标就是起点、数字环绕、死亡集合很大。双向 BFS 能明显减少状态展开。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{994 腐烂的橘子}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。所有初始腐烂橘子同时作为 BFS 源点。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“图建模、BFS 与 DFS”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法常从每个节点重新搜索，或者在图中不加访问标记地反复扩展状态。图题第一步不是选 DFS 还是 BFS，而是明确节点和边。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点来自重复访问和邻居查询过慢。若节点很多但边很少，用邻接矩阵会浪费空间；若每次找邻居都扫描全体节点，BFS 本身再正确也会超时。 放到本题里，要重点观察“按层扩散，分钟数等于最后一个新鲜橘子被首次访问的层数。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。无权最短步数用 BFS，连通块和状态检测用 DFS 或显式栈，有向无环依赖用拓扑排序。多源问题把所有源同时入队，状态带资源的问题不能只按位置去重。 本题的优化应当围绕“按层扩散，分钟数等于最后一个新鲜橘子被首次访问的层数。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 访问标记通常在入队或入栈时设置，保证一个状态只被加入一次。BFS 队列按距离递增；拓扑排序队列保存当前入度为零的节点。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。BFS 正确性来自层次扩展，第一次到达某状态就是最短距离。连通块算法正确性来自一次搜索会标记与起点可达的全部节点且不会越过非法边。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 图节点连续时用 \code{std::vector<std::vector<int>>}；节点是字符串时先编号或用哈希表。网格方向数组用 \code{constexpr std::array}，边界判断集中写。本题还要特别注意：没有新鲜橘子、没有腐烂源、隔离新鲜橘子、空格阻断。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：没有新鲜橘子、没有腐烂源、隔离新鲜橘子、空格阻断。多源 BFS 也适用于最近零距离、地图扩散等题。实验包含孤立节点、自环、重边、不连通图、有向环、不可达目标、多源同层竞争、网格边界和障碍物。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{最短路与带权图}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：最短路与带权图推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 路径数量可能指数级。最短路算法的核心是用当前已知最小代价组织扩展顺序，而不是盲目枚举路径。\\
结构选择 & 非负权图用 Dijkstra，小顶堆保存当前距离最小的候选；边权为一用 BFS；有负边要考虑 Bellman-Ford 或重新建模。路径代价若是最大边权，也可用 Dijkstra 或二分答案。\\
不变量 & Dijkstra 的不变量是：每次弹出的未过期节点，其距离已经是最终最短距离。过期堆元素必须跳过，否则会重复扩展旧状态。\\
代码落地 & 由于 \code{priority_queue} 没有 decrease-key，更新距离时压入新状态，弹出时比较距离数组。距离用 \code{long long} 更稳，图用邻接表保存 \code{(to, weight)}。\\
\end{tabular}
\end{principlebox}

\subsection{743 网络延迟时间}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。有向正权图从起点到所有节点的最短路最大值。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“最短路与带权图”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法可能枚举所有路径，或者把带权边错误当成普通 BFS。只要边权不全相等，按步数最少就不等于总代价最小。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。路径数量可能指数级。最短路算法的核心是用当前已知最小代价组织扩展顺序，而不是盲目枚举路径。 放到本题里，要重点观察“Dijkstra 求每个节点最短距离，最后取最大，若有无穷则不可达。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。非负权图用 Dijkstra，小顶堆保存当前距离最小的候选；边权为一用 BFS；有负边要考虑 Bellman-Ford 或重新建模。路径代价若是最大边权，也可用 Dijkstra 或二分答案。 本题的优化应当围绕“Dijkstra 求每个节点最短距离，最后取最大，若有无穷则不可达。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} Dijkstra 的不变量是：每次弹出的未过期节点，其距离已经是最终最短距离。过期堆元素必须跳过，否则会重复扩展旧状态。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。非负边保证从当前最小距离节点再走出去不会得到更短路径回头改写它。二分可达性则要证明阈值越大，可用边只会增加。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 由于 \code{priority_queue} 没有 decrease-key，更新距离时压入新状态，弹出时比较距离数组。距离用 \code{long long} 更稳，图用邻接表保存 \code{(to, weight)}。本题还要特别注意：节点编号从一开始、不可达、重边、过期堆元素。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：节点编号从一开始、不可达、重边、过期堆元素。边权相同才可用普通 BFS。实验包含不可达节点、零权边、多条等长路径、旧堆状态、起点到自己、网格代价不是求和而是取最大值等情形。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{1631 最小体力消耗路径}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。路径代价是沿途最大高度差，不是边权和。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“最短路与带权图”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法可能枚举所有路径，或者把带权边错误当成普通 BFS。只要边权不全相等，按步数最少就不等于总代价最小。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。路径数量可能指数级。最短路算法的核心是用当前已知最小代价组织扩展顺序，而不是盲目枚举路径。 放到本题里，要重点观察“Dijkstra 的松弛改成取当前代价和新边代价的最大值。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。非负权图用 Dijkstra，小顶堆保存当前距离最小的候选；边权为一用 BFS；有负边要考虑 Bellman-Ford 或重新建模。路径代价若是最大边权，也可用 Dijkstra 或二分答案。 本题的优化应当围绕“Dijkstra 的松弛改成取当前代价和新边代价的最大值。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} Dijkstra 的不变量是：每次弹出的未过期节点，其距离已经是最终最短距离。过期堆元素必须跳过，否则会重复扩展旧状态。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。非负边保证从当前最小距离节点再走出去不会得到更短路径回头改写它。二分可达性则要证明阈值越大，可用边只会增加。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 由于 \code{priority_queue} 没有 decrease-key，更新距离时压入新状态，弹出时比较距离数组。距离用 \code{long long} 更稳，图用邻接表保存 \code{(to, weight)}。本题还要特别注意：单格、平坦网格、必须绕路、多个相同代价。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：单格、平坦网格、必须绕路、多个相同代价。也可二分体力上限后 BFS 检查可达。实验包含不可达节点、零权边、多条等长路径、旧堆状态、起点到自己、网格代价不是求和而是取最大值等情形。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{并查集与动态连通性}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：并查集与动态连通性推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 瓶颈是连通性查询和合并交替出现。若只需要知道是否同组，不需要路径本身，并查集正好匹配。\\
结构选择 & 路径压缩把查找路径上的节点直接挂到根，按大小或按秩合并控制树高。邮箱、等式、边、网格都可以映射成元素集合。\\
不变量 & 每个集合有一个代表元，\code{parent[root] == root}。合并只改变一个根的父亲，非根节点的父亲可能不是最终根，但 \code{find} 会返回正确代表。\\
代码落地 & 类成员访问使用 \code{this->}。迭代版 \code{find} 可以避免递归。若需要维护集合大小、边数、权值差或奇偶关系，要扩展额外数组并在合并时同步更新。\\
\end{tabular}
\end{principlebox}

\subsection{547 省份数量}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。城市连接关系形成无向图，省份就是连通分量。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“并查集与动态连通性”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法每加一条边就重新 DFS 判断连通，或每次查询都扫描整个分量。这在动态合并场景下会重复处理大量已经稳定的连接关系。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。瓶颈是连通性查询和合并交替出现。若只需要知道是否同组，不需要路径本身，并查集正好匹配。 放到本题里，要重点观察“并查集合并所有直接连接的城市，最后统计根数量。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。路径压缩把查找路径上的节点直接挂到根，按大小或按秩合并控制树高。邮箱、等式、边、网格都可以映射成元素集合。 本题的优化应当围绕“并查集合并所有直接连接的城市，最后统计根数量。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 每个集合有一个代表元，\code{parent[root] == root}。合并只改变一个根的父亲，非根节点的父亲可能不是最终根，但 \code{find} 会返回正确代表。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性来自等价关系维护：自反、对称、传递由集合代表保证。合并两个代表后，两个集合中任意元素的代表都会归到同一根。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 类成员访问使用 \code{this->}。迭代版 \code{find} 可以避免递归。若需要维护集合大小、边数、权值差或奇偶关系，要扩展额外数组并在合并时同步更新。本题还要特别注意：邻接矩阵对称、自连接、孤立城市、只扫描上三角。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：邻接矩阵对称、自连接、孤立城市、只扫描上三角。BFS 也可做；并查集更突出动态合并。实验包括重复合并、已经连通的边、孤立节点、字符串编号映射、带权比例冲突、按大小合并前后树高变化。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{684 冗余连接}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。树加一条边会形成唯一环。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“并查集与动态连通性”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法每加一条边就重新 DFS 判断连通，或每次查询都扫描整个分量。这在动态合并场景下会重复处理大量已经稳定的连接关系。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。瓶颈是连通性查询和合并交替出现。若只需要知道是否同组，不需要路径本身，并查集正好匹配。 放到本题里，要重点观察“按顺序加边，若一条边两端已连通，则它就是冗余边。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。路径压缩把查找路径上的节点直接挂到根，按大小或按秩合并控制树高。邮箱、等式、边、网格都可以映射成元素集合。 本题的优化应当围绕“按顺序加边，若一条边两端已连通，则它就是冗余边。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 每个集合有一个代表元，\code{parent[root] == root}。合并只改变一个根的父亲，非根节点的父亲可能不是最终根，但 \code{find} 会返回正确代表。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性来自等价关系维护：自反、对称、传递由集合代表保证。合并两个代表后，两个集合中任意元素的代表都会归到同一根。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} 类成员访问使用 \code{this->}。迭代版 \code{find} 可以避免递归。若需要维护集合大小、边数、权值差或奇偶关系，要扩展额外数组并在合并时同步更新。本题还要特别注意：节点编号、最后一条成环边、重复边、并查集初始化大小。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：节点编号、最后一条成环边、重复边、并查集初始化大小。有向冗余连接需要同时处理入度为二和环，复杂度更高。实验包括重复合并、已经连通的边、孤立节点、字符串编号映射、带权比例冲突、按大小合并前后树高变化。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。
