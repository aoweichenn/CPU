\section{array-window 深度题卡}

\subsection{数组与原地维护}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：数组与原地维护推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。\\
结构选择 & 优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。\\
不变量 & 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。\\
代码落地 & C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。\\
\end{tabular}
\end{principlebox}

\subsection{5 最长回文子串}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。回文围绕中心对称，中心可以是字符也可以是两个字符之间。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“中心扩展避免枚举子串后再逐个检查；每次扩展只比较新增左右边界。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“中心扩展避免枚举子串后再逐个检查；每次扩展只比较新增左右边界。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：偶数长度回文、单字符、全相同字符、多个同长答案。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：偶数长度回文、单字符、全相同字符、多个同长答案。若要求回文子序列，应转成区间 DP，不能用中心扩展。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{26 删除有序数组重复项}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。有序数组让重复元素相邻，慢指针表示下一个唯一值写入位置。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“快指针扫描，只有发现新值才写入慢指针并推进。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“快指针扫描，只有发现新值才写入慢指针并推进。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：空数组、全重复、无重复、返回长度不等于物理容量。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空数组、全重复、无重复、返回长度不等于物理容量。若允许每个元素最多保留两次，只需改变写入条件为和前两个比较。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{27 移除元素}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。原地过滤，慢指针左侧始终是不等于目标值的稳定前缀。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“保持顺序用快慢指针；不保持顺序可用左右交换减少写入。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“保持顺序用快慢指针；不保持顺序可用左右交换减少写入。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：所有元素都被移除、没有元素被移除、目标值在尾部密集。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：所有元素都被移除、没有元素被移除、目标值在尾部密集。工程里要先确认是否要求稳定顺序，不同要求对应不同写法。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{31 下一个排列}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。字典序结构由最长非递增后缀决定，枢轴是后缀前一个位置。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“从右找第一个上升点，再从右找刚好更大的元素交换，最后反转后缀。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“从右找第一个上升点，再从右找刚好更大的元素交换，最后反转后缀。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：完全降序、重复数字、只有一个元素、交换后后缀必须最小化。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：完全降序、重复数字、只有一个元素、交换后后缀必须最小化。若要求上一个排列，比较方向和后缀处理对称变化。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{54 螺旋矩阵}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。四个边界收缩模拟一圈一圈访问。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“每走完一条边就收缩对应边界，并在走反方向前检查是否仍合法。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“每走完一条边就收缩对应边界，并在走反方向前检查是否仍合法。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：单行、单列、空矩阵、最后一圈只剩一个元素。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：单行、单列、空矩阵、最后一圈只剩一个元素。若改成生成矩阵，边界逻辑相同，只是读变成写。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{75 颜色分类}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。三指针维护零区、一号区、未知区和二区。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“遇到二时只移动右边界，不移动扫描指针，因为换回来的元素未知。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“遇到二时只移动右边界，不移动扫描指针，因为换回来的元素未知。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：全零、全二、已经有序、逆序、交换后漏检查。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全零、全二、已经有序、逆序、交换后漏检查。这是荷兰国旗问题，能推广到三类分区的原地维护。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{88 合并两个有序数组}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。第一个数组尾部有空位，适合从后往前写入最终最大值。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“逆向填充避免覆盖尚未读取的 nums1 元素。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“逆向填充避免覆盖尚未读取的 nums1 元素。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：第二个数组为空、第一个有效元素为空、重复值、尾部剩余。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：第二个数组为空、第一个有效元素为空、重复值、尾部剩余。若没有额外空位，只能新开数组或使用更复杂原地归并。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{189 轮转数组}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。右移 k 位等价于把数组切成两段后换序。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“三次反转能原地完成：整体、前 k、后 n 减 k。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“三次反转能原地完成：整体、前 k、后 n 减 k。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：k 为零、k 大于 n、空数组、单元素。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：k 为零、k 大于 n、空数组、单元素。环状替换也可做，但要处理最大公约数个循环。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{238 除自身以外数组的乘积}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。不能用除法时，答案由左侧乘积和右侧乘积组成。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“先写入左侧前缀积，再从右向左乘右侧后缀积。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“先写入左侧前缀积，再从右向左乘右侧后缀积。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：一个零、多个零、负数、乘积溢出。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：一个零、多个零、负数、乘积溢出。这个题展示输出数组可作为状态存储的一部分。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{283 移动零}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。稳定保留非零元素顺序，把零集中到尾部。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“慢指针写入下一个非零位置，最后补零或用交换。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“慢指针写入下一个非零位置，最后补零或用交换。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：全零、无零、零在开头结尾、稳定顺序要求。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全零、无零、零在开头结尾、稳定顺序要求。若目标是移动指定值，模型与移除元素相同。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{581 最短无序连续子数组}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。要找破坏全局有序性的最左和最右位置。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“从左维护历史最大值确定右边界，从右维护历史最小值确定左边界。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“从左维护历史最大值确定右边界，从右维护历史最小值确定左边界。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：已经有序、逆序、重复值、局部乱序。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：已经有序、逆序、重复值、局部乱序。排序比较法更直观但复杂度更高。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{647 回文子串}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。每个回文都有唯一中心或中心间隙。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“数组与原地维护”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法通常枚举每个位置的最终去向，或者为每个位置重新扫描一段区间。它容易写出正确答案，却没有利用数组的连续存储、下标可随机访问、前后缀可复用这些条件。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。慢点在于重复搬移和重复求同一段信息。只要一个位置的状态已经由前缀、后缀、慢指针或边界指针确定，再回头扫描就是浪费。 放到本题里，要重点观察“对每个中心向两边扩展，扩展成功一次就计数一次。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。优化要把数组切成若干语义明确的区间：已经处理好的前缀、未知区、已经确定的后缀，或者左侧贡献和右侧贡献。双指针、前后缀数组、原地交换和稳定写入，本质都是让每个元素只进入少数几次状态转移。 本题的优化应当围绕“对每个中心向两边扩展，扩展成功一次就计数一次。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 不变量必须能落到下标上：慢指针左侧保存什么，右指针右侧保存什么，当前下标之前是否已经满足题目要求。只要这个叙述含糊，代码中的加一减一就会变成猜。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。正确性通常用区间不变量证明。每次循环只扩大已经正确的区域，未知区严格缩小，且被移出的元素已经放到符合语义的位置。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 实现优先使用 \code{std::vector<int>} 和明确的整型下标。涉及乘积、面积、总和时要主动判断是否需要 \code{long long}。如果题目要求原地修改，要说明返回值表示新长度还是数组本身，避免把输出语义和容器容量混在一起。本题还要特别注意：空串、全相同字符、偶数回文、奇数回文。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空串、全相同字符、偶数回文、奇数回文。Manacher 算法可线性解决，但中心扩展更适合面试基础。实验时准备空数组、单元素、全相同、严格递增、严格递减、包含负数和极值的用例。把每轮指针位置打印出来，比只看最终答案更容易发现边界错误。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{滑动窗口与双指针}
下面的题卡都按同一条链路展开：从问题出发，先写暴力，再定位重复工作，然后选择数据结构或状态不变量，最后落到 C++ 实现、边界和扩展。格式统一是为了训练面试表达，而每张卡的关键观察和失效条件都要单独复盘。

\begin{principlebox}{图示：滑动窗口与双指针推导链}
\begin{tabular}{@{}p{0.18\linewidth}p{0.74\linewidth}@{}}
暴力基线 & 枚举候选、完整模拟或逐个检查，先保证覆盖全部可能答案。\\
瓶颈定位 & 真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。\\
结构选择 & 滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。\\
不变量 & 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。\\
代码落地 & C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。\\
\end{tabular}
\end{principlebox}

\subsection{3 无重复字符的最长子串}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。窗口保存当前无重复子串，右端每次加入一个字符。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“用上次出现位置直接跳过无效左端，左边界只能向右不能回退。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“用上次出现位置直接跳过无效左端，左边界只能向右不能回退。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：空串、全相同字符、重复字符出现在窗口左侧之外、扩展字符集。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：空串、全相同字符、重复字符出现在窗口左侧之外、扩展字符集。若要求恰好包含 k 种字符，窗口条件从无重复变成种类计数。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{11 盛最多水的容器}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。左右指针代表当前选取的两条边，面积由短板和宽度共同决定。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“每次移动短板，因为移动长板只会缩小宽度且不会提高短板。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“每次移动短板，因为移动长板只会缩小宽度且不会提高短板。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：两端高度相等、数组长度为二、面积乘法可能溢出。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：两端高度相等、数组长度为二、面积乘法可能溢出。接雨水计算每个位置贡献，不能把本题双指针证明直接搬过去。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{15 三数之和}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。排序后固定第一个数，剩余两数转成有序双指针。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“和太小移动左指针，和太大移动右指针；固定值和左右值都要跳过重复。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“和太小移动左指针，和太大移动右指针；固定值和左右值都要跳过重复。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：全零、重复元素很多、没有答案、结果顺序不要求但组合不能重复。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：全零、重复元素很多、没有答案、结果顺序不要求但组合不能重复。四数之和再外层固定一个数，并用 long long 防止目标和溢出。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{16 最接近的三数之和}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。排序后固定一数，双指针寻找最接近目标的两数和。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“每次根据当前和与目标的大小移动指针，同时更新最小绝对差。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“每次根据当前和与目标的大小移动指针，同时更新最小绝对差。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：差值相等、负数、目标很大、三数和溢出。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：差值相等、负数、目标很大、三数和溢出。若要求返回所有最接近组合，需要记录同差值并去重。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{18 四数之和}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。两层固定加一层双指针，关键是剪枝和去重。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“排序提供单调性，外层可用最小可能和和最大可能和提前跳过。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“排序提供单调性，外层可用最小可能和和最大可能和提前跳过。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：重复元素、目标超出 int、答案很多导致输出空间主导复杂度。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：重复元素、目标超出 int、答案很多导致输出空间主导复杂度。k 数之和可以递归降维，但工程实现要控制重复和边界。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{76 最小覆盖子串}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。窗口内字符计数必须覆盖目标计数，满足后尽量收缩左端。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“用 formed 记录满足需求的字符种类，避免每次全量比较。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“用 formed 记录满足需求的字符种类，避免每次全量比较。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：目标有重复字符、源串缺字符、最小窗口在开头或结尾、大小写。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：目标有重复字符、源串缺字符、最小窗口在开头或结尾、大小写。若字符集固定，数组计数更快；若是 Unicode，哈希表更通用。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{209 长度最小的子数组}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。正数数组让窗口和随右端增加、随左端减少。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“右端扩展到满足目标后，不断左移缩短并更新答案。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“右端扩展到满足目标后，不断左移缩短并更新答案。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：不存在答案、单元素达到目标、全正是必要条件。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：不存在答案、单元素达到目标、全正是必要条件。若含负数，需要前缀和加单调队列。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。

\subsection{438 找到字符串中所有字母异位词}

\textbf{从问题出发。} 先不要问“这题套什么模板”，而要把题面翻译成对象、关系、约束和输出目标。固定长度窗口内字符频次等于目标频次即为答案。对象决定我们要保存数组下标、节点、区间、字符还是图状态；关系决定候选之间能不能比较、合并或淘汰；输出目标决定最后保存的是最值、计数、路径、集合还是判定结果。把这些拆开以后，才进入“滑动窗口与双指针”这条主线。

\textbf{先把暴力写清楚。} 按上面的模型，最直接的办法是完整枚举可能答案或完整模拟题目过程，再对每个候选重新检查是否合法。暴力做法枚举所有左端和右端，再检查窗口是否合法。对于字符串和正数数组，这种写法会把相邻窗口中大量相同内容反复计算。 写暴力不是为了提交它，而是为了暴露状态空间：哪些候选一定会出现，哪些判断被反复做，哪些中间量其实只和少量历史状态有关。只有这一步说清楚，后面的优化才不是凭空跳出来的答案。

\textbf{瓶颈定位。} 暴力版本跑慢，通常不是因为代码写得不够巧，而是因为同一类信息被一次次重新取得。真正的瓶颈不是两层循环本身，而是窗口状态没有被增量维护。右端移动带来的变化只有一个新元素，左端移动移走的也只有一个旧元素，若每次重新统计，就丢掉了题目给的连续性。 放到本题里，要重点观察“右进左出维护计数，窗口长度达到目标长度后检查差异。”这条线索：它说明有一部分信息可以从已经处理过的输入中继承下来，而不需要回头重算。这里的关键观察不是结论，而是从暴力过程里提炼出来的重复工作。

\textbf{结构选择和优化。} 既然瓶颈已经定位，下一步才是选择结构。滑动窗口成立必须有单调性或固定长度边界。右端扩展让某个条件单调变好或变坏，左端收缩可以恢复合法性或缩短答案。若数组含负数、状态会来回震荡，就不能硬套窗口。 本题的优化应当围绕“右进左出维护计数，窗口长度达到目标长度后检查差异。”建立一个可维护状态：新增输入只更新一次，过期候选及时删除，已经稳定的子答案被合并使用。优化后的循环不应重新审查全部输入，而应只处理这一步真正变化的那一小块状态。若题目条件发生变化，例如有序性、正数条件、字符集、边权或是否允许修改输入发生改变，就必须重新验证这个结构是否仍然成立。

\textbf{不变量和证明。} 窗口不变量要写成当前窗口满足什么条件，或者当前窗口是第一个可能满足条件的最短前缀。每次移动指针之前先更新状态，移动之后状态要和区间边界一致。 用这道题复盘时，把不变量写成三句话：已经处理的部分满足什么，尚未处理的部分还保留什么可能性，当前操作为什么不会丢掉最优答案或合法答案。证明重点是排除。某个左端被移过之后，所有以它为左端的更长窗口都不可能比当前答案更优，或者已经被当前状态完整统计。 证明不需要很形式化，但必须能排除反例；如果只能说“这样写好像可以”，说明你还没有把优化从暴力中推导出来。

\textbf{代码落地。} C++ 中建议把窗口写成左闭右开或左闭右闭中的一种，并在全题坚持。字符计数用 \code{std::array<int, 128>} 或 26 长度数组时要说明字符集假设；泛化字符集再换哈希表。本题还要特别注意：目标比源串长、重复字符、字符集固定、答案重叠。写代码前先把边界条件放进函数入口、循环条件或状态定义里，而不是写完主体后再用补丁式分支修样例。容器选择要和访问模式一致：需要顺序就别用无序表，需要极值就别完整排序，需要历史计数就别只保存布尔值。

\textbf{复杂度、边界和实验。} 时间复杂度要按真实输入维度说明，不能把字符串长度、图边数、值域大小都混成一个 n。空间复杂度也要区分辅助结构和输出本身。测试时至少覆盖：最小输入、没有答案、答案在边界、重复元素或重复状态、以及会让暴力解最慢的规模输入。对于这题，边界复盘重点是：目标比源串长、重复字符、字符集固定、答案重叠。维护差异计数可以避免每次比较整个数组。实验应包含重复字符集中、目标字符缺失、窗口刚好覆盖、窗口必须连续收缩、多次同字符需求等样例。若把负数放进正数窗口题，应能解释为什么算法不再适用。实验报告建议记录三件事：暴力版本在什么规模开始变慢，优化版本减少了哪类重复工作，哪一个边界用例最容易打破错误实现。
