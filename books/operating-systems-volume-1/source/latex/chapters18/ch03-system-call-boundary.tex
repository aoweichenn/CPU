\chapter{内核怎样建立受控入口：从异常帧到系统调用}

\begin{statebox}
\textbf{项目里程碑 v0.5。} ELF64 内核已经获得控制，但 Stage 1 的描述符状态只是临时
启动环境。内核先建立自己的 GDT、TSS、IDT 和 160 字节统一异常帧，使 INT3 能恢复、
其他故障能留下 panic 现场。已完成的 v0.8 增加 Ring 3 段、TSS 的 RSP0 栈和
DPL3 的 \code{INT 0x80} 门，并定义写日志与退出的最小 ABI。整机验证已经覆盖
非法用户指针、未知调用号、正常退出、用户态非法指令和用户态缺页，并证明内核能够
收束用户执行后继续运行。项目仍只有单个同步用户执行单元；深入正文继续说明入口帧
之外为什么还需要参数、稳定引用、并发页生命周期与权限边界。
\end{statebox}

\inputchapterbody{chapters/ch09-system-calls-permissions}

\section{入口契约的实现证据}
\inputdetail{chapters/ch09-stage-three-boundary-implementation}
\inputdetail{chapters/ch00-stage-one-syscall-source-walk}

\begin{problemframe}
\textbf{尚未解决的问题。} 内核已经能规范化异常现场，并知道未来系统调用怎样验证参数
和对象引用；它仍只有启动器留下的固定内存布局。下一章回到项目 v0.6，把物理内存图变成
页帧所有权、内核页表和具有明确失败语义的分配器。
\end{problemframe}
