\chapter{为什么设备请求需要公共块层与文件接口}

\begin{statebox}
\textbf{继承的机器。} 驱动已经有请求身份、DMA 所有权、完成与复位边界。上层仍不能把
任意大小的文件区间直接变成设备命令，也没有稳定对象表达“某进程以何种方式打开了哪个
持久对象、当前偏移是多少、操作应分派给谁”。
\end{statebox}

\inputchapterbody{chapters/ch12-block-io-files}

\section{从文件对象到持久块的综合验证}
\inputdetail{chapters/ch16-io-filesystem-deep-dive}

\begin{problemframe}
\textbf{尚未解决的问题。} 块层能够提交有顺序约束的请求，VFS 能把 fd 分派到具体实现，
但一片可寻址块仍没有空闲记录、文件身份、名称映射或重新挂载入口。下一章从这些最小持久
事实开始形成文件系统。
\end{problemframe}
