\chapter{为什么共享状态需要同步与通信对象}

\begin{statebox}
\textbf{继承的机器。} 内核已经有多个可抢占 task、受控系统调用入口和可替换程序映像。
只要两个执行流都能修改同一状态，“各自执行正确”就不再保证组合结果正确；只禁止并发又会
让等待设备或事件的任务占住处理器。
\end{statebox}

\inputchapterbody{chapters/ch07-synchronization-ipc-locks}

\section{把同步规则落实为状态机}
\inputdetail{chapters/ch07-stage-two-sync-state-machines}
\inputdetail{chapters/ch07-concurrency-ipc-signals}
\inputdetail{chapters/ch08-kernel-locking-deadlocks}

\begin{problemframe}
\textbf{尚未解决的问题。} 执行与并发边界已经形成，但所有程序仍把地址直接解释为同一
物理位置。下一章从两个程序使用同一数值地址的冲突开始，把地址含义、访问权限和物理归属
分离。
\end{problemframe}
