\section{第一阶段深讲：CPU 对操作系统暴露的契约}

CPU 不是操作系统可以随意想象的执行器。它给软件暴露的是一组精确契约：指令如何改变寄存器，异常在什么边界发生，特权模式如何切换，页表根在哪里，系统调用从哪里进入，返回用户态前哪些状态必须恢复。理解这些契约，比背一张 CPU 结构图更重要。

\subsection{寄存器不是临时变量，而是架构状态}

软件能直接观察和依赖的 CPU 状态称为架构状态。通用寄存器、程序计数器、栈指针、状态寄存器、控制寄存器、浮点和向量寄存器，都属于这个范围。微架构内部的乱序窗口、重命名寄存器、分支预测表通常不可直接观察，但它们会影响性能和安全。

\begin{longtable}{p{0.2\linewidth}p{0.34\linewidth}p{0.34\linewidth}}
\toprule
状态 & 对普通程序的意义 & 对 OS 的意义 \\
\midrule
RIP & 下一条或当前指令位置 & 异常返回、信号、调试、上下文切换 \\
RSP & 当前栈顶 & 用户栈、内核栈、ABI 对齐、栈溢出 \\
通用寄存器 & 参数、返回值、临时计算 & syscall ABI、trap frame、调度保存恢复 \\
RFLAGS & 条件码、中断使能、模式位 & 中断开关、返回用户态、单步调试 \\
页表根寄存器 & 当前地址空间 & 进程切换、内核映射、TLB 语义 \\
向量寄存器 & SIMD/浮点计算 & 信号帧、延迟保存、上下文切换成本 \\
\bottomrule
\end{longtable}

一条用户指令被中断打断后，程序将来还要像没发生过中断一样继续运行。为此，内核不能只保存 RIP，还要保存足够恢复用户可见状态的寄存器。x86-64 ABI 和入口代码共同定义保存集合；凡是用户态能依赖的状态，内核要么保存恢复，要么在 ABI 中明确它会被破坏。

\subsection{指令执行不是一句“取指执行”}

最小模型可以写成取指、译码、执行、访存、写回。真实 CPU 可能乱序、预测和并行，但最终必须提交出与 ISA 一致的结果。OS 关心的是每条指令在异常和中断面前的边界。

\begin{lstlisting}
instruction retirement view:
  fetch bytes from instruction address
  decode according to ISA
  read architectural operands
  execute operation or memory access
  if fault, report precise exception
  otherwise commit architectural result
\end{lstlisting}

“提交”这个概念很关键。乱序 CPU 可以提前执行后面的指令，但不能让用户态看到不符合顺序语义的架构状态。若一条 load 触发 page fault，内核必须知道 faulting instruction 是哪一条，之前的指令已经生效，之后的指令不能对架构状态产生不可回滚影响。否则 page fault handler 无法修复状态并重试。

\subsection{精确异常是虚拟内存的地基}

精确异常的意思是，异常发生时 CPU 能给出一个像顺序执行一样的状态点：异常指令之前的指令已经完成，异常指令和之后的指令没有提交最终架构效果。缺页、除零、非法指令、断点、系统调用，都依赖这个性质。

\begin{lstlisting}
if instruction at RIP faults:
  saved_rip = RIP of faulting instruction
  saved_error = reason and access type
  architectural state corresponds to before this instruction commits
  kernel may fix the cause and return to saved_rip
\end{lstlisting}

没有精确异常，COW 就很难实现。写 COW 页的 store 必须在真正写入共享物理页之前停下来，让内核复制新页并更新 PTE，再回到同一条 store。若 store 已经部分写入旧页，父子隔离就被破坏。硬件异常语义直接决定 OS 能实现哪些内存抽象。

\subsection{fault、trap、interrupt 的差别}

中文里常把异常、中断、陷入混着说，但写内核时必须分清事件来源和返回语义。

\begin{longtable}{p{0.16\linewidth}p{0.28\linewidth}p{0.28\linewidth}p{0.18\linewidth}}
\toprule
类型 & 来源 & 典型例子 & 返回位置 \\
\midrule
fault & 当前指令无法完成 & page fault、保护错误 & 常返回同一条指令重试 \\
trap & 当前指令主动进入内核或调试 & syscall、breakpoint & 常返回下一条指令 \\
interrupt & 外部异步事件 & timer、device IRQ、IPI & 返回被打断位置 \\
abort & 严重硬件或不可恢复错误 & machine check、总线错误 & 可能无法安全返回 \\
\bottomrule
\end{longtable}

这个分类影响内核入口保存什么状态。fault 要保留足够信息让指令重试；syscall 要按 ABI 把返回值写回指定寄存器；interrupt 要避免破坏被打断上下文；严重硬件错误可能只能记录日志、隔离页或 panic。

\subsection{用户栈和内核栈为什么要分开}

用户栈由用户程序控制，可能被破坏、未映射、接近栈底，甚至故意指向恶意地址。内核进入特权态后不能继续信任用户栈，否则中断帧、返回地址和内核局部变量都会落在不可信内存里。因此系统调用、中断和异常入口通常切换到内核栈。

\begin{lstlisting}
on privilege transition:
  CPU or entry code chooses kernel stack for current task
  save user RIP, RSP, RFLAGS and selected registers
  run kernel handler on trusted stack
  restore or validate state before returning to user mode
\end{lstlisting}

每个线程需要自己的内核栈，因为线程可能在系统调用中阻塞。若所有线程共享一个内核栈，线程 A 阻塞在内核里后，线程 B 进入内核就会覆盖 A 的调用链。内核栈大小有限，所以内核代码不能随意放巨大局部数组，也不能无限递归。

\subsection{系统调用入口是一段极谨慎的汇编}

系统调用入口处，C 运行环境还不完整。入口代码要处理用户寄存器、切换栈、建立内核可用的调用约定、屏蔽或允许中断、检查系统调用号、进入 C 函数分发。返回路径同样危险：返回用户态前要处理信号、调度、审计、单步调试和可能的抢占。

\begin{lstlisting}
syscall entry outline:
  save user register frame
  switch to trusted kernel stack if needed
  sanitize user-provided state
  dispatch syscall table entry
  store return value in ABI register
  check pending signal or reschedule flag
  restore user state
  return to user mode
\end{lstlisting}

这里不能有“差不多”。若入口漏保存一个用户可见寄存器，程序会随机坏；若没有清理某些特权状态，用户态可能继承内核状态；若返回路径错误恢复 RFLAGS，用户程序可能以错误中断状态或错误单步状态运行。

\subsection{上下文切换不是只保存寄存器}

最浅的说法是“上下文切换保存现场”。更准确地说，它要切换当前 CPU 正在代表哪个任务执行。这个切换至少涉及调度实体、内核栈、寄存器保存区、地址空间、抢占计数、FPU 状态和 CPU 本地 current 指针。

\begin{lstlisting}
switch from prev to next:
  prev.thread.rsp = current RSP
  save callee-saved registers into prev thread context
  current = next
  load next address-space root if mm changes
  switch RSP to next.thread.rsp
  restore next registers
  return as if next's old schedule call just finished
\end{lstlisting}

注意最后一句：上下文切换不是“跳到 next 的 main 函数”。next 之前可能正在系统调用中睡眠，也可能刚被定时器抢占。恢复后，它从自己上次离开 CPU 的内核位置继续运行，最终回到用户态或继续内核逻辑。

\subsection{FPU 和 SIMD 为什么让切换变贵}

现代向量寄存器很大。若每次切换都保存所有向量状态，成本高；若延迟保存，又要处理任务第一次使用 FPU/SIMD 时的异常、跨任务泄漏和安全补丁。许多系统在性能和安全之间选择 eager 策略，即每次切换都明确保存恢复用户可见向量状态。

\begin{longtable}{p{0.18\linewidth}p{0.36\linewidth}p{0.34\linewidth}}
\toprule
策略 & 好处 & 风险或成本 \\
\midrule
lazy save & 不使用 FPU 的任务不付成本 & 状态管理复杂，历史上暴露过安全问题 \\
eager save & 语义清晰，隔离更直接 & 每次切换固定成本增加 \\
按需扩展状态 & 只为使用的扩展保存空间 & 需要精确追踪任务使用的 CPU 特性 \\
\bottomrule
\end{longtable}

这解释了为什么 CPU 新增向量扩展不只是编译器问题。OS 要知道如何启用、保存、恢复、暴露给用户、在信号帧中表示，并在不支持时给出正确非法指令异常。

\subsection{控制寄存器把硬件策略交给内核}

页表根、向量表基址、系统调用入口、定时器配置、中断屏蔽、cache 策略、性能计数器控制，这些状态通常只能在内核或更高特权层修改。它们让 OS 能建立策略，也让错误配置变得危险。

\begin{lstlisting}
privileged configuration examples:
  set page table root for current address space
  install exception vector table
  configure syscall entry point
  program local timer deadline
  enable or disable selected interrupts
  configure performance monitoring counters
\end{lstlisting}

普通用户程序不能直接改这些寄存器。若能改页表根，它可以访问任意内存；若能改异常向量，它可以劫持系统调用；若能关闭定时器中断，它可以阻止抢占。特权级的价值，就体现在这些寄存器的写权限被硬件强制限制。

\subsection{x86-64 特权状态逐项拆开}

很多读者卡在这里，是因为书上只说“控制寄存器”“中断表”“系统调用入口”，却没有说这些名字各自保存什么。下面这张表把本册反复出现的 x86-64 特权状态拆开。它们不是一堆需要背的缩写，而是内核启动、系统调用、缺页、中断和调度必须配置的硬件根。

\begin{longtable}{p{0.22\linewidth}p{0.30\linewidth}p{0.36\linewidth}}
\toprule
硬件状态 & 保存什么 & OS 依赖它做什么 \\
\midrule
CR0/CR4/EFER & 分页、保护、长模式和扩展控制位 & 打开 long mode、启用分页、启用 PCID 等能力 \\
CR2 & 最近一次 page fault 的线性地址 & page fault handler 查 VMA/PTE 的起点 \\
CR3 & 顶级页表物理地址和 PCID 相关位 & 切换地址空间，建立当前 CPU 的翻译根 \\
RFLAGS.IF & 当前 CPU 是否接受可屏蔽中断 & 临界入口、抢占、中断返回语义 \\
IDTR/IDT & vector 到 gate/handler 的表 & page fault、timer IRQ、IPI、device IRQ 分发 \\
GDTR/GDT/TSS & 段描述、TSS、RSP0、IST & 从 Ring 3 进 Ring 0 时选择可信栈 \\
\code{IA32\_LSTAR} & \code{syscall} 的 64 位入口 RIP & 用户态只能跳到内核指定入口 \\
\code{IA32\_STAR}/\code{IA32\_FMASK} & syscall 段选择和 RFLAGS mask & 进入内核时约束返回状态和标志位 \\
\code{IA32\_KERNEL\_GS\_BASE} & 内核 GS base 备用值 & \code{swapgs} 后找到 per-CPU/current 数据 \\
local APIC registers & 本地定时器、IPI、EOI、pending 状态 & 抢占 tick、跨 CPU 调度、TLB shootdown \\
\bottomrule
\end{longtable}

把这些状态放在一起看，系统调用就不再神秘。用户执行 \code{syscall} 时，CPU 用 MSR 找入口；入口代码用 \code{swapgs} 找到当前 CPU 的内核数据；用 TSS/per-CPU 数据切到可信内核栈；用 IDT 处理异步中断和异常；用 CR3/PCID 决定当前地址空间；用 RFLAGS 控制返回时的中断和调试状态。少配置任意一项，内核入口都会从“受控边界”变成不可诊断崩溃。

\subsection{CR3、PCID 和地址空间对象不是一回事}

源码里常见 \code{mm\_struct}、页表页、CR3、PCID、TLB 这些词。它们在同一条路径上，但不是同一个对象。\code{mm\_struct} 是内核管理的地址空间对象；页表页是内存里的多级表；CR3 是 CPU 当前使用的顶级页表根；PCID 是 TLB 项的短标签；TLB 是 CPU 内部缓存的翻译结果。

\begin{longtable}{p{0.18\linewidth}p{0.34\linewidth}p{0.36\linewidth}}
\toprule
名字 & 所在位置 & 不能误解成什么 \\
\midrule
\code{mm\_struct} & 内核内存 & 不是硬件直接读取的寄存器 \\
PML4/页表页 & 物理内存 & 不是进程的全部内存，只是翻译结构 \\
CR3 & CPU 控制寄存器 & 不是普通指针，写它会改变当前翻译根 \\
PCID & TLB 标签/CR3 相关位 & 不是 pid，也不是永久身份 \\
TLB entry & CPU 内部缓存 & 不是页表本体，PTE 改了它不会自动消失 \\
\bottomrule
\end{longtable}

切换地址空间时，内核的目标不是“复制进程内存”，而是让当前 CPU 后续的取指和 load/store 使用 next 的翻译根。简化算法如下：

\begin{lstlisting}
switch_address_space(prev_mm, next_mm):
  if prev_mm == next_mm:
    keep CR3 and TLB state
    return

  if next_mm has valid PCID that was not recycled unsafely:
    load CR3 with next_mm.pml4_phys and next_mm.pcid
  else:
    pcid = allocate_or_recycle_pcid()
    invalidate stale TLB entries for pcid
    load CR3 with next_mm.pml4_phys and pcid

  ensure kernel global mappings remain valid
\end{lstlisting}

这里有三个容易出错的点。第一，PCID 只是有限标签，复用前必须处理旧翻译，否则新地址空间可能命中旧地址空间的 TLB 项。第二，PTE 权限收紧后，即使 CR3 没变，也要失效对应 TLB 项。第三，释放物理页前必须确认所有可能持有旧翻译的 CPU 都失效完成，否则旧 TLB 可能写到已经分配给别人的页。

\subsection{IDT、TSS 和 IST：入口为什么需要可信栈}

异常和中断不是普通 \asmcode{call handler}。CPU 收到 vector 后查 IDT，确认 gate 类型、目标特权级和 handler RIP。若事件从 Ring 3 进入 Ring 0，CPU 需要把栈切到内核可信栈；x86-64 用 TSS 中的 RSP0 或 IST 项提供这些栈。IST 专门给 NMI、double fault 等高风险入口使用，避免当前内核栈已经损坏时继续往坏栈上写。

\begin{lstlisting}
event delivery:
  vector = fault_vector or irq_vector
  gate = IDT[vector]
  if CPL changes from Ring 3 to Ring 0:
    RSP = TSS.RSP0 for current CPU
  if gate selects IST:
    RSP = TSS.IST[gate.ist_index]
  push return state and optional error code
  RIP = gate.handler
\end{lstlisting}

\begin{longtable}{p{0.18\linewidth}p{0.30\linewidth}p{0.40\linewidth}}
\toprule
入口 & 关键硬件证据 & 内核第一步要做什么 \\
\midrule
page fault & vector、error code、CR2、RIP & 保存寄存器，查 VMA/PTE，判断能否修复 \\
general protection & vector、error code、RIP & 判断是用户违规还是内核 bug \\
invalid opcode & RIP 和指令字节 & 发送信号、模拟指令或报告内核错误 \\
timer IRQ & APIC vector、被打断 RIP & 记账，设置 need\_resched，必要时调度 \\
IPI & APIC vector、发送方协议 & 执行 reschedule、TLB invalidate 或 stop CPU \\
device IRQ & MSI-X vector、设备状态寄存器 & 读取 completion，确认中断，唤醒等待者 \\
\bottomrule
\end{longtable}

这解释了为什么入口汇编通常先保存通用寄存器，再把 \code{rsp} 作为参数传给 C 函数。C 代码不能假设普通调用约定已经完整建立；它接手的是一份由硬件和汇编共同构造的 trap frame。

\begin{lstlisting}
page_fault_entry sketch:
  swapgs if entering from user mode
  push general purpose registers
  mov rdi, rsp
  call do_page_fault
  restore general purpose registers
  iretq
\end{lstlisting}

\subsection{\code{syscall} 入口到底做了哪些事}

x86-64 的 \code{syscall} 快，但它并不替内核完成所有入口工作。它把用户返回 RIP 放到 \code{rcx}，把用户 RFLAGS 放到 \code{r11}，把 RIP 改为 \code{IA32\_LSTAR} 指定的内核入口，并按 \code{IA32\_FMASK} 清理部分标志位。它不会自动保存所有通用寄存器，也不会自动复制用户参数，更不会自动验证用户指针。

\begin{lstlisting}
; user side, Intel syntax
mov rax, SYS_read
mov rdi, fd
mov rsi, buf
mov rdx, len
syscall
\end{lstlisting}

进入内核后，低级入口必须把硬件给出的最小状态扩展成内核能处理的状态：

\begin{lstlisting}
syscall entry sketch:
  swapgs
  save user RSP into per-CPU or task state
  load trusted kernel RSP
  save rcx as user RIP
  save r11 as user RFLAGS
  save syscall arguments from registers
  call syscall dispatcher
  prepare return value in rax
  handle signal, tracing, audit or reschedule
  return with sysretq or iretq
\end{lstlisting}

\code{sysretq} 和 \code{iretq} 的区别也来自硬件契约。\code{sysretq} 快，但要求返回状态满足更严格条件；\code{iretq} 通用，能恢复更完整的状态，适合信号、调试、异常返回或复杂标志位场景。内核返回路径不是简单 \code{return}，而是在判断“当前保存的用户状态能不能走快速出口”。

\subsection{典型故障：硬件名词错配会怎样崩}

下面这些故障比定义更能说明名词边界。

\begin{longtable}{p{0.24\linewidth}p{0.36\linewidth}p{0.28\linewidth}}
\toprule
错误 & 直接后果 & 可观察证据 \\
\midrule
CR3 指向未映射或损坏页表 & 取指或访存立即 fault，可能递归 fault & 早期 boot 崩溃、page fault 日志异常 \\
TSS.RSP0 错误 & Ring 3 进 Ring 0 时 trap frame 写到坏栈 & double fault、triple fault、无日志重启 \\
IDT gate 未安装 & 对应异常或 IRQ 无入口 & unhandled vector、系统挂死 \\
\code{IA32\_LSTAR} 错误 & \code{syscall} 跳到错误 RIP & 用户一调用系统调用就崩溃 \\
忘记 TLB invalidate & 权限收紧或 unmap 后旧访问仍成功 & 随机内存破坏、安全绕过 \\
中断返回恢复 RFLAGS 错误 & 中断状态或调试状态错乱 & 丢中断、单步调试异常、抢占异常 \\
DMA completion 前释放页 & 设备迟到写破坏新对象 & IOMMU fault、KASAN、随机数据损坏 \\
\bottomrule
\end{longtable}

读源码时不要问“这个字段叫什么”就停住，而要继续问：谁写它，硬件什么时候读它，错误时进入哪个 vector，能否恢复，日志里有什么证据。这样才算把组成原理学成 OS 能用的判断力。

\subsection{源码阅读锚点}

为了避免只停在概念，本节对应的 Linux 源码可从下面入口读。路径会随版本变化，但职责长期稳定。

\begin{longtable}{p{0.34\linewidth}p{0.54\linewidth}}
\toprule
源码入口 & 重点看什么 \\
\midrule
\filepath{arch/x86/entry/entry\_64.S} & syscall、interrupt、exception 入口如何保存和恢复状态 \\
\filepath{arch/x86/kernel/cpu/common.c} & GDT、TSS、CPU 特性和 per-CPU 初始化 \\
\filepath{arch/x86/mm/tlb.c} & PCID、TLB flush、shootdown 和 lazy TLB \\
\filepath{arch/x86/kernel/apic/} & local APIC、IPI、timer、EOI 和中断投递 \\
\filepath{arch/x86/mm/fault.c} & page fault 如何读取 CR2、解析 error code、进入内存管理 \\
\filepath{kernel/sched/core.c} & 调度器如何把 need\_resched 变成上下文切换 \\
\bottomrule
\end{longtable}

\subsection{x86-64 名字与共同原则}

本册不再把多个 ISA 混在一起讲。x86-64 的名字就是本书后续源码和汇编的默认语言：RIP/RSP/RFLAGS 表示执行现场，CR3 表示当前页表根，IDT 表示中断和异常入口，\code{syscall} 表示用户进入内核，Ring 3/Ring 0 表示用户态和内核态。重要的是把这些名字背后的角色讲清：有用户态和特权态，有异常入口，有地址翻译，有中断，有返回用户态的受控路径，有保存恢复上下文的要求。

\begin{longtable}{p{0.24\linewidth}p{0.28\linewidth}p{0.36\linewidth}}
\toprule
问题 & x86-64 名字 & OS 角色 \\
\midrule
用户进入内核 & \code{syscall} & 受控系统调用入口 \\
特权层级 & Ring 3/Ring 0 & 用户态和内核态隔离 \\
页表根 & CR3 & 当前地址空间翻译根 \\
异常入口 & IDT & 中断、异常和 trap 的分发入口 \\
返回用户态 & \code{sysretq}/\code{iretq} & 恢复用户 RIP/RSP/RFLAGS 和 CPL \\
\bottomrule
\end{longtable}

学习 OS 时要把“名字”和“角色”分开。CR3 不是普通变量，它是地址空间切换的硬件根；IDT 不是函数表，它是异常和中断的受控入口；\code{syscall} 不是函数调用，它是跨特权级入口。

\subsection{CPU 契约的最低验收}

读完本节，应该能回答以下问题：为什么 page fault 可以返回同一条指令重试；为什么系统调用不能使用用户栈保存内核状态；为什么上下文切换要考虑地址空间和向量寄存器；为什么 \code{syscall}、IDT、TSS、CR3、PCID 和 APIC 是同一条内核入口链上的不同硬件支点；为什么特权寄存器必须由硬件限制写入。

如果这些问题能回答清楚，进程、线程、系统调用和虚拟内存就不再是孤立概念，而是 CPU 契约自然推出的软件结构。
