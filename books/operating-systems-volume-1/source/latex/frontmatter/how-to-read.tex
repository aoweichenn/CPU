\chapter*{怎样阅读本册}
\addcontentsline{toc}{chapter}{怎样阅读本册}

本册按照“对象、状态转换、边界与证据”的顺序组织，而不是把术语并列罗列。开篇先用一次文件保存请求建立操作系统全景，随后进入进程、调度、系统调用、虚拟内存、设备、文件系统、网络、安全与恢复。涉及具体体系结构时统一采用 x86-64；汇编统一采用 Intel 语法，目的操作数在前，源操作数在后，例如 \asmcode{mov rax, qword ptr [rbx]}、\asmcode{add rax, 1}、\code{syscall}、\code{iretq}。

第一部分回答“操作系统如何把请求和事件组织成长期对象”。从系统全景出发，依次讨论 task、系统调用边界、程序映像、等待队列和同步规则。硬件条件在相关机制中按需说明，不作为独立阅读阶段。

第二部分回答“地址如何获得含义、权限和生命周期”。虚拟内存、文件映射、页缓存和内核分配器共同决定页面何时建立映射、何时写回、何时回收，以及内存压力下如何保持状态一致。

第三部分回答“异步设备如何形成文件与网络语义”。驱动、DMA、块层、VFS、文件系统和网络栈沿着 request 身份、buffer 所有权、完成记录和持久化边界逐层展开。

第四部分回答“系统在攻击、崩溃和性能异常下如何保持可解释性”。安全隔离、持久化、错误恢复与系统观测共同要求结论能够指向具体状态、权限边界、失败路径和可观察证据。

\begin{longtable}{|L{0.2\linewidth}|L{0.34\linewidth}|L{0.36\linewidth}|}
\toprule
阅读阶段 & 先问的问题 & 不合格读法 \\
\midrule
对象与执行 & 状态怎样进入和离开 runqueue、wait queue 与内核边界 & 只背调度算法或系统调用名称 \\\tablerowrule
地址与内存 & 何时验证、映射、写回和回收 & 把虚拟地址直接等同于物理内存 \\\tablerowrule
设备与文件 & 请求身份、buffer 所有权和完成记录在何处 & 把 \code{write} 成功直接等同于持久化 \\\tablerowrule
安全与观测 & 哪条证据支持契约成立或失败 & 把工具输出直接当作结论 \\
\bottomrule
\end{longtable}

学习每章时都要落到五个问题：

\begin{enumerate}
\tightlist
\item 状态在哪里：进程表、runqueue、锁等待队列、页表、VMA、fd 表、socket、inode、page cache、日志还是设备队列。
\item 硬件在哪里：RIP、RSP、RFLAGS、通用寄存器、cache、TLB、页表、MMIO 寄存器、DMA 描述符、APIC/MSI-X 还是定时器。
\item 权限在哪里：CPU 特权级、页表权限、文件权限、capability、namespace 还是对象引用。
\item 等待在哪里：runnable 等 CPU、blocked 等事件、mutex 等持有者、page fault 等页面、socket 等缓冲、I/O 等 completion。
\item 提交在哪里：\code{exec} 切换地址空间、状态更新完成、写入 page cache、块设备 flush、目录持久化还是 manifest 生效。
\end{enumerate}

每章可以按固定节奏读：先找失败场景，再看直接补丁为什么不够，再看 OS 机制如何建立状态对象，最后用不变量和证据收尾。若一节讲 fd，就要画出 fd table、open file description、inode 和 page cache 的关系；若一节讲调度，就要写出 runnable、running、blocked、zombie 和 reaped 的状态转换；若一节讲锁，就要画出锁顺序图和等待队列；若一节讲 \code{exec}，就要标出失败回滚边界和提交点；若一节讲持久化，就要写出每个崩溃点后恢复程序应该相信什么。

阅读实现小节时，不能只看伪代码能不能运行。要追问四件事：数据结构是什么，复杂度是多少，维护了哪些不变量，失败路径是否会留下半提交状态。一个教学模型可以比真实内核小，但不能把真实问题讲错。

\begin{warningbox}
不要把工具输出当成结论。工具只是证据来源；结论必须说明哪条 OS 契约被验证，哪条仍然只是合理假设。
\end{warningbox}
