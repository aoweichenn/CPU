# 操作系统演进样本

这是一册独立样稿，用来验证“从无操作系统硬件出发，每次只由一个具体失败推导一个新机制”
的叙事结构。样稿不被正式操作系统书引用，也不会改变现有书稿。

构建与手机导出分别使用：

```bash
make pdf
make phone-export
```

手机导出会把正式《操作系统第一册》和本样稿作为两个 PDF 一并写入同一目录，更新其中
任意一本都不会清理或覆盖另一本。
